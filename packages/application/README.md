# Application modules

Use cases, policy orchestration, and injected ports live under `packages/application/<domain>`. Application modules depend on contracts and domain modules; concrete adapters and UI rendering remain downstream.
