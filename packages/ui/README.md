# Design system

Typed design tokens, global cascade layers, and accessible shared primitives live here. Feature packages may consume these exports but may not redefine global tokens or introduce Tailwind, runtime CSS-in-JS, inline styles, or a second styling system.
