# Typed configuration

Environment schemas and closed configuration registries live here once introduced through an approved contract-first slice. Secrets remain server-side; browser exports are explicitly allowlisted and validated.
