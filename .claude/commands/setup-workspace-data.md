Run setup-workspace-data workflow.

Use skill: `setup-workspace-data`.
Reference: `.claude/skills/setup-workspace-data/SKILL.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
