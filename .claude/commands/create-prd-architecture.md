Run create-prd-architecture workflow.

Use skill: `create-prd-architecture`.
Reference: `.claude/skills/create-prd-architecture/SKILL.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
