name: Probe production AC209 Email Routing day counts

on:
  workflow_dispatch:
    inputs:
      source_revision:
        description: Full lowercase main revision containing this day-counts diagnostic
        required: true
        type: string
      confirm_day_counts_probe:
        description: Confirm protected read-only AC209 routing day-counts diagnostic
        required: true
        type: boolean
        default: false

concurrency:
  group: probe-production-ac209-routing-day-counts
  cancel-in-progress: false

permissions: { contents: read }

jobs:
  preflight:
    if: inputs.confirm_day_counts_probe == true && github.ref == 'refs/heads/main'
    runs-on: [self-hosted, wejammin]
    timeout-minutes: 5
    steps:
      - name: Verify dispatch intent and exact main revision
        env:
          CONFIRM_DAY_COUNTS_PROBE: ${{ inputs.confirm_day_counts_probe }}
          DISPATCHED_SOURCE_SHA: ${{ github.sha }}
          SOURCE_REVISION: ${{ inputs.source_revision }}
        run: |
          test "$CONFIRM_DAY_COUNTS_PROBE" = "true"
          test "$GITHUB_REF" = "refs/heads/main"
          test "$SOURCE_REVISION" = "$DISPATCHED_SOURCE_SHA"
      - name: Check out the exact day-counts revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.source_revision }}
          persist-credentials: false
      - name: Verify live production environment protection
        env:
          CONFIRM_VERIFICATION: ${{ inputs.confirm_day_counts_probe }}
          GITHUB_TOKEN: ${{ github.token }}
          SOURCE_SHA: ${{ inputs.source_revision }}
        run: node --experimental-strip-types infra/workflows/verify-production-environment.ts

  probe:
    needs: preflight
    runs-on: [self-hosted, wejammin]
    timeout-minutes: 10
    environment: { name: production }
    env:
      AC209_ROUTING_DAY_COUNTS_OUTPUT_PATH: ac209-routing-day-counts/day-counts.json
    steps:
      - name: Check out the exact day-counts revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.source_revision }}
          persist-credentials: false
      - name: Verify protected execution identity
        env:
          EXPECTED_SOURCE_REVISION: ${{ inputs.source_revision }}
        run: |
          checked_out_sha="$(git rev-parse HEAD)"
          test "$GITHUB_REF" = "refs/heads/main"
          test "$checked_out_sha" = "$EXPECTED_SOURCE_REVISION"
      - name: Set up pinned workspace dependencies
        uses: ./.github/actions/setup
      - name: Reverify immutable workspace before secret use
        env:
          EXPECTED_SOURCE_REVISION: ${{ inputs.source_revision }}
        run: |
          checked_out_sha="$(git rev-parse HEAD)"
          test "$GITHUB_REF" = "refs/heads/main"
          test "$checked_out_sha" = "$EXPECTED_SOURCE_REVISION"
          git diff --quiet
          git diff --cached --quiet
          test -z "$(git status --porcelain --untracked-files=all)"
      - name: Collect redacted AC209 routing day counts
        env:
          CLOUDFLARE_EMAIL_ZONE_ID: '${{ vars.CLOUDFLARE_EMAIL_ZONE_ID }}'
          CLOUDFLARE_OBSERVABILITY_API_TOKEN: '${{ secrets.CLOUDFLARE_OBSERVABILITY_API_TOKEN }}'
          SOURCE_REVISION: ${{ inputs.source_revision }}
        run: node --experimental-strip-types infra/workflows/probe-production-ac209-routing-day-counts.ts
      - name: Upload redacted AC209 routing day counts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: production-ac209-routing-day-counts-${{ inputs.source_revision }}
          path: ac209-routing-day-counts/day-counts.json
          if-no-files-found: warn
          retention-days: 7
