# Session: 2026-09-25

## Context

- Resumed from: Phase 2 Slice 09 blocked on AC209/AC211/AC265 at 279/282 active.
- Goal for session: persist the owner's 2026-09-25 AC265 identity-source and
  narrow read-grant decisions, correct the stale TOTP plan comment, and open a
  branch. Documentation/config only.

## Accomplished

- [x] Recorded the owner's **Cloud Identity Free** decision for the dedicated
  AC265 staging test identities on `wejamm.in` with no paid Google Workspace.
- [x] Recorded the owner's approval of **exactly one** narrowly scoped, expiring
  `cms.schema_registry.read` staging test-account grant as an explicit exception,
  keeping the existing owner account as sole admin with no `cms.schema_designer`
  and no admin/design permission.
- [x] Resolved gates 1-2 of the 2026-09-23 dedicated-accounts record and linked
  the two records in both directions. That record's gate 3 (separate async
  question on a privileged admin test identity) received no reply and stays
  unanswered; no second privileged identity is authorized, and it is not needed
  for the current plan.
- [x] Added `DEC-103` to `.memory/wiki/decisions.md`, an entry to
  `.memory/pipeline/progress/memory/decisions.md`, and a Slice 09 log section.
- [x] Corrected the stale `supabase/config.toml` comment claiming MFA requires
  the Supabase Pro plan.

## Deferred

- [ ] Owner-performed Google-side setup: Cloud Identity account/administrator,
  `wejamm.in` domain verification, Google terms acceptance.
- [ ] Provisioning the nonprivileged staging identities and the single expiring
  `cms.schema_registry.read` grant.
- [ ] Confirming an enrolled TOTP factor for `admin_step_up`.
- [ ] The step-up surface decision (new server-side operation vs browser Supabase
  session) remains an open owner architecture decision.
- [ ] AC209, AC211, and AC265 remain open; Slice 10 remains locked.

## Next Session

- Provision the Cloud Identity Free tenant and the nonprivileged staging
  identities under owner control, then apply the single expiring
  `cms.schema_registry.read` grant.
- Resolve the step-up surface decision before `admin_step_up` can pass.
- Route `staff_case_scoped` to Slice 09 scope rather than accounts.

## DEC-104 prelaunch production-evidence rescope

- [x] Owner directive: production-only evidence must not gate implementation
  while prelaunch — "rescope 100% if we need to collect data from production
  yet we are still in development."
- [x] Wrote the [propagation scan](../../wiki/specs/audits/propagation-scan-2026-09-25.md)
  classifying `P2-S09-AC-209` and `P2-S09-AC-211` as production-only gates,
  AC265 as still active, and AC266 as the existing deferred pre-release gate.
- [x] Authored `DEC-104`. New active denominators: Slice 09 **279/280**,
  Phase 2 **1,997 active / 2,000 authored**. `P2-S09-AC-265` is the only
  Slice 10 implementation prerequisite. All 283 authored Slice 09 IDs and
  every AC209/AC211/AC265/AC266 row remain authored and unchecked.
- [x] Distinct gate timing recorded: AC209 is a
  **production-rollout/post-initial-controlled-deployment alert gate** that
  must pass before alerting is declared ready and does not gate Slice 10 or
  the initial controlled deployment; AC211 is **post-launch operational SLO
  acceptance**, mandatory after initial launch and not a blocker of it; AC266
  remains the **pre-release** real-device gate.
- [x] Cascaded through the Phase 2 plan; Slice 09, Phase 2, index,
  spec-pipeline, and blockers trackers; architecture map; release-evidence
  runbook; progress-consistency validator; focused policy tests; and
  propagation record.
- [x] TDD: the rewritten completion-policy suite failed 7 of 8 tests against
  the prior policy, then passed 8 of 8 after the documents were corrected.
  `scripts/check-progress-consistency.mjs` was updated in the same pass and
  reports `consistent`.
- [x] Latent ledger defect found and repaired: `DEC-102` and `DEC-103` existed
  only as hand-edits of the compile-generated `.memory/wiki/decisions.md`,
  which the memory compiler regenerates from
  `.memory/raw/{events,sessions}/*.jsonl`; running the compiler silently
  deleted them (reproduced). Both were backfilled as raw records and `DEC-104`
  was authored the same way, so the ledger now compiles to 104 decisions with
  102/103/104 all present.

### Open boundary carried forward

- AC265 is the only Slice 10 prerequisite but **cannot close prelaunch** as
  implemented: its contract is staging-only, yet its declared acceptance route
  requires the combined release-evidence verifier, which pins
  `alerting.deploymentId` and `slo.deploymentId` to the expected production
  deployment. Closing AC265 therefore requires the AC209/AC211 production
  evidence it is meant to precede — the circularity this rescope removes.
- Follow-up (separate PR, recorded in `DEC-104`): add a hosted-scope
  acceptance route — a hosted-scope evidence shape and hosted-only expected
  identity without `productionDeploymentId`/`productionDeployedAt` — and route
  AC265 and AC266 acceptance through the retained hosted-report verifier plus
  the existing axe and manual verifiers. `alerting` and `slo` stay mandatory
  in the production sidecar for AC209 and AC211.
