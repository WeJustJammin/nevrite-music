## AC211 queue analytics row-shape diagnostic (local only, unmerged)

- Collection run `35846440023` (UTC day 2026-09-22) passed preflight and failed
  closed inside the Queue Analytics parser with `malformed_queue_analytics_row`
  before any sample counts were produced. Runs `35560241699` and
  `35673313035` failed later at the sample-sufficiency gate, so their date gate
  was untested and the real provider date shape stays **unverified**. The date
  format was not relaxed and no production sample was synthesized.
- The collector now names the exact rejecting gate through a shared module
  (`content-schema-registry-slo-queue-shape.ts`) that owns the row gate order and
  envelope classification, and a new read-only diagnostic
  (`diagnose-content-schema-registry-queue-shape.mjs`) reuses that classifier plus
  the collector exact query. Diagnostic output is limited to closed value
  classes, bounded counts, and the rejected gate, capped at 12 rows; it is wired
  into `collect-production-ac211.yml` as a warning-only pre-collection step.
- RED/GREEN: the two new collector tests failed at the pinned base with the bare
  message against `(row_not_object)` / `(count_sum_overflow)`, then passed.
  19/19 in the touched suites, 72/72 across the eight-file AC211 surface, and
  full local `pnpm test` 589 files / 4,835 passed plus one intentional skip.
- Local validation on pinned Node `22.23.1` / pnpm `11.24.0`: format, lint,
  type-check, contracts, progress, `db:types:check`, `test:evidence:s09`,
  build, bundle, and performance smoke all exited 0. E2E was Chrome-only against
  system Google Chrome `154.0.8037.57` using a temporary `channel: 'chrome'`
  override (101/101 functional, 5/5 real-route); the override was reverted and
  `playwright.config.ts` is byte-identical to HEAD.
- No criterion closes and no contributor count moves: Slice 09 stays 279/282
  active (283 authored IDs), Phase 2 8/17, and 1,999/2,000 active criteria.
  AC209, AC211, and AC265 remain open, Slice 10 remains locked, and AC266 stays
  owner-deferred as the mandatory post-Phase 2 production-readiness/release
  gate. The next protected dispatch is what can finally reveal the provider row
  shape; the diagnostic is a hint and never acceptance.

## AC211 real outcome shape and the Delete-only contract (local only, unmerged)

- Protected run `36038432808` (UTC day 2026-09-22) is the first run to reach the
  row gates with a real payload: three Queue Analytics rows, each with a bare-day
  `date`, a non-negative safe-integer `count`, and a three-key `dimensions`
  object. The earlier "real provider date shape stays unverified" note is
  superseded: the date, count, and dimensions gates are exercised and passing.
- The remaining failure was the outcome gate. The redacted diagnostic reported
  `ReadMessage` and `WriteMessage` with `outcomeClass: other_string` and
  `rejectionGate: outcome_shape`, while a `DeleteMessage` row reported
  `dlq` and was accepted. No raw outcome value was retained and none is
  asserted here.
- Cloudflare documents the `outcome` dimension as applicable only to
  `DeleteMessage`
  (https://developers.cloudflare.com/queues/observability/metrics/), which
  makes the read/write value inapplicable rather than an error. The shared row
  classifier now accepts an absent, null, or bounded (64-character) string
  outcome on read and write rows, never counts it, and still rejects a
  non-string, an over-long string, or the `dlq` marker on a non-delete row.
  `DeleteMessage` keeps its closed `success`/`dlq`/`fail` vocabulary, so
  `dlq` on a delete row remains the only DLQ evidence the collector accepts.
  No date, count, or dimensions gate was relaxed.
- RED/GREEN: the new suite failed at the parent commit with
  `malformed queue analytics row (outcome_shape)` on five cases, then passed
  14/14. 129/129 across the eleven-file AC211 surface. Format, lint, and
  type-check each exited 0 on the changed files, and `pnpm progress:check`
  stayed green.
- No criterion closes and no contributor count moves: Slice 09 stays 279/282
  active (283 authored IDs), Phase 2 8/17, and 1,999/2,000 active criteria.
  AC209, AC211, and AC265 remain open, Slice 10 remains locked, and AC266 stays
  owner-deferred. This fix is on an unmerged branch and changes nothing until
  it reaches `main`; only a new protected dispatch against that revision can
  produce AC211 evidence, and the diagnostic is a hint and never acceptance.

## AC209 routing day counts and the 2026-09-23 AC211 collection (documentation only)

- The bounded read-only AC209 email diagnostic `36069837542` then succeeded from
  the same exact `main` `20338c72` (`run_attempt=1`, conclusion success) and
  queried `emailSendingAdaptive` over the inclusive hour
  `2026-09-22T20:00:00Z`-`2026-09-22T20:59:59Z`, which contains the verified
  `~20:22Z` send. It returned `settings=available`, `enabled=true`,
  `requiredFieldsAvailable=true`, `rowsReturned=0` of any kind, `pageFull=false`,
  `pageTruncated=false`, classification `zero_rows`, and retained a 620-byte
  ZIP artifact (7-day retention) whose digest, over the 1,109 bytes of
  uncompressed JSON, is
  `sha256:efe7a2ee6b6be762eec5a936d4c82f22d5a17393e67ff6fc7b53a94a2e58c177`,
  recomputed locally and matching the log line.
- That clears the provider permissions/scope/query-shape condition: `zero_rows`
  is only reachable on a successful, non-truncated response, so the former
  `provider_graphql_error` does not reproduce. It establishes **no** correlated
  Sending telemetry and **no** delivered `dlq_nonempty` row. Because the evidence
  path cannot yet return a row for a known-delivered send, a fresh protected email
  exercise is deferred until it can; AC209 remains open.
- The protected read-only Email Routing day-count probe `36067233068` succeeded
  from exact `main` `20338c72` on `run_attempt=1`, verifying dispatch intent,
  `refs/heads/main`, and `SOURCE_REVISION` equality before any secret use. It
  retained a redacted 582-byte artifact (7-day retention, expiring
  `2026-10-01T22:25:22Z`) whose digest, over the 967 bytes of uncompressed JSON,
  is
  `sha256:95e3cc7378a1d3128ff7660bdf842808a175fc6c52fff07b0f522249b7a88c3a`,
  recomputed locally. Reported groups: 2026-09-22/delivered=1,
  2026-09-12/delivered=4, 2026-09-11/delivered=3, 2026-09-05/delivered=1 -
  `reportedTotalCount=9` across `distinctDays=4`, `pageComplete=true`.
- That reading is provider-reported and may be sampled; the rows carry only
  `date` and `status`, so the 2026-09-22 row cannot be attributed to the
  control alert the mailbox received near `2026-09-22T20:22Z`. Email Sending
  remains `zone_wide_missing` (zero rows in both windows), so AC209 stays open
  on a uniquely correlated provider event plus a delivered `dlq_nonempty` row
  and a retained exercise artifact.
- AC211 collection `36038007951` (UTC day 2026-09-23, dispatched from `main`
  `21929176`) passed preflight with `content_schema_registry_slo_source=passed`
  and failed closed with `commands=0`, `protectedRpcs=0`, `acceptances=0`,
  `queueFirstAttempts=0` against floors of 200/200/200/1, producing zero
  artifacts. Its queue envelope was accepted with `rowCount=0`, so production
  volume is the blocker; no synthetic volume was created.
- Cloudflare support case `02343626` was read live in Chrome and remains **New**
  with no reply; its last-modified timestamp (`2026-09-24T18:28Z`) predates the
  `22:24Z` probe.
- Documentation only: one new verification record, the Slice 09
  blocking-evidence section, this session log, and the blocker entry were
  updated. `index.md`, `spec-pipeline.md`, the phase file, and the slice header
  are unchanged. Slice 09 stays 279/282 active (283 authored IDs), Phase 2 8/17,
  and 1,999/2,000 active criteria; AC209, AC211, and AC265 remain open, Slice 10
  remains locked, and AC266 stays owner-deferred and mandatory for post-Phase 2
  production-readiness/release. No criterion closed and no count moved.

## AC209 per-event routing probe (documentation only)

- The per-event diagnostic added by PR #107 was dispatched read-only from exact
  `main` `859dc5f3` at `2026-09-25T01:44:42Z` (`run_attempt=1`, conclusion
  success), after that revision passed exact-main CI `36082498084` and staging
  `36083138344`. Preflight `107909789841` and probe `107909817033` both succeeded.
- It read `emailRoutingAdaptive` per-event rows over the same hour the Email
  Sending diagnostic asked, `2026-09-22T20:00:00Z`-`2026-09-22T20:59:59Z`, and
  returned exactly **one** row: `rowsReturned=1`, `withinWindowRows=1`,
  `outsideWindowRows=0`, `uniqueMessageIds=1`, `messageIdsMissing=0`,
  `finalEventRows=1`, `messageIdDigestCoverage=complete`. Status tally 1, action
  tally 1, both published only as label digests. Hashing candidate text recovered
  `status=delivered` and `action=unknown`; source confirms labels are digested with
  a plain SHA-256 and that malformed labels fail the run rather than being
  replaced, so `unknown` is provider-reported.
- Retained artifact (7-day retention, expiring `2026-10-02T01:46:02Z`): GitHub
  reports **809 bytes** (ZIP, digest
  `sha256:01fa45c3d8d9060d75dcaca2f6083dbc069b28b8fc477d5173f1ef00c59aed3f`);
  the digested payload is the **1,248-byte** uncompressed JSON with
  `sha256:914c65bf537cfa7c3ed8e10a77bde1fa86370de3904ef915c051c9f3f278dac2`.
  Both recomputed locally and both match; the log line's digest is over the JSON.
- AC209 stays open: there is no comparable send-side message identifier, so the
  single row is not attributable to the control alert, the `action` transport is
  `unknown`, and no delivered `dlq_nonempty` alert row exists. No score moved:
  Slice 09 stays 279/282 active (283 authored IDs), Phase 2 8/17, and
  1,999/2,000 active criteria; Slice 10 remains locked and AC266 owner-deferred.
