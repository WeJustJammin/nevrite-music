# Session — 2026-08-31

## Phase 1 main promotion and staging artifact remediation

- [x] Merged reviewed PR #2 to `main` at
      `be36d68c495b9a244dac4fd29c24a83e0c68ce7a` after explicit owner approval.
- [x] Main CI run `33357532073` passed quality, database, clean immutable build,
      and exact artifact upload for the merge SHA.
- [x] Staging run `33357943998` downloaded that exact artifact and stopped
      safely before deployment at the artifact-boundary verifier.
- [x] Confirmed the archive retains `apps/web` and `apps/worker`, while the
      verifier and deploy commands expected `web` and `worker` at archive root.
- [x] Added red-to-green deployment contracts, corrected verifier and deploy
      paths to retain `apps/`, and added explicit missing-artifact errors across
      staging and the downstream production promotion candidate.
- [x] Verified the corrected boundary script against the actual main-run
      artifact and passed `pnpm validate` with 148 files/871 tests, 100% coverage,
      21 Playwright tests, all static/build gates, and zero Node warnings.
- [x] Corrected production clean-runner order to checkout, download, validate,
      then setup; a red-to-green identity contract verifies the candidate survives.
- [x] Merged reviewed artifact-path/order PR #3 to `main` at
      `1bb432358cc44f992ce3d15597d298cce86d0a72` after explicit owner approval;
      exact main CI run `33359583799` passed.
- [x] Staging run `33359752069` verified and downloaded the exact artifact,
      deployed the API and web scripts, then failed the web custom-domain trigger
      because legacy Pages project `wejammin-web-staging` still owns
      `staging.wejamm.in` (Cloudflare code `100117`).
- [x] Live Worker tail identified missing `SUPABASE_SECRET_KEY` and
      `SUPABASE_URL` plus strict parsing of the `PLATFORM_JOBS` resource binding;
      sanitized run, API, routing, and tail evidence is preserved in
      `.memory/wiki/specs/audits/phase-1-staging-runtime-evidence.md`.
- [x] Added red-to-green server projection, secure shared deployment script,
      staging Queue isolation, and a dynamic SSR smoke proof that rejects the stale
      Pages shell.
- [x] Passed `pnpm validate` with 148 files/877 tests, 100% coverage, 21
      Playwright tests, all static/build gates, and zero Node warnings.
- [ ] Commit, review, and merge the runtime remediation; then detach the
      staging-only Pages domain/DNS record only with explicit owner authorization
      and require successful exact-SHA staging promotion before readiness.
