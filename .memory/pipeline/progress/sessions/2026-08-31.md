# Session — 2026-08-31

## Phase 1 main promotion and staging artifact remediation

- [x] Merged reviewed PR #2 to `main` at
      `be36d68c495b9a244dac4fd29c24a83e0c68ce7a` after explicit owner approval.
- [x] Main CI run `33357532073` passed quality, database, clean immutable build,
      and exact artifact upload for the merge SHA.
- [x] Staging run `33357943998` downloaded that exact artifact and stopped
      safely before deployment at the artifact-boundary verifier.
- [x] Confirmed the archive retains `apps/web` and `apps/worker`, while the
      verifier and deploy commands expected `web` and `worker` at archive root.
- [x] Added red-to-green deployment contracts, corrected verifier and deploy
      paths to retain `apps/`, and added explicit missing-artifact errors across
      staging and the downstream production promotion candidate.
- [x] Verified the corrected boundary script against the actual main-run
      artifact and passed `pnpm validate` with 148 files/871 tests, 100% coverage,
      21 Playwright tests, all static/build gates, and zero Node warnings.
- [x] Corrected production clean-runner order to checkout, download, validate,
      then setup; a red-to-green identity contract verifies the candidate survives.
- [x] Merged reviewed artifact-path/order PR #3 to `main` at
      `1bb432358cc44f992ce3d15597d298cce86d0a72` after explicit owner approval;
      exact main CI run `33359583799` passed.
- [x] Staging run `33359752069` verified and downloaded the exact artifact,
      deployed the API and web scripts, then failed the web custom-domain trigger
      because legacy Pages project `wejammin-web-staging` still owns
      `staging.wejamm.in` (Cloudflare code `100117`).
- [x] Live Worker tail identified missing `SUPABASE_SECRET_KEY` and
      `SUPABASE_URL` plus strict parsing of the `PLATFORM_JOBS` resource binding;
      sanitized run, API, routing, and tail evidence is preserved in
      `.memory/wiki/specs/audits/phase-1-staging-runtime-evidence.md`.
- [x] Added red-to-green server projection, secure shared deployment script,
      staging Queue isolation, and a dynamic SSR smoke proof that rejects the stale
      Pages shell.
- [x] Passed `pnpm validate` with 148 files/877 tests, 100% coverage, 21
      Playwright tests, all static/build gates, and zero Node warnings.
- [x] Committed and pushed runtime remediation
      `b6999ea58d17321117f555bd49ccb1b839d7704c`; opened PR #4. Exact push CI
      `33363038760` passed quality, database, immutable build, and artifact upload.
- [x] Diagnosed simultaneous PR run `33363059831`: database passed, but its
      quality job stopped before browser tests because the duplicate exact-SHA
      run already owned Playwright port 4321. Added a red-to-green contract and
      per-run web/docs ports derived from validated `GITHUB_RUN_ID`; the docs
      request consumes the same dynamic origin through Playwright metadata, and
      CI disables shared Cloudflare inspector/persistence state. Concurrent
      reproductions using both original run IDs pass 42/42 browser tests.
- [x] Committed and pushed the port-isolation remediation as
      `b1000143852dd8e97c2fd2fb69136e313f747ec6` for exact managed-runner proof.
- [x] Exact `b1000143852dd8e97c2fd2fb69136e313f747ec6` runs `33365214572`
      and `33365216682` both passed database and avoided HTTP, inspector, and
      persistence collisions; overlapping coverage workloads instead timed out
      an architecture scan and an environment-build contract.
- [x] Added red-to-green scheduling contracts: feature branches validate through
      the PR event only, main retains push validation, and repository CI runs use
      one concurrency group on the shared self-hosted host. The group prevents
      overlapping active jobs, preserves the running job, and retains GitHub's
      latest pending run rather than guaranteeing every queued SHA executes.
- [x] Pushed the single-run scheduling remediation at exact revision
      `dd6692b718a83ac1b64fad915b5eb6b0aca2c5e2`; its sole CI run `33420700305`
      passed quality, database, and immutable-artifact jobs, with no duplicate
      feature-branch push run.
- [x] After explicit owner approval, removed only the staging Pages custom-domain
      attachment and exact conflicting CNAME; no production DNS/runtime changed.
- [x] Merged PR #4 as `c2880f34a3127235b859d69e89dc8129d0746d6d`.
      Main CI run `33425577715` passed quality, database, and immutable build.
- [x] Staging run `33425837272` attempt 2 deployed that exact merge and passed
      API 200, web 200, and protected-route 303 verification. Isolated staging
      Queue/DLQ resources and the Worker custom domain are present.

## Phase 1 readiness validation

- [x] Completed the mandatory gate-applicability matrix.
- [x] Passed OpenAPI synchronization: six paths/operations, ten schemas, focused
      contract/toolchain tests 12/12, and `pnpm contracts:check`.
- [x] Reconciled all 390/390 operational criteria and 488/488 source links;
      feature ledger 776/776 is complete and intentionally has zero Phase 1
      domain-feature rows.
- [x] Audited active boundary stubs: zero `BOUNDARY:`, TODO/FIXME/HACK,
      skipped-test, or not-implemented markers in active surfaces.
- [x] Passed full and production dependency audits with zero vulnerabilities at
      every severity; secret/SAST/workflow scans found no credential or critical
      unsafe-code finding.
- [ ] Accessibility readiness: automated 14 files/72 tests and 21/21 browser
      tests pass, but upload-admission focus targets and upload-completion success
      announcement/focus violate two P1 contracts.
- [ ] Performance readiness: workbench island is 53,399 bytes gzip against 35 KB
      and route JS is 113,541 bytes against 90 KB; Phase 1 p95-smoke evidence is
      absent. `PERF-DEFER-01` defers only the representative-data load profile.
- [ ] Security readiness: staging serves HTTP 200 without HTTPS redirect and
      omits all six locked response-security headers.
- [ ] Production-promotion protection: automatic run `33426269934` stopped
      before migrations/deploy on empty `PRODUCTION_WEB_ORIGIN`, but the created
      `production` environment has zero reviewer/branch protection rules.
- [x] Wrote the consolidated FAIL report and constrained remediation sequence in
      `.memory/wiki/specs/audits/phase-1-validation.md`.

## Phase 1 readiness blocker remediation

- [x] Repaired upload-admission summary/focus IDs, upload-completion success
      announcement/focus, live-region politeness, table sort semantics, and
      route-heading focus; retained focused accessibility regressions.
- [x] Replaced the optimistic bundle check with emitted-graph closure accounting
      and explicit-action deferral. Fresh build: complete Workbench hydration
      closure 14,913 gzip bytes, initial route 72,576, largest lazy/shared chunk
      27,884.
- [x] Added the deterministic 20-request, one-user, zero-retry API p95 smoke to
      local validation, CI, and staging promotion. Fresh p95 is 1.584 ms with
      zero errors.
- [x] Bound bundle and p95 evidence to source revision, locked thresholds, and
      `passed: true`; staging and production candidate verification reject
      malformed evidence and superseded staging runs.
- [x] Added fail-closed HTTP 308 and exact locked security-header boundaries for
      web, API, generated Worker, static assets, redirects, 404s, and protected
      500s. Development uses a separate Wrangler config so Astro/Vite modules
      hydrate without weakening production or staging routing.
- [x] Removed automatic production promotion, added immutable staging-run/SHA
      verification and live environment-policy checks, configured exact-main
      deployment policy, and disabled the production workflow.
- [x] Disabled production administrator bypass while retaining the sole exact
      `main` branch policy; live environment reports `can_admins_bypass: false`.
- [ ] GitHub required reviewer: API returned HTTP 422 because the current plan
      does not support reviewer protection for this private repository. Workflow
      remains disabled and fail-closed.
- [x] Fresh `pnpm validate`: 158 files/939 tests, 100% coverage, 26/26 browser
      tests, all static/build gates, bundle budgets, and p95 smoke passed.
- [x] Fresh `pnpm audit --audit-level=high`: no known vulnerabilities.
- [ ] Push exact remediation SHA, pass CI, and recertify redirects, six headers,
      static assets, protected route, and p95 on live staging.
