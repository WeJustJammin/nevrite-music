# Session 2026-09-13

## AC265 candidate provenance preflight and read-only workflow scaffold

- PR #71 merged at `origin/main` SHA
  `03329530ce1375de63d1d5a0ecee6b5ce8ccb50d`. Exact-main CI
  `34776703106` attempt 1 passed. Exact-main staging `34777077865` attempt 1
  passed with deployment `6425379348`; candidate artifact
  `staging-verified-candidate` (`10323417853`) passed read-only hosted
  preflight run `34777223023` with `candidate_provenance_verified`.
- PR #71 added a read-only main-only preflight workflow, strict candidate provenance,
  authenticated policy mapping and outage-target contracts, and a fail-closed
  collector port boundary. This remains scaffolding: no protected
  collector/service protocol or hosted acceptance exists, and AC265 is not
  closed.
- Focused AC265 verification passes 9 files / 68 tests, including the public
  contract-export guard. Format, lint, and
  type-check pass. `pnpm validate` passes 514 Vitest files / 4,022 tests plus
  one intentional skip at 100% coverage, the Slice 09 evidence gate, 101
  functional plus 5 recovery Playwright checks, workspace builds, bundle
  budgets, and performance smoke. `pnpm db:verify` passes 50 pgTAP files /
  1,818 tests with migrated database types matching.
- No hosted acceptance item closed. Slice 09 remains **279/283** with depth
  ratio **0.986**; Phase 2 remains **8/17**. AC209, AC211, AC265, and AC266
  remain open, and Slices 10–17 remain dependency-locked.

## Phase 2 Slice 09 external-gate continuation

- Refreshed authoritative GitHub state. Exact main
  `395a4253a60241171b0cb15f845c57af771ce46b` passed CI `34736688510`
  and staging `34736997698` / deployment `6417505372`. Production remains
  on `c8f0cbd52cb6140ee1a756f106fa329f8c23b0e2` through run
  `34734646414` / deployment `6417116181`.
- AC209 collector `34734943588` passed on the current production release.
  Exercise `34735006559` failed before queue mutation at the Email Sending
  GraphQL capability check. The observability token requires its existing
  Workers Observability Write and Account Analytics Read permissions plus Zone
  Analytics Read scoped only to `wejamm.in`. No receipt was inferred.
- Added and hardened AC266 protected manual-report intake and verification
  infrastructure. Strict report contracts bind the two real platform tuples,
  authenticated CMS workbench, UTC timing, all 11 structured checks, report
  bytes, candidate run/attempt, deployment history, origin, and trusted cutoff.
  Raw reports are private inputs; only sanitized manifests are artifacts.
- Created GitHub environment `ac266-manual-evidence` (ID `21821361680`)
  with exact staging origin, `main` policy, required reviewer
  `WeJustJammin`, and administrator bypass disabled. No AC266 report secret
  or physical-platform evidence is present.
- Moved the AC266 report-secret jobs to GitHub-hosted `ubuntu-24.04` runners
  after verifying that the three existing `wejammin` self-hosted runners are
  persistent services shared with same-repository PR CI.
- Independent focused AC266 verification passes 12 files and 136 tests. Clean
  local `pnpm validate` passes 486 Vitest files, 3,785 tests plus one intentional
  skip, exact 100% coverage, all executable Slice 09 evidence checks, 101
  functional and five production-built browser tests, builds, bundle budgets,
  and performance smoke. `pnpm db:verify` separately passes 50 pgTAP files and
  1,818 tests with migrated type parity. Exact-main CI/staging remain pending
  for this worktree.
- The current published staging candidate predates the new run-attempt
  identity sidecar, so a fresh exact-main staging run is required before AC266
  collection can pass. The sanitized AC266 manifest also remains a prerequisite
  rather than an automatic input to the combined four-gate verifier.

No external acceptance item closed. Slice 09 remains **279/283** with depth
ratio **0.986**. AC209, AC211, AC265, and AC266 remain open, and Slices 10–17
remain dependency-locked.

## Exact-main AC209 production retest and diagnostic hardening

- The AC266 prerequisite work superseded the earlier pending note: exact main
  `47b5ff2ca788f4470254c0161e636246719b98da` passed CI `34749050376` and
  staging `34749287577` / deployment `6419900767`. The staging candidate binds
  its run and attempt explicitly.
- Restored administrator bypass to disabled on protected environment
  `ac266-manual-evidence` (`21821361680`) after live-state drift. The exact
  business-account reviewer, allowed owner self-review, and custom `main`
  branch policy were preserved; no report secret was created or read.
- Production attempts `34749383380` and `34749687614` passed identity/protection
  preflight and protected approval, then stopped before any migration or
  deployment at the zone Email Sending GraphQL capability check. Both emitted
  only sanitized `provider_graphql_error`; production stayed on
  `c8f0cbd52cb6140ee1a756f106fa329f8c23b0e2`.
- Added secret-safe classification for HTTP-200 Cloudflare GraphQL errors.
  Explicit auth phrases, fixed schema/resource phrases, mixed classes, unknown
  details, oversized details, and misleading field names are covered through
  RED→GREEN tests. Provider messages, zone identifiers, and tokens are never
  included in the public error.
- Final local validation passes 486 Vitest files / 3,789 tests plus one skip at
  exact 100% coverage, all Slice 09 evidence gates, 101 functional plus five
  production-built Playwright checks, builds/budgets/performance, and 50 pgTAP
  files / 1,818 database tests with type parity.

The token edit remains ineffective or unsaved; no delivery receipt was
inferred. AC209, AC211, AC265, and AC266 remain open at **279/283**, and Slices
10–17 remain locked.

## AC209 production attempt 2 and AC265 local contract hardening

- Exact main SHA `5c1af8cb7be676ec3e0bca4be5f28ceb91aeb776` passed CI
  `34751474024` and staging `34751910125`.
- Protected production run `34752000687`, attempt 2, received environment
  approval and then failed closed at the Cloudflare Email Sending capability
  check before any migration or deployment. Production remains unchanged on
  `c8f0cbd52cb6140ee1a756f106fa329f8c23b0e2` / deployment `6417116181`.
  Cloudflare Email Sending domain onboarding/DNS remains required; no genuine
  delivery receipt was produced.
- Added the local `ac265-hosted-runner-v1` trust-boundary contract and hardened
  AC265 report/receipt time checks: caller-supplied maximum run duration,
  report-window and trusted-cutoff bounds for every receipt, and cleanup receipt
  issuance at or after cleanup completion. Focused temporal-coherence Vitest:
  68/68 passing.
- Final adversarially hardened AC265/retained-evidence verification passes 22
  files / 201 tests. Full `pnpm validate` passes 507 Vitest files / 3,981 tests
  plus one intentional skip at exact 100% coverage, all executable Slice 09
  evidence gates, 101 functional plus five real-Slice-09 Playwright checks,
  builds, bundle budgets, and performance smoke. Separate `pnpm db:verify`
  remains green at 50 pgTAP files / 1,818 tests with database-type parity.
- These are local contract and test results only. No protected hosted runner
  execution or complete AC265 acceptance report is claimed. AC209, AC211,
  AC265, and AC266 remain open at **279/283** with depth ratio **0.986**;
  Slices 10–17 remain dependency-locked.

## AC211 provenance and AC266 report-preparation tooling

- PR #74 merged at exact `main` SHA
  `f8e10db06b5e70ea6262a3531f7fab69965febc3`. Exact-main CI `34792878043`
  and staging `34793282345` / deployment `6428325619` passed. Immutable build
  artifact `10329300135` and `staging-verified-candidate` artifact
  `10328985796` are retained. The release adds bounded, canonical GitHub
  deployment provenance and status-history checks for AC211, plus a local
  template and bounded report-preparation CLI for the two AC266 platform
  reports. This is landed evidence tooling, not hosted acceptance.
- Final local validation passes: `pnpm validate` exits 0 with 522 Vitest files,
  4,091 passed and 1 skipped, and 100% coverage (12,634 statements, 9,578
  branches, 2,099 functions, 11,747 lines). Playwright passes 101 functional
  plus 5 real-route checks. `pnpm db:verify` passes 50 pgTAP files / 1,818
  checks with migrated types matching. Focused AC211 passes 59/59; focused
  AC266 passes 83/83. Adversarial reviews have no remaining findings.
- Tracking checks pass after this update: `pnpm progress:check`, Prettier check
  on the four updated records, and `git diff --check`.
- No external acceptance item closed. AC209 still lacks a genuine delivery
  receipt after the protected exercise failed at Cloudflare's Zone Analytics
  Email Sending permission check. AC211's earliest eligible complete UTC day is
  2026-09-14 and earliest dispatch is 2026-09-15 00:01Z, subject to natural
  sample floors. AC265 still needs the accepted hosted matrix and identity
  lifecycle evidence; AC266 still needs genuine VoiceOver/Safari and
  NVDA/Firefox results. Slice 09 remains **279/283** with depth ratio **0.986**;
  Phase 2 remains **8/17**, and Slices 10–17 remain dependency-locked.
