# Session 2026-09-21

## AC266 owner-deferred phase propagation and AC211 collection

- The authored Slice 09 ledger remains **283 IDs**. Excluding unchecked,
  owner-deferred AC266 from active Phase 2 completion leaves Slice 09 at
  **279/282 active** and Phase 2 at **1,999 active criteria out of 2,000
  authored**.
- AC266 remains owner-deferred because the required real devices are
  unavailable. It is excluded from active Phase 2 completion, but remains
  mandatory for post-Phase 2 production-readiness/release and is not accepted
  or waived.
- AC211 run `35560241699` passed preflight, but collection failed for
  insufficient samples: `commands=0`, `protectedRpcs=0`, `acceptances=0`, and
  `queueFirstAttempts=0`. No artifact was produced; AC211 remains open.
- AC209, AC211, and AC265 remain the only active Slice 10 blockers. Slice 10
  remains locked on those three criteria; AC266 does not block Slice 10
  implementation while remaining a post-Phase 2 release gate.
- The policy change followed RED to GREEN: the new four-test policy suite first
  failed all four tests, then the focused policy/traceability set passed 20
  tests with one intentional skip.
- Full validation under Node `22.23.1` and pnpm `11.24.0` passed 536 Vitest
  files / 4,234 tests plus one intentional skip at 100% coverage, 101
  functional browser checks, five production-built Slice 09 checks, contracts,
  database types, progress, formatting, lint, type checks, builds, bundle
  budgets, and performance smoke (`p95=1.527671 ms`, threshold `500 ms`, zero
  errors).
- Database verification passed 52 pgTAP files / 1,917 tests with generated
  types matching the migrated schema. The graph compiler refreshed 1,632 nodes
  / 10,125 edges; all 55 warnings were pre-existing.
- Housekeeping PR #81 merged at exact-main SHA
  `f8a6b77e529c7b150df367f5ce59e43b1aed0db1`; CI `35560004822` and staging
  `35560514952` / deployment `6561553954` passed.

## AC265 CP-01 one-use outage-lease control plane

- Added strict staging-only acquire, consume, and release contracts plus private
  forced-RLS approved-target and one-use lease ledgers. Service-role-only,
  fixed-`search_path` RPCs own the lifecycle and derive timestamps and digests.
- TDD moved from missing-contract/RPC RED to 2 contract files / 8 tests, 80
  main pgTAP assertions, and 3 two-connection concurrency assertions passing.
- `pnpm db:verify` passed 54 pgTAP files / 2,000 assertions with generated type
  parity. Full `pnpm validate` passed 537 Vitest files / 4,241 tests plus one
  intentional skip at 100% coverage (13,048 statements, 9,828 branches, 2,152
  functions, and 12,129 lines), 101 functional browser checks, five
  production-built Slice 09 checks, builds, bundle budgets, and performance
  smoke (`p95=1.190315 ms`, threshold `500 ms`, zero errors).
- The checkpoint seeds no approved target, mounts no public Worker route, runs
  no live fault or hosted browser matrix, and produces no independently signed
  receipt. AC265 remains open; AC209 and AC211 remain open; Slice 10 remains
  locked. AC266 remains unchecked and owner-deferred to the mandatory
  post-Phase 2 production-readiness/release gate.
- Next AC265 dependency: CP-02 protected runner-mapping and approved-safe-resource
  registry, followed by broker, evidence resolver/receipt, hosted workflow, and
  genuine report verification.
- Detailed evidence:
  [AC265 CP-01 verification](../verification/2026-09-21-ac265-outage-lease-control-plane.md).
- PR #83 merged CP-01 as exact-main
  `05e88ea52f1c9cf206d54f455e53bc849044cb9b`; CI `35569923623` and staging
  `35570556554` passed, producing successful staging deployment `6563225343`.
  Promotion proves the private foundation only and leaves AC265 open.

## AC265 CP-02 approved safe-resource and runner-mapping registry

- Added the local, staging-scoped CP-02 contract and private forced-RLS
  registry foundation. Service-role-only RPCs return redacted opaque resource
  references and complete role/scenario mapping envelopes while deriving
  candidate/run/identity/source/deployment/project scope. No raw resource
  contents, locators, credentials, or tokens are retained; the migration seeds
  no registry rows and is not promoted evidence.
- Focused CP-02 contract verification passed 2 files / 11 tests. Broader AC265
  contract verification passed 4 files / 31 tests. Targeted CP-02 SQL passed
  main plus concurrency suites: 2 files / 65 assertions.
- The first full validation run exposed 99.96% branch coverage in the new schema.
  Impossible defensive branches were refactored, focused schema coverage reached
  100%, and the clean full `pnpm validate` pass then succeeded: 538 Vitest files,
  4,250 passed + 1 intentional skip (4,251 total), 100% coverage with 13,093
  statements, 9,836 branches, 2,156 functions, and 12,174 lines; the Slice 09
  evidence command, 101 functional E2E checks, 5 production-built checks,
  builds, bundle budgets, and performance smoke (`p95=1.402399 ms`, threshold
  `500 ms`, zero errors) also passed. Lint reported only pre-existing warnings.
- Full `pnpm db:verify` passed 56 pgTAP files / 2,065 assertions with generated
  types matching the migrated schema.
- CP-02 does not authenticate mapping provenance, verify underlying resource
  contents, expose a hosted route, issue an independently authenticated receipt,
  or run browser acceptance. AC265 remains open at 279/282 active; Slice 10
  remains locked on AC209, AC211, and AC265.
- Next AC265 dependency is CP-03's independently authenticated canonical
  mapping/resource source, followed by the run-scoped broker, evidence/receipt
  resolver, protected hosted workflow, and genuine hosted execution.

## AC265 CP-03 approved runner-mapping attestation

- Added the local signed runner-mapping attestation boundary for the exact
  `ac265-approved-runner-mappings-v1` bytes. It binds mapping ID, run ID,
  canonical mapping SHA-256, trusted key ID, Ed25519 signature, domain
  separator, and bounded validity; duplicate/noncanonical JSON, invalid key
  windows, stale reports, and mismatched canonical bytes fail closed.
- Local focused verification covers **44 AC265 files / 392 tests**. Both
  `pnpm type-check` and `pnpm lint` pass for the current CP-03 changes. The
  final exact-runtime `pnpm validate` passed **543 Vitest files / 4,312 passed
  - 1 intentional skip (4,313 total)** at 100% coverage (**13,101 statements,
    9,840 branches, 2,157 functions, and 12,182 lines**), 101 functional browser
    checks, five production-built checks, builds, bundle budgets, and local
    performance smoke (`p95=1.277622 ms`, threshold `500 ms`, zero errors).
- Final hardening also covers UUID-v4 mapping-ID alignment, awaited
  response-body cancellation, realpath/symlink-safe execution, an unnamed Linux
  `O_TMPFILE` writability preflight, no-follow held descriptors, summaries
  constrained beneath `RUNNER_TEMP`, and deletion-free fail-closed handling
  that preserves only private runner-local remnants. Current
  `pnpm db:verify` passed; `pnpm db:test` passed exactly **56 files / 2,065
  assertions**.
- The trusted-key list, trusted cutoff, and maximum run duration are trusted
  release-policy context preconditions. A future protected orchestration
  constructor must source them from authenticated release/deployment policy
  context; no current untrusted caller exists.
- The CP-03 verification record is
  [AC265 CP-03 approved runner-mapping attestation](../verification/2026-09-21-ac265-approved-runner-mapping-attestation.md).
  CP-03 was then promoted as code: PR #85 merged at exact main SHA
  `a94ffbca3d41da703218dff12ee7527f31c23a34`; exact-main CI
  `35592046696` passed; staging workflow `35592722418` reached endpoint
  verification/deploy, failed its first attempt at the immediate Cloudflare
  provider-evidence query, and succeeded on one failed-job retry with no
  provider configuration change; deployment `6567092259` succeeded at
  `https://staging.wejamm.in`. This promotion is not hosted acceptance:
  no live signing-key configuration, approved registry rows, retained
  mapping/attestation artifact, attestation workflow run, independently
  authenticated receipt, browser matrix, or AC265 acceptance exists. AC265
  remains open at 279/282 active; Slice 10 remains locked on AC209, AC211,
  and AC265. AC266 remains unchecked and deferred to the mandatory post-Phase 2
  production-readiness/release gate.
- Remaining AC265 dependencies are the live protected mapping/resource source
  and target source, run-scoped session broker, MFA/step-up and Auth/RLS/IdP
  execution, evidence/receipt resolver, teardown, isolated hosted workflow,
  and accepted 9-role/10-scenario report.

## AC265 CP-04a approved outage-target attestation

- Added the now-promoted approved outage-target read and attestation
  boundary. The strict service-role-only
  `ac265_approved_outage_target_read` RPC reads CP-01 target rows and returns a
  redacted canonical target projection with the stored `targetSha256`.
- Added the canonical `ac265-approved-outage-target-v1` contract and a
  distinct domain-separated Ed25519 envelope binding the exact target bytes,
  UUID-v4 target/reference, run-scoped hosting/Supabase/deployment/dependency/
  route scope, key ID, and validity window. The protected manual main/staging
  entrypoint and workflow are fail-closed; the verifier and policy require the
  signed target and do not accept a caller-provided authenticity callback.
- Focused AC265 verification passes **54 files / 483 tests** with
  `pnpm type-check` green. Exact-runtime `pnpm validate` exits 0 with **549
  Vitest files, 4,366 passed + 1 intentional skip (4,367 total)**, 100%
  coverage, 101 functional Chromium checks, five production-built checks,
  green builds/bundle checks, and local API p95 **1.377056 ms**.
- After a clean reset, all `pnpm db:verify` components are green: **57 pgTAP
  files / 2,087 assertions**, database lint, and generated-type checks pass.
- Independent security review found no CP-04a blocker; a protected orchestrator
  remains a required trust boundary. PR #86 / exact-main SHA
  `4fa8691d24177d0a528335f3c3d06ef50d67d3a9`, CI `35597438023`, and staging
  workflow `35598236704` / deployment `6568074493` are green.
- No live target-signing key/configuration, seeded CP-01 target or registry
  rows, retained target/attestation artifact, attestation workflow run, hosted
  browser matrix, independently authenticated receipt, or AC265 acceptance
  exists despite code promotion. AC265 remains open at **279/282 active**; Slice 10 remains locked on
  AC209, AC211, and AC265. AC266 remains unchecked and owner-deferred at the
  mandatory post-Phase 2 production-readiness/release gate.
- See the [CP-04a verification record](../verification/2026-09-21-ac265-approved-outage-target-attestation.md).

## AC265 CP-04b approved outage-target registration (promoted private foundation)

- Added the local approved-outage-target registration contract, bounded
  service-role-only RPC, and forward-only migration. The policy and
  registration ledgers are immutable and empty; server policy and authenticated
  authorization/candidate context derive the target scope, while exact
  correlation and idempotency references prevent caller-selected target data.
  No live policy or target is seeded and no hosted route is exposed.
- The RPC client suite passes **15 tests**; together with the registration
  contract/public-export tests this is **3 files / 24 tests**. Registration SQL
  passes **35 pgTAP assertions**, including direct registration-to-lease
  acquisition proving that a registered target remains valid for the exact
  CP-01 60-second lease. The separate two-connection concurrency proof passes
  **2 assertions**.
- The policy requires **exact 120-second target validity**, leaving a bounded
  60-second acquisition window before the exact 60-second lease; future-dated
  or too-short policy windows return generic conflict.
- Final canonical `pnpm validate` is current-final at **551 Vitest files, 4,387
  passed + 1 intentional skip**, with **13,143/13,143 statements, 9,850/9,850
  branches, 2,160/2,160 functions, and 12,224/12,224 lines** (100%). The
  evidence-map gate passed; Playwright passed **101 functional + 5 production-built
  Slice 09 real-route checks**. Builds, bundle budgets, and performance are
  green; API p95 is **1.491154 ms**. Fresh `pnpm db:verify` is current-final at
  **59 pgTAP files / 2,124 assertions**; database lint exits 0 with **46
  longstanding warnings** (39 never-read, 6 unused, 1 immutable/stable), and
  generated database types match. Architecture compile passed **1,632 nodes /
  10,125 edges** with 55 known lint issues.
- Initial PR #88 CI run `35609574745` exposed an existing scheduling-dependent coverage
  branch in the profile-ownership in-flight coalescing test: one arbitrary
  `setImmediate` allowed the changed request to overtake the matching request on
  the hosted runner. The test now waits for the second rate-limit boundary before
  dispatching the changed request. Both in-flight branches were covered in five
  consecutive isolated runs, and the full **551-file / 4,387-pass** coverage gate
  returned to 100% locally before the replacement CI run.
- PR #88 promotion is green at implementation main SHA
  `52b66272e61331827c59ac1e169868474a2c09c8`, PR CI `35611484121`, exact-main
  CI `35612415141`, staging `35613284966`, and deployment `6570861931`.
  Promotion artifact `10645302055` has digest
  `sha256:12f05e8371586996dc413b85b75167934045f342091defff5197435b8b707782`;
  staging p95 was **32.589357 ms** and automated axe digest was
  `df2522f8512dcea587146f5bce43ad5232b94964d5048825ffdb745286dd772d`.
- CP-04b is promoted as code/staging evidence only and has no live
  policy/target, signing-key configuration, retained target/attestation/evidence
  artifact, hosted matrix, independently authenticated receipt, or AC265
  acceptance. Read-only AC209 verifier `35612514031` failed with
  `provider_graphql_error` after all preflight/protection/workspace gates and
  produced no effects or receipt. AC265 remains open at **279/282 active**;
  Slice 10 remains locked on AC209, AC211, and AC265, and AC266 remains
  unchecked, owner-deferred, and mandatory at the post-Phase 2
  production-readiness/release gate.
- See the [CP-04b verification record](../verification/2026-09-21-ac265-approved-outage-target-registration.md).

## AC265 CP-04c hosted artifact attestation and branded resolver (local/private only)

- Added strict Ed25519 attestation over exact artifact bytes and a branded
  resolver that enforces exact artifact kind/reference/key/subject/run/
  candidate/runner bindings. The resolver bounds its source set at **256**
  entries and fails closed outside exact membership.
- Promoted the private foundation in PR #90 at exact main SHA
  `e7525fa9ea80bbdf2325e8ce08d18930d6485b15`; exact-main CI `35634692281`
  succeeded, and staging workflow `35635650934` failed on `run_attempt=1`
  only at transient web release-identity propagation before succeeding on
  `run_attempt=2`.
  Deployment `6574859596` succeeded at `https://staging.wejamm.in`; the exact
  staging endpoints now serve `e7525fa9ea80bbdf2325e8ce08d18930d6485b15`,
  and the CP-04c promotion record predates the CP-04d retry hardening later
  promoted in PR #91. Candidate artifact `10656615428` has digest
  `sha256:5568778c8bec9eacd8090ae2020518caa070ce82b1a901aa4c7fe9a95f03d592`;
  deployment evidence artifact `10655784856` has digest
  `sha256:02422c5ef8b4988fe50bdc7d02771c286ac81c148e51bac6bc80927736ec461d`;
  staging p95 was `49.30431599999997 ms` and automated axe digest
  `00968b6a806db4993ab895f83fcb592af81a65ec925f9b38e6aa3140c0f87d87`.
- CP-04c is a locally validated private construction foundation only. Upstream
  authenticated manifest/registry/run authority and the external replay ledger
  remain open; no hosted artifact, hosted matrix, independently authenticated
  receipt, or AC265 acceptance is claimed. Focused local verification passes
  **5 files / 74 tests** and `pnpm type-check` is green.
- The first full-validation attempt was invalidated by a concurrent coverage
  directory cleanup race, not by a test failure. An isolated rerun passed:
  **556 files; 4,461 passed and 1 skipped of 4,462; 13,156/13,156 statements,
  9,854/9,854 branches, 2,161/2,161 functions, and 12,237/12,237 lines**
  (100%). Slice 09 nonbrowser evidence passed; Playwright passed **101/101
  functional checks** and **5/5 Slice 09 real-route checks**. Builds and bundle
  budgets were green, and performance smoke recorded p95 **1.240784 ms** against
  the **500 ms** threshold.
- `pnpm db:verify` passed after a full local reset and migrations through
  `20260921040000`; database lint completed with warnings, pgTAP reported **59
  files / 2,124 tests** all successful, and generated database types matched
  the migrated schema.
- No totals or gates changed: Slice 09 remains **279/282 active** (**283
  authored IDs**), Phase 2 remains **8/17** with **1,999/2,000 active
  criteria**, AC209/AC211/AC265 remain open, Slice 10 remains locked, and
  AC266 remains owner-deferred as the mandatory post-Phase 2
  production-readiness/release gate.

## AC265 CP-04d signed source-manifest and authority foundation (local/private only)

- Added the strict signed artifact-source manifest contract with canonical
  code-point ordering, exact source references, a server-derived authorization
  window, and a manifest digest kept separate from the request hash.
- Added the immutable reserve-to-finalize/readback authority ledger with
  concurrency-safe source bindings and replay protection. Readback exposes
  truthful `sourceSetComplete` and `kindComplete` fields only; neither field
  asserts acceptance or substitutes for hosted evidence.
- Hardened the resolver, semantic-subject serializer, and protected
  verification context against structural/callback-only substitutes and
  mutable-input TOCTOU changes. Staging verification now prevalidates inputs
  and retries the complete release contract for **13 attempts at 5 seconds**.
- Known focused TypeScript evidence is **16 files / 104 tests**. Focused
  database authority evidence is **78/78 assertions** and concurrency evidence
  is **6/6** (**84/84 total**). After a fresh reset, `pnpm db:test` passed
  **61 files / 2,208 tests**; `pnpm db:lint` passed with unrelated existing
  warnings, and `pnpm db:types:check` passed. Final local `pnpm validate`
  passed **562 files** with **4,498 passed + 1 skipped / 4,499**, 100% coverage
  (**13,184 statements, 9,862 branches, 2,164 functions, 12,263 lines**);
  Slice 09 evidence passed, Playwright passed **101/101 functional** and **5/5
  real-route** checks, builds and bundle budgets passed, and local API p95 was
  **1.2129150000000095 ms**. Final local `pnpm db:verify` passed after a fresh
  reset with migrations through `20260921050000`; database lint had existing
  warnings only, **61 files / 2,208 tests** passed, and generated types matched.
  The initial validate failure was root-caused to a fixture `PUBLIC_KEY_PEM`
  re-export issue; after the fix, focused **8/8** and **12-repeat** stability
  checks passed before the successful rerun. At this local-validation
  checkpoint, exact-main CI and staging promotion were pending; their later
  success is recorded below.
- No hosted AC265 acceptance is claimed. Live producer/source population,
  protected signer execution, retained hosted artifacts, independently
  authenticated receipts, and the complete hosted matrix remain open. Totals
  remain Slice 09 **279/282 active** (**283 authored IDs**) and Phase 2
  **1,999/2,000 active criteria** (**8/17 slices**); AC209, AC211, and AC265
  remain open, Slice 10 remains locked, and AC266 remains owner-deferred as the
  mandatory post-Phase 2 production-readiness/release gate.

## AC265 CP-04d exact-main promotion evidence

- PR #91 merged to `main` at exact SHA
  `289ed3a2f4f92da383aa1464340f804777496257`. Exact-main CI run `35656504913`
  passed all three required jobs, and automatic staging run `35657406613`
  passed on `run_attempt=1` with deployment `6578526934`.
- Candidate artifact `10665966829` has digest
  `sha256:d51c0104d8ea7933e5d4a45af021b0ac2067a24150739390edb58e171cab827c`;
  deployment evidence artifact `10665756858` has digest
  `sha256:2724a98beb5e2152b7c601697c95042af508ee94d2e62da7ee143495362c9664`.
  Staging p95 was **29.956710999999927 ms** against the **500 ms** threshold;
  automated axe digest
  `6a7f59d9792a176e3032a9b410c6a0c1bb03850f27ed2918bc98fe82fddbbcf4`
  reported serious/critical counts **0/0**.
- These are exact-main CI and staging promotion proofs only. Hosted AC265
  acceptance remains open; totals and the Slice 10 lock are unchanged.
