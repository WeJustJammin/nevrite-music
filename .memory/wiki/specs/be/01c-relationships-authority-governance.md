# Relationships, Authority & Governance — Backend Specification

> **IA Source**: [Shard 01 — Identity authority and party governance](../ia/01-identity-authority.md)
> **Deep Dives**: [Identity authority and party governance deep dive](../ia/deep-dives/01-identity-authority.md)
> **Foundation**: [Shard 00 — Cross-cutting platform foundation](00-infrastructure.md)
> **Status**: Complete

## Split Group

> **Split origin**: 01-identity-authority
> **Companion specs**: [01a Authentication & Account Linking](01a-auth-account-linking.md), [01b Party Identity & Aliases](01b-party-identity-aliases.md), [01d Identifiers & Legacy](01d-identifiers-legacy.md)
> **Shared entities**: PersonParty and Party are owned by 01b; authenticated RequestContext and the platform audit/outbox records are owned by 00. This spec owns OrganizationParty and the organization relationship/governance records below.
> **Boundary rule**: generic relationship activity never creates the Shard 30 capability announce_waive. Shard 30 owns AnnounceWaiveCapability and P-07; this spec only publishes canonical party, relationship, and authority-source snapshots.

## Classification

- **Type**: multi-domain-split.
- **IA source**: 01-identity-authority.
- **Owned interactions**: IDA-06 through IDA-14.
- **Owned records**: OrganizationParty, OrganizationTypeAssignment, MembershipTenure, MembershipCapacityPeriod, RepresentationEdge, MandateGrant, GovernanceTermsVersion, GovernanceConfirmation, and the implementation records explicitly marked in the Database Schema section.
- **Excluded**: Supabase Auth/provider/session mechanics; account linking; PersonParty/facets; personal aliases/handles; legal identity and disclosure storage; identifiers and legacy nominations; memorialisation; estate representation; domain-specific label distribution mandates; booking deals; Shard 30 announce_waive/P-07.
- **Status rule**: Complete is valid only because endpoint reconciliation, source mapping, schema/index/RLS design, exact authority ordering, failure hygiene, tests, and ambiguity passes 1–10 are recorded below.

## Referenced Material Inventory

| Material | Consumed sections and exact lines | Use |
|---|---|---|
| Shard 01 IA | Acceptance Criteria IDA-06–IDA-14, lines 33–48 | Product behavior and refusal conditions for this split |
| Shard 01 IA | Global Interaction Rules and Contracts, lines 77–119 | Acting context, authority, defaults, governance and cross-cutting guarantees |
| Shard 01 IA | Data Models and access control, lines 121–182 | Canonical field names, cardinality, protected projections and denial behavior |
| Shard 01 IA | Event Schemas, lines 206–219 | Event names and identifier-only payloads |
| Shard 01 IA | Cross-Shard Dependencies and Contract Map, lines 277–293 | Ownership and consumers, especially Shards 22, 30 and 39 |
| Identity authority deep dive | Resolved choices and deterministic policies, lines 20–61 | Locked interpretation of scope, duplicate detection, overlaps, defaults and terminal lifecycle |
| Identity authority deep dive | Canonical field contracts, lines 63–101 | Exact owned columns, temporal fields, arrays, hash and immutability requirements |
| Identity authority deep dive | State machines, lines 103–115 | Organization, membership, representation and governance transition rules |
| Identity authority deep dive | Authority Resolution, lines 117–130 | Exact ten-step command authorization algorithm |
| Identity authority deep dive | Band defaults and concurrency, lines 132–149 | Seeded authority, ETag/CAS, idempotency and atomic revocation |
| Identity authority deep dive | Disclosure, cross-shard, abuse, recovery and implementation, lines 151–209 | Privacy, consumer handoffs, limits, recovery and verification obligations |
| Shard 00 BE | Common wire types and errors, lines 112–155 | Inherited strict Zod rules and four-field ApiError |
| Shard 00 BE | Schema, indexes and permissions, lines 202–251 | Non-exposed private schemas, RLS, grants, audit/idempotency/outbox boundary |
| Shard 00 BE | Middleware and route archetypes, lines 253–296 | Hono order, principal matrix, rate classes and deadlines |
| Shard 00 BE | Protected transaction, event/consumer and error rules, lines 298–435 | Atomic state/audit/outbox, leases, retries, DLQ and failure mapping |
| Shard 00 BE | Observability, release/recovery and tests, lines 452–503 | Safe telemetry, restore fence, migration and shared test gates |
| Architecture Design | API/data/security/error decisions, lines 440–447, 535–668, 709–788 | RPC/RLS, canonical PostgreSQL, trust boundaries, limits and retention |
| Architecture Design | Integration and observability, lines 916–995 | Registered integrations, structured logs, provider-native diagnostics and SLOs |
| Data Placement Strategy | Store, security, retention, tenancy and consistency, lines 5–17, 19–55, 86–148 | PII placement, Queue minimization, RLS tenancy, retention and recovery |
| Engineering Standards | Tests, budgets, security, migrations and CI, lines 27–44, 96–138, 149–165, 185–207 | Quality thresholds and production gates |
| Shard 22 IA | Distributor eligibility and declaration, lines 138–140; data model line 160–162 | Consumer of label type/control snapshot; distribution mandate remains Shard 22-owned |
| Shard 22 IA | Cross-shard map, lines 346–358 | Canonical producer/consumer direction |
| Shard 30 IA | Announce waiver contract and invariants, lines 148–155, 199–200, 226–239 | Explicit no-inference boundary for announce_waive and P-07 |
| Shard 30 IA | Cross-shard map, lines 439–449 | Current relationship/authority version handoff |
| Shard 39 IA | Contract/data model, lines 94–132 | Acting party, mandate, revision and provider-derived identity consumer |

## IA Source Map

| BE section | Authoritative source | Exact lines / decision |
|---|---|---|
| Classification and split boundary | Shard 01 IA | 33–48 and 277–293 |
| Endpoint inventory and reconciliation | Shard 01 IA; deep dive | IA 56–71; deep dive 117–149 |
| Request and response schemas | Shard 01 IA; Shard 00 BE | IA 85–119 and 121–164; BE00 112–155 |
| Organization and relationship database | Shard 01 IA; deep dive | IA 121–164; deep dive 63–101 |
| State machines | Shard 01 IA; deep dive | IA 33–48; deep dive 103–115 |
| Authority resolver and middleware | Shard 01 IA; deep dive; BE00 | IA 77–83 and 171–182; deep dive 117–130; BE00 253–296 |
| RLS/grants and transaction boundary | Architecture; data placement; BE00 | Architecture 440–447 and 636–668; placement 42–55 and 116–148; BE00 202–251, 298–308 |
| Events and consumers | Shard 01 IA; Shard 00 BE | IA 206–219; BE00 355–404 |
| Downstream authority handoff | Shards 22, 30 and 39 IA | 22:138–140, 346–358; 30:238, 439–449; 39:94–110, 263–279 |
| Errors, observability, rate and recovery | Shard 00 BE; architecture; standards | BE00 122–155, 286–296, 416–450, 452–474; Architecture 578–608, 770–788, 938–995; Standards 96–138 |

## Endpoint Completeness Reconciliation

IDA-06 through IDA-14 require canonical commands and protected reads. The route registry below is the complete HTTP surface for this split. A route cannot be implemented unless it appears in this table and has the schema, error profile, authorization, idempotency, rate, audit, metric and trace declarations that follow. Routes use the path parameter name organizationId, tenureId, edgeId, mandateId, termsId or statementId exactly as shown.

All nine owned IA interactions map to at least one command, every mutable aggregate also has its required protected read or projection, and no route from 01a, 01b, 01d, Shard 22, Shard 30, or Shard 39 is duplicated here.

## Feature Ledger Coverage

| Feature ID | Feature | Endpoint coverage |
|---|---|---|
| `01.02.01` | Organization Types & Type-Specific Attributes | ORG-01/02 and TYPE-01/02. |
| `01.02.02` | Organization Creation & Lifecycle | ORG-01/02 and LIFE-01 through LIFE-05. |
| `01.02.03` | Organization Type Transitions & Multi-Type Entities | TYPE-01/02 with lifecycle/version checks from ORG/LIFE routes. |
| `01.03.01` | Membership Records & Lifecycle | MEM-01 through MEM-06. |
| `01.03.02` | Representation & Roster Relationships | REP-01 through REP-04. |
| `01.03.03` | Mandate Scope & Delegated Authority | MAN-01/02 and AUTH-01. |
| `01.04.01` | Partnership Terms & Decision Rules | GOV-01 through GOV-04. |
| `01.04.02` | Name & Trademark Ownership | NAME-01/02; the platform records statements and does not adjudicate title. |
| `01.04.03` | Treasury Mandate | TRE-01/02 plus current MAN/AUTH projection; no pooled balance or payout is implied. |
| `01.04.04` | Dissolution & Succession | LIFE-03 through LIFE-05; dissolution is terminal and re-form creates lineage. |

## API Endpoints

### Route Registry

| ID | Method and path | Operation and ownership | Auth / ownership predicate | Success and cache | Idempotency / concurrency | Rate, deadline, SLO |
|---|---|---|---|---|---|---|
| ORG-01 | POST /api/v1/organizations | CreateOrganization; IDA-06 | Verified human; acting context is server-derived and mode is validated | 201 OrganizationResource, Location, no-store | Idempotency-Key required; no If-Match for new aggregate | Ordinary mutation 60/min/user, 120/min/party; 15s; Tier 2 p95 <1,200ms |
| ORG-02 | GET /api/v1/organizations/{organizationId} | ReadOrganization | Anonymous only publication-approved projection; authenticated detail still requires current permitted party context | 200 OrganizationPublicResource or authorized OrganizationResource, ETag; public cache only allowlisted fields | Safe read; no idempotency; conditional GET may return 304 | Public 120/min/IP burst 30/10s, authenticated 300/min/user and 600/min/party; 8s; Tier 1 p95 <750ms |
| TYPE-01 | POST /api/v1/organizations/{organizationId}/type-assignments | AddOrganizationType; IDA-07 | Current owner/admin grant on organization | 201 OrganizationTypeAssignmentResource, ETag, no-store | Idempotency-Key and organization If-Match required | Ordinary mutation; 15s; Tier 2 |
| TYPE-02 | DELETE /api/v1/organizations/{organizationId}/type-assignments/{assignmentId} | RemoveOrganizationType; IDA-07 | Same current owner/admin grant and active assignment ownership | 200 OrganizationResource, ETag, no-store | Idempotency-Key and organization If-Match required | Ordinary mutation; 15s; Tier 2 |
| MEM-01 | POST /api/v1/organizations/{organizationId}/membership-invitations | InviteMembership; IDA-08 | Organization actor with invitation authority; target person may not be fabricated | 201 MembershipTenureResource, no-store | Idempotency-Key and organization If-Match required | Ordinary mutation; 15s; Tier 2 |
| MEM-02 | POST /api/v1/organizations/{organizationId}/membership-assertions | AssertHistoricalMembership; IDA-08 | Organization owner/admin with protected evidence, or the person asserting their own historical tenure as self | 201 MembershipTenureResource with asserted state, no-store | Idempotency-Key and organization If-Match required | Ordinary mutation; 15s; Tier 2 |
| MEM-03 | POST /api/v1/membership-tenures/{tenureId}/accept | AcceptMembership; IDA-08 | Invited person acting as self; current governance terms must match | 200 MembershipTenureResource, ETag, no-store | Idempotency-Key and tenure If-Match required; invitation and terms locked in one RPC | Ordinary mutation; 15s; Tier 2 |
| MEM-04 | POST /api/v1/membership-tenures/{tenureId}/end | EndMembership; IDA-08 | Current confirmed counterparty or capable organization actor; retroactive end also needs counterpart confirmation | 200 MembershipTenureResource, ETag, no-store | Idempotency-Key and tenure If-Match required | High-risk relationship mutation 10/min/user, 10/min/party; 15s; Tier 2 |
| MEM-05 | POST /api/v1/membership-tenures/{tenureId}/capacity-periods | AddCapacityPeriod; IDA-08 | Organization owner/admin, or the tenure subject acting as self while the tenure is current confirmed | 201 MembershipCapacityPeriodResource, no-store | Idempotency-Key and tenure If-Match required; exclusion constraint is final arbiter | Ordinary mutation; 15s; Tier 2 |
| MEM-06 | GET /api/v1/organizations/{organizationId}/memberships | ReadMemberships | Current accepted member for own membership; owner/admin for organization; operator named capability | 200 MembershipCollection, no-store; private fields allowlisted | Safe read; cursor binds party/context/filter | Authenticated read; 8s; Tier 1 |
| REP-01 | POST /api/v1/representation-edges | CreateRepresentationEdge; IDA-09 | Actor controls one named principal or representative side; both parties must later confirm | 201 RepresentationEdgeResource pending, no-store | Idempotency-Key required; If-Match for controlled party aggregate | Ordinary mutation; 15s; Tier 2 |
| REP-02 | POST /api/v1/representation-edges/{edgeId}/confirm | ConfirmRepresentationEdge; IDA-09 | Human controls exactly the confirming party; second confirmation cannot be impersonated | 200 RepresentationEdgeResource active or pending, ETag, no-store | Idempotency-Key and edge If-Match required; row lock serializes dual confirmation | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| REP-03 | POST /api/v1/representation-edges/{edgeId}/revoke | RevokeRepresentationEdge; IDA-09 | Current principal, representative or authorized organization actor | 200 RepresentationEdgeResource revoked, ETag, no-store | Idempotency-Key and edge If-Match; invalidates affected projections atomically | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| REP-04 | GET /api/v1/parties/{partyId}/representation-edges | ReadRepresentationEdges | Party participant or scoped delegate; public receives none | 200 RepresentationCollection, no-store | Safe read; cursor bound to party/context | Authenticated read; 8s; Tier 1 |
| MAN-01 | POST /api/v1/mandates | GrantMandate; IDA-10 | Grantor resolved from active relationship; requested scope is a subset of source snapshot | 201 MandateGrantResource, ETag, no-store | Idempotency-Key and source aggregate If-Match required | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| MAN-02 | POST /api/v1/mandates/{mandateId}/revoke | RevokeMandate; IDA-10 | Grantor/current owner or authorized administrator for source relationship | 200 MandateGrantResource revoked, ETag, no-store | Idempotency-Key and mandate If-Match; dependent grants CAS-revoked in one RPC | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| AUTH-01 | GET /api/v1/parties/{partyId}/authority | ReadAuthorityProjection | Human owns/resolves context for party; consumers receive only registered projection | 200 AuthorityProjectionResource, ETag, no-store | Safe read; projection version is authoritative freshness marker | Authenticated read; 8s; Tier 1 |
| GOV-01 | POST /api/v1/organizations/{organizationId}/governance-terms | ProposeGovernanceTerms; IDA-11 | Eligible governance proposer under current authority; member set is server-derived | 201 GovernanceTermsResource proposed, ETag, no-store | Idempotency-Key and organization If-Match required | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| GOV-02 | GET /api/v1/organizations/{organizationId}/governance-terms/{termsId} | ReadGovernanceTerms | Current permanent member, owner/admin or named operator evidence capability | 200 GovernanceTermsResource with private terms allowlist, ETag, no-store | Safe read; no client terms authority | Authenticated read; 8s; Tier 1 |
| GOV-03 | POST /api/v1/governance-terms/{termsId}/confirm | ConfirmGovernanceTerms; IDA-11 | Required current permanent member acting as self; exact hash must match | 200 GovernanceConfirmationResource, ETag, no-store | Idempotency-Key and terms If-Match required; member/terms row lock | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| GOV-04 | POST /api/v1/governance-terms/{termsId}/activate | ActivateGovernanceTerms; IDA-11 | Eligible proposer/owner/admin; all current permanent members must have confirmed exact version | 200 GovernanceTermsResource active, ETag, no-store | Idempotency-Key and organization If-Match; one RPC locks terms, member set, confirmations and active pointer | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| NAME-01 | POST /api/v1/organizations/{organizationId}/name-ownership-statements | RecordNameOwnershipStatement; IDA-12 | Current owner/admin band grant; no ownership adjudication | 201 NameOwnershipStatementResource, no-store | Idempotency-Key and organization If-Match required; append-only statement | Ordinary mutation; 15s; Tier 2 |
| NAME-02 | GET /api/v1/organizations/{organizationId}/name-ownership-statements | ReadNameOwnershipStatements | Authorized organization participant; public only if a publication-approved projection explicitly allows it | 200 NameStatementCollection, no-store | Safe read; cursor binds context and filter | Authenticated read; 8s; Tier 1 |
| TRE-01 | GET /api/v1/organizations/{organizationId}/treasury-authority | ReadTreasuryAuthority | Confirmed band member for view; command capabilities never inferred by this read | 200 TreasuryAuthorityView, no balance or pooled-fund fields, no-store | Safe read; current authority projection re-read | High-risk data read 120/min/user, 240/min/party; 8s; Tier 1 |
| TRE-02 | POST /api/v1/organizations/{organizationId}/treasury-authorizations | AuthorizeTreasuryAct; IDA-13 | Confirmed band member with current mandate, amount, single payee, term and currency predicates | 201 TreasuryAuthorizationRecord, no funds moved, no-store | Idempotency-Key and organization If-Match required; authorization is an implementation record | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| LIFE-01 | POST /api/v1/organizations/{organizationId}/close | CloseOrganization; IDA-14 | Current owner/admin and finite resolvable obligations; disposition rows required | 200 OrganizationResource closing or closed, ETag, no-store | Idempotency-Key and organization If-Match; lifecycle CAS | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| LIFE-02 | POST /api/v1/organizations/{organizationId}/reopen | ReopenClosedOrganization; IDA-14 | Current owner/admin; only ordinary closed organization, never dissolved | 200 OrganizationResource active, ETag, no-store | Idempotency-Key and organization If-Match; lifecycle CAS | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| LIFE-03 | POST /api/v1/organizations/{organizationId}/dissolve | DissolveBand; IDA-14 | Current permanent members vote under active terms; former/non-members are notified but voteless | 200 OrganizationResource dissolving or dissolved, ETag, no-store | Idempotency-Key and organization If-Match; expected member-set hash and terms version required | High-risk 10/min/user, 10/min/party; 15s; Tier 2 |
| LIFE-04 | POST /api/v1/organizations/{organizationId}/re-form | ReFormSuccessor; IDA-14 | Current owner/admin for ordinary closed source, or current permanent member after the recorded dissolution vote for dissolved source | 201 OrganizationLineageResource with successor, Location, no-store | Idempotency-Key and source organization If-Match; predecessor/successor/lineage atomic | High-risk 10/min/user, 10/min/party; async acceptance target p95 <=500ms, p99 <=1,000ms |
| LIFE-05 | GET /api/v1/organizations/{organizationId}/lineage | ReadOrganizationLineage | Authorized party participant or approved public projection; no hidden successor data | 200 OrganizationLineageResource, no-store | Safe read; cursor not needed for one lineage chain | Authenticated read; 8s; Tier 1 |

### Endpoint contract matrix

This matrix closes the per-endpoint completeness gate. The request alias supplies every body/path/header field and example in the next section; the error profile supplies every validation/auth/not-found/conflict/rate/downstream class; the route registry supplies the authority predicate, cache, deadline, rate and SLO. Every command writes an allow/deny audit decision and a correlation-linked outbox event when state changes.

| ID | Request schema / success schema | Error profile | Audit action / event | BOLA and replay rule |
|---|---|---|---|---|
| ORG-01 | CreateOrganizationRequest -> OrganizationResource | Organization | organization.create / organization.changed | Actor-only create; same key replays exact party |
| ORG-02 | path OrganizationId -> OrganizationPublicResource or OrganizationResource | Public/read | organization.read / no event | Concealed 404 for unauthorized ID; ETag conditional read |
| TYPE-01 | AddOrganizationTypeRequest -> OrganizationTypeAssignmentResource | Organization | organization.type.add / organization.changed | Current owner/admin and org ETag required |
| TYPE-02 | path assignmentId plus mutation headers -> OrganizationResource | Organization | organization.type.remove / organization.changed | Assignment must belong to path org; no history deletion |
| MEM-01 | MembershipInvitationRequest -> MembershipTenureResource | Membership | membership.invite / relationship.changed | Target person and org are resolved and checked server-side |
| MEM-02 | HistoricalMembershipAssertionRequest -> MembershipTenureResource | Membership | membership.assert / relationship.changed | Evidence reference is protected; public projection is hidden |
| MEM-03 | AcceptMembershipRequest -> MembershipTenureResource | Membership | membership.accept / relationship.changed | Only invited person; terms hash and tenure ETag match |
| MEM-04 | EndMembershipRequest -> MembershipTenureResource | Membership | membership.end / relationship.changed plus context.revoked when applicable | Tenure path and counterpart confirmation are checked atomically |
| MEM-05 | CapacityPeriodRequest -> MembershipCapacityPeriodResource | Membership | membership.capacity.add / relationship.changed | Tenure containment and exclusion constraint are final |
| MEM-06 | query/cursor -> MembershipCollection | Public/read | membership.read / no event | Collection is context-bound and cannot enumerate hidden people |
| REP-01 | RepresentationRequest -> pending RepresentationEdgeResource | Representation | representation.create / relationship.changed | Principal/representative are distinct canonical parties |
| REP-02 | RepresentationConfirmationRequest -> RepresentationEdgeResource | Representation | representation.confirm / relationship.changed | Confirming party comes from context, never body |
| REP-03 | RepresentationRevokeRequest -> revoked RepresentationEdgeResource | Representation | representation.revoke / relationship.changed plus context.revoked | Current participant or scoped authority only |
| REP-04 | query/cursor -> RepresentationCollection | Public/read | representation.read / no event | Only participant/scoped delegate projection |
| MAN-01 | MandateGrantRequest -> MandateGrantResource | Mandate/authority | mandate.grant / relationship.changed | Source snapshot subset check; no client grantor |
| MAN-02 | path mandateId plus mutation headers -> MandateGrantResource | Mandate/authority | mandate.revoke / relationship.changed plus context.revoked | Dependent subgrants revoke in same RPC |
| AUTH-01 | path partyId -> AuthorityProjectionResource | Public/read | authority.read / no event | Projection is derived, stale source never returned as active |
| GOV-01 | GovernanceTermsRequest -> proposed GovernanceTermsResource | Governance | governance.propose / organization.changed | Required members are calculated from confirmed permanent rows |
| GOV-02 | path org/terms IDs -> GovernanceTermsResource | Public/read | governance.read / no event | Terms JSON only to member/owner/operator policy |
| GOV-03 | GovernanceConfirmationRequest -> GovernanceConfirmationResource | Governance | governance.confirm / organization.changed | Member and exact hash derived/rechecked server-side |
| GOV-04 | GovernanceActivationRequest -> active GovernanceTermsResource | Governance | governance.activate / governance.activated | One active pointer and exact member set; replay returns active version |
| NAME-01 | NameOwnershipStatementRequest -> NameOwnershipStatementResource | Name | name.statement.record / organization.changed | Owner/admin grant; competing statement never overwrites |
| NAME-02 | query/cursor -> NameStatementCollection | Public/read | name.statement.read / no event | Private by default; public only approved projection |
| TRE-01 | path organizationId -> TreasuryAuthorityView | Public/read | treasury.authority.read / no event | Confirmed band member view; no balance data exists |
| TRE-02 | TreasuryAuthorizationRequest -> TreasuryAuthorizationRecord | Treasury | treasury.authorize / organization.changed | One payee, current mandate and exact ceiling; no fund effect |
| LIFE-01 | CloseOrganizationRequest -> OrganizationResource | Lifecycle | organization.close / organization.changed | Current owner/admin and finite obligation gate |
| LIFE-02 | ReopenOrganizationRequest -> OrganizationResource | Lifecycle | organization.reopen / organization.changed | Closed ordinary only; dissolved is permanently concealed as non-reopenable |
| LIFE-03 | DissolveOrganizationRequest -> OrganizationResource | Lifecycle | organization.dissolve / organization.changed | Current permanent member vote and active terms hash |
| LIFE-04 | ReFormRequest -> OrganizationLineageResource | Lifecycle | organization.reform / organization.changed | One successor and lineage row; no caller-selected successor |
| LIFE-05 | path organizationId -> OrganizationLineageResource | Public/read | organization.lineage.read / no event | Only authorized lineage projection; no unrelated party traversal |

### Command-specific completion obligations

- ORG-01 records the selected creation mode, registry version and actor snapshot. Shadow/custodial and external-reference modes do not create an external identity, provider link or identifier.
- TYPE-01 refuses an unregistered/deactivated type before mutation. TYPE-02 ends exactly one active assignment and increments the organization version; it never deletes the organization or rewrites old assignments.
- MEM-01 refuses invitation when the organization lacks a capable actor or current governed terms. MEM-02 never changes current authority. MEM-03 requires the invited person’s self context and the current terms hash. MEM-04 performs immediate revocation even when the historical end date is disputed. MEM-05 does not change tenure authority.
- REP-01 validates both parties, scope and term before creating a pending edge. REP-02 writes only the current party’s confirmation. REP-03 writes revocation, invalidates projections and emits context invalidation in one transaction.
- MAN-01 derives grantor and source projection, then checks activity, domain, term, currency and ceiling as a subset. MAN-02 recursively invalidates dependent grants before commit and leaves relied-upon historical actions attributable.
- GOV-01 freezes a server-derived permanent-member set and stores the exact document hash. GOV-03 stores a decision over that exact hash. GOV-04 locks terms, member rows, confirmations and the organization active pointer and either commits all or none.
- NAME-01 stores an attributed statement and optional self-supplied reference only. It does not call a registry, perform search/clearance or return legal advice.
- TRE-02 is a local authorization record for a downstream single-payee boundary. A downstream payment system, provider operation, balance, split, escrow or tax record is outside this shard.
- LIFE-01 records finite dispositions and may enter closing before closed. LIFE-03 requires the active governance vote and records unresolved dispositions without blocking terminal state. LIFE-04 creates a fresh canonical party and immutable predecessor/successor row.

### Read projection and cursor obligations

ORG-02 public output is an allowlisted projection and never varies based on an unauthenticated guessed context. MEM-06, REP-04, GOV-02, NAME-02 and TRE-01 omit private fields unless the caller’s current context passes the matching RLS predicate. AUTH-01 omits revoked/expired source rows from active projection and returns a safe stale/empty state only after authority checks. LIFE-05 follows only the requested organization’s lineage and cannot be used to enumerate parties.

Collection cursors use BE00’s opaque authenticated format: route, normalized filters, deterministic sort/tie-breaker, audience, user/acting context and contract version are bound into the cursor; expiry is at most 24 hours; tamper or cross-context reuse is 400 without cursor internals. Collection limits are 1–50/default 25. Protected reads use no-store and never put terms, mandates, member lists or treasury details in Realtime hints.

Every route inherits BE00’s route registry, request deadline, no-store default for protected data, X-Request-Id, response validation and four-field ApiError. ORG-02 is the only route with an anonymous projection. Existence-sensitive unauthorized reads return 404; authenticated policy denials that do not reveal existence return 403 with only a reasonCode.

## Request/Response Contracts (Zod 4 schemas)

Zod 4 runtime schemas are strict and are the source for TypeScript, Hono validation, OpenAPI and tests. The following examples are normative shapes; optional fields remain absent rather than null unless the table says nullable. Unknown keys fail. All UUIDs are lowercase canonical UUIDs, all timestamps are RFC 3339 UTC, all date-only values are ISO dates, and all arrays are deduplicated in canonical order before hashing.

### Common wire schemas

    const Id = z.string().uuid();
    const Version = z.string().regex(/^[1-9][0-9]{0,18}$/)
      .refine((value) => BigInt(value) <= 9223372036854775807n, "version_out_of_range");
    const IdempotencyKey = z.string().min(8).max(128)
      .regex(/^[\x20-\x7E]+$/)
      .refine((value) => value.trim() === value, "idempotency_key_outer_whitespace");
    const Currency = z.string().regex(/^[A-Z]{3}$/);
    const MoneyMinor = z.number().int().nonnegative().safe();
    const Timestamp = z.iso.datetime({ offset: true });
    const StrongETag = z.string().regex(/^"[1-9][0-9]{0,18}"$/);
    const Activity = z.enum(["book","sign","spend","list","release","settle","administer","negotiate","bind"]);
    const DomainCode = z.string().regex(/^[a-z][a-z0-9_.-]{0,63}$/);
    const PartyId = z.object({ partyId: Id }).strict();
    const MutationHeaders = z.object({
      idempotencyKey: IdempotencyKey,
      ifMatch: z.string().regex(/^"[1-9][0-9]{0,18}"$/),
    }).strict();

MutationHeaders are HTTP headers, not body fields. The server rejects a body containing idempotencyKey or ifMatch. If-Match follows BE00’s exact grammar at lines 341–346: one strong quoted positive bigint, no weak tag, wildcard, list, internal whitespace or leading zero. New aggregate commands omit If-Match only when the route registry explicitly says so.

### Domain request schemas

| Schema | Strict fields and constraints | Example |
|---|---|---|
| CreateOrganizationRequest | mode enum self_member, shadow_custodial, external_reference; typeCodes array 0–32 of registered lowercase codes (the launch registry has seven); no caller owner, lifecycle, party, duplicate decision or authority fields | { mode: "self_member", typeCodes: ["band"] } |
| AddOrganizationTypeRequest | typeCode exactly one current registry code | { typeCode: "label" } |
| MembershipInvitationRequest | personId UUID; startsOn ISO date; termsVersionId UUID for a governed organization; capacity one of permanent, touring, staff, honorary; inviteExpiresAt RFC3339 and later than now | { personId: "uuid", startsOn: "2026-08-28", termsVersionId: "uuid", capacity: "permanent" } |
| HistoricalMembershipAssertionRequest | personId UUID; startsOn; optional endsOn after startsOn; provenance historical_assertion; evidenceRef UUID for protected evidence; no acceptedAt or authority fields | { personId: "uuid", startsOn: "2024-01-01", provenance: "historical_assertion", evidenceRef: "uuid" } |
| AcceptMembershipRequest | termsVersionId UUID; termsHash exactly 64 lowercase hex; decision literal accept | { termsVersionId: "uuid", termsHash: "64-hex", decision: "accept" } |
| EndMembershipRequest | mode now or retroactive; optional endsOn; counterpartConfirmationId required for retroactive and absent for now; reasonCode from closed registry | { mode: "retroactive", endsOn: "2025-12-31", counterpartConfirmationId: "uuid", reasonCode: "DATE_CORRECTION" } |
| CapacityPeriodRequest | capacity permanent, touring, staff or honorary; startsOn; optional endsOn strictly after startsOn; period must be contained by tenure | { capacity: "touring", startsOn: "2026-01-01", endsOn: "2026-06-01" } |
| RepresentationRequest | principalPartyId and representativePartyId distinct UUIDs; activities nonempty max 32; domains nonempty max 32; territories nonempty ISO alpha-2 values or exactly WORLDWIDE; startsAt; endsAt after startsAt; communicate boolean; optional nonnegative ceilingMinor paired with currency; optional agreementRef UUID | { principalPartyId: "uuid", representativePartyId: "uuid", activities: ["negotiate"], domains: ["music"], territories: ["US"], startsAt: "2026-08-28T12:00:00Z", endsAt: "2027-08-28T12:00:00Z", communicate: true } |
| RepresentationConfirmationRequest | confirmation literal confirm; no partyId in body because party is server-derived | { confirmation: "confirm" } |
| RepresentationRevokeRequest | reasonCode closed enum; no scope rewrite | { reasonCode: "AUTHORITY_WITHDRAWN" } |
| MandateGrantRequest | relationshipType membership or representation; relationshipId UUID; activities nonempty; domainsMode all or explicit; explicit mode requires nonempty domains; startsAt/endsAt inside source term; optional ceilingMinor/currency as pair; source is explicit only and grantor is derived | { relationshipType: "representation", relationshipId: "uuid", activities: ["negotiate"], domainsMode: "explicit", domains: ["music"], startsAt: "2026-08-28T12:00:00Z", endsAt: "2027-08-28T12:00:00Z" } |
| GovernanceTermsRequest | termsSchemaVersion positive integer; terms object strict with authorityMode seeded or explicit, commercialCeilingMinor nullable nonnegative, commercialCurrency nullable uppercase and paired, approvalRule unanimous_permanent_members, nameDisposition explicit_statement, treasuryRule single_payee_only; documentHash 64 lowercase hex | { termsSchemaVersion: 1, terms: { authorityMode: "explicit", commercialCeilingMinor: 100000, commercialCurrency: "USD", approvalRule: "unanimous_permanent_members", nameDisposition: "explicit_statement", treasuryRule: "single_payee_only" }, documentHash: "64-hex" } |
| GovernanceConfirmationRequest | termsHash exact hash of stored document; decision confirm or reject; no member identity in body | { termsHash: "64-hex", decision: "confirm" } |
| GovernanceActivationRequest | expectedMemberSetHash 64 lowercase hex; expectedOrganizationVersion strong ETag value; no client required-member list | { expectedMemberSetHash: "64-hex", expectedOrganizationVersion: "42" } |
| NameOwnershipStatementRequest | owners nonempty max 32 party UUIDs; disposition asserted, licensed, disputed or superseded; optional trademarkReference max 256 printable characters and explicitly self_supplied; no verification flag or legal conclusion | { owners: ["uuid"], disposition: "asserted", trademarkReference: "self-supplied-ref" } |
| TreasuryAuthorizationRequest | activity one registered commercial activity; amountMinor positive safe integer; currency uppercase; payeePartyId UUID exactly one; mandateId UUID; expectedAuthorityVersion Version; no split, escrow, tax, balance, account or provider fields | { activity: "spend", amountMinor: 50000, currency: "USD", payeePartyId: "uuid", mandateId: "uuid", expectedAuthorityVersion: "9" } |
| CloseOrganizationRequest | expectedOrganizationVersion Version; dispositions array 1–32 of strict { kind, status `unresolved|assigned|waived`, referenceId? }; only finite obligation classes accepted | { expectedOrganizationVersion: "7", dispositions: [{ kind: "rights", status: "unresolved" }] } |
| ReopenOrganizationRequest | expectedOrganizationVersion Version; no body action override | { expectedOrganizationVersion: "7" } |
| DissolveOrganizationRequest | expectedOrganizationVersion Version; activeTermsId UUID; expectedMemberSetHash; dispositions same strict list; no former/non-member votes | { expectedOrganizationVersion: "8", activeTermsId: "uuid", expectedMemberSetHash: "64-hex", dispositions: [{ kind: "name", status: "unresolved" }] } |
| ReFormRequest | expectedOrganizationVersion Version; reasonCode; no caller successorId and no deletion instruction | { expectedOrganizationVersion: "9", reasonCode: "NEW_ENTITY" } |

### Typed success response resources

| Resource | Required response fields and disclosure |
|---|---|
| OrganizationResource | `organizationId: Id`; `ownershipState: z.enum([unclaimed, owned, ownerless])`; `lifecycle: z.enum([active, dormant, closing, closed, dissolving, dissolved])`; `typeCodes: z.array(DomainCode).max(32)`; `version: Version`; `etag: StrongETag`; `createdAt/updatedAt: Timestamp`; no hidden membership/legal fields |
| OrganizationPublicResource | `organizationId: Id`; `typeDisplay: z.array(z.string().trim().max(128)).max(32)` publication-approved; `lifecycleLabel: z.string().trim().max(128).nullable()` policy-gated; `version: Version`; no private authority, members, mandates or terms |
| OrganizationTypeAssignmentResource | `assignmentId: Id`; `organizationId: Id`; `typeCode: DomainCode`; `startsAt: Timestamp`; `endsAt: Timestamp.nullable()`; `state: z.enum([active, ended])`; `version: Version`; `etag: StrongETag` |
| MembershipTenureResource | `tenureId/organizationId: Id`; `personId: Id` authorized-only; `state: z.enum([invited, asserted, confirmed, ended, disputed, rejected, expired])`; `provenance: DomainCode`; `startsOn: z.iso.date()`; `endsOn/acceptedAt/revokedAt: Timestamp.nullable()`; `version: Version`; `etag: StrongETag` |
| MembershipCapacityPeriodResource | `periodId/tenureId: Id`; `capacity: DomainCode`; `startsOn/endsOn: z.iso.date().nullable()` with `startsOn` required by the route; `version: Version`; `etag: StrongETag` |
| RepresentationEdgeResource | `edgeId/principalPartyId/representativePartyId: Id` participant-filtered; `activities/domains/territories: z.array(DomainCode).max(32)`; `startsAt/endsAt: Timestamp`; `communicate: z.boolean()`; `ceilingMinor: MoneyMinor.nullable()` and `currency: Currency.nullable()` paired; `state: z.enum([draft, pending, active, rejected, revoked, expired])`; `version: Version`; `etag: StrongETag` |
| MandateGrantResource | `mandateId/relationshipId/grantorPartyId: Id` with grantor filtered; `relationshipType: z.enum([membership, representation])`; `activities/domains: z.array(DomainCode).max(32)`; `domainsMode: z.enum([all, explicit])`; `startsAt/endsAt: Timestamp`; `ceilingMinor: MoneyMinor.nullable()` and `currency: Currency.nullable()` paired; `source: DomainCode`; `state: z.enum([draft, active, revoked, expired])`; `version: Version`; `etag: StrongETag` |
| AuthorityProjectionResource | `humanId/actingPartyId: Id` with human self/operator filter; `sourceRelationshipId/sourceMandateId: Id.nullable()`; `sourceVersion/projectionVersion: Version`; `activities/domains: z.array(DomainCode).max(32)`; `communicate: z.boolean()`; `ceilingMinor: MoneyMinor.nullable()` and `currency: Currency.nullable()` paired; `validFrom/validThrough: Timestamp`; `etag: StrongETag`; never client-writable |
| GovernanceTermsResource | `termsId/organizationId: Id`; `versionNo/termsSchemaVersion: z.number().int().positive()`; `termsHash/requiredMemberSetHash: z.string().regex(/^[a-f0-9]{64}$/)`; `state: z.enum([draft, proposed, active, superseded, withdrawn, rejected])`; `proposedAt/effectiveAt: Timestamp.nullable()`; `supersedesTermsId: Id.nullable()`; terms detail only authorized |
| GovernanceConfirmationResource | `confirmationId/termsId: Id`; `decision: z.enum([confirm, reject])`; `occurredAt: Timestamp`; `memberId: Id` authorized-only; `termsHash: z.string().regex(/^[a-f0-9]{64}$/)`; `version: Version` |
| NameOwnershipStatementResource | `statementId/organizationId: Id`; `termsVersionId: Id.nullable()`; `owners: z.array(Id).min(1).max(32)`; `disposition: z.enum([asserted, licensed, disputed, superseded])`; `trademarkReference: z.string().max(256).nullable()`; `effectiveAt/supersededAt: Timestamp.nullable()`; `version: Version` |
| TreasuryAuthorityView | `organizationId: Id`; `viewable: z.literal(true)`; `currentMandates: z.array(z.object({ mandateId: Id, activities: z.array(DomainCode), domains: z.array(DomainCode), startsAt: Timestamp, endsAt: Timestamp, ceilingMinor: MoneyMinor.nullable(), currency: Currency.nullable() }).strict()).max(32)`; `policyVersion: Version`; no balances, account numbers, payee history or transfer operation |
| TreasuryAuthorizationRecord | `authorizationId/organizationId/mandateId/payeePartyId: Id`; `activity: Activity`; `amountMinor: MoneyMinor`; `currency: Currency`; `decision: z.enum([authorized, refused, expired, revoked])`; `authoritySourceVersion: Version`; `createdAt: Timestamp`; `version: Version`; implementation record, not a payment ledger |
| OrganizationLineageResource | `predecessorOrganizationId/successorOrganizationId: Id`; `relationship: z.literal(formed_successor)`; `reasonCode: DomainCode`; `occurredAt: Timestamp`; `sourceVersion/lineageVersion: Version`; no hidden successor details |
| Collections | `items: z.array(Resource).max(50)` for the named collection; `nextCursor: z.string().max(512).nullable()`; `hasMore: z.boolean()`; cursor follows BE00 binding and limit 1–50 with default 25 |

The response names above are runtime schemas, not documentation-only aliases. This compact Zod 4 registry gives every success field an executable type; collection aliases bind `Resource` to the concrete item type named by the route matrix.

```ts
const OrganizationResource = z.strictObject({
  organizationId: Id, ownershipState: z.enum(["unclaimed", "owned", "ownerless"]),
  lifecycle: z.enum(["active", "dormant", "closing", "closed", "dissolving", "dissolved"]),
  typeCodes: z.array(DomainCode).max(32), version: Version, etag: StrongETag,
  createdAt: Timestamp, updatedAt: Timestamp,
});
const OrganizationPublicResource = z.strictObject({
  organizationId: Id, typeDisplay: z.array(z.string().min(1).max(128)).max(32),
  lifecycleLabel: z.string().min(1).max(128).nullable(), version: Version,
});
const OrganizationTypeAssignmentResource = z.strictObject({
  assignmentId: Id, organizationId: Id, typeCode: DomainCode, startsAt: Timestamp,
  endsAt: Timestamp.nullable(), state: z.enum(["active", "ended"]), version: Version, etag: StrongETag,
});
const MembershipTenureResource = z.strictObject({
  tenureId: Id, organizationId: Id, personId: Id, state: z.enum(["invited", "asserted", "confirmed", "ended", "disputed", "rejected", "expired"]),
  provenance: DomainCode, startsOn: z.iso.date(), endsOn: z.iso.date().nullable(),
  acceptedAt: Timestamp.nullable(), revokedAt: Timestamp.nullable(), version: Version, etag: StrongETag,
});
const MembershipCapacityPeriodResource = z.strictObject({
  periodId: Id, tenureId: Id, capacity: DomainCode, startsOn: z.iso.date(), endsOn: z.iso.date().nullable(), version: Version, etag: StrongETag,
});
const RepresentationEdgeResource = z.strictObject({
  edgeId: Id, principalPartyId: Id, representativePartyId: Id,
  activities: z.array(DomainCode).max(32), domains: z.array(DomainCode).max(32), territories: z.array(DomainCode).max(32),
  startsAt: Timestamp, endsAt: Timestamp, communicate: z.boolean(),
  ceilingMinor: MoneyMinor.nullable(), currency: Currency.nullable(),
  state: z.enum(["draft", "pending", "active", "rejected", "revoked", "expired"]), version: Version, etag: StrongETag,
});
const MandateGrantResource = z.strictObject({
  mandateId: Id, relationshipId: Id, grantorPartyId: Id,
  relationshipType: z.enum(["membership", "representation"]), activities: z.array(DomainCode).max(32), domains: z.array(DomainCode).max(32),
  domainsMode: z.enum(["all", "explicit"]), startsAt: Timestamp, endsAt: Timestamp,
  ceilingMinor: MoneyMinor.nullable(), currency: Currency.nullable(), source: DomainCode,
  state: z.enum(["draft", "active", "revoked", "expired"]), version: Version, etag: StrongETag,
});
const AuthorityProjectionResource = z.strictObject({
  humanId: Id, actingPartyId: Id, sourceRelationshipId: Id.nullable(), sourceMandateId: Id.nullable(),
  sourceVersion: Version, projectionVersion: Version, activities: z.array(DomainCode).max(32), domains: z.array(DomainCode).max(32),
  communicate: z.boolean(), ceilingMinor: MoneyMinor.nullable(), currency: Currency.nullable(),
  validFrom: Timestamp, validThrough: Timestamp, etag: StrongETag,
});
const GovernanceTermsResource = z.strictObject({
  termsId: Id, organizationId: Id, versionNo: z.number().int().positive(), termsSchemaVersion: z.number().int().positive(),
  termsHash: z.string().regex(/^[a-f0-9]{64}$/), requiredMemberSetHash: z.string().regex(/^[a-f0-9]{64}$/),
  state: z.enum(["draft", "proposed", "active", "superseded", "withdrawn", "rejected"]),
  proposedAt: Timestamp.nullable(), effectiveAt: Timestamp.nullable(), supersedesTermsId: Id.nullable(),
});
const GovernanceConfirmationResource = z.strictObject({
  confirmationId: Id, termsId: Id, decision: z.enum(["confirm", "reject"]), occurredAt: Timestamp,
  memberId: Id, termsHash: z.string().regex(/^[a-f0-9]{64}$/), version: Version,
});
const NameOwnershipStatementResource = z.strictObject({
  statementId: Id, organizationId: Id, termsVersionId: Id.nullable(), owners: z.array(Id).min(1).max(32),
  disposition: z.enum(["asserted", "licensed", "disputed", "superseded"]), trademarkReference: z.string().max(256).nullable(),
  effectiveAt: Timestamp.nullable(), supersededAt: Timestamp.nullable(), version: Version,
});
const TreasuryAuthorityView = z.strictObject({
  organizationId: Id, viewable: z.literal(true), currentMandates: z.array(z.strictObject({
    mandateId: Id, activities: z.array(DomainCode).max(32), domains: z.array(DomainCode).max(32), startsAt: Timestamp, endsAt: Timestamp,
    ceilingMinor: MoneyMinor.nullable(), currency: Currency.nullable(),
  })).max(32), policyVersion: Version,
});
const TreasuryAuthorizationRecord = z.strictObject({
  authorizationId: Id, organizationId: Id, mandateId: Id, payeePartyId: Id, activity: Activity,
  amountMinor: MoneyMinor, currency: Currency, decision: z.enum(["authorized", "refused", "expired", "revoked"]),
  authoritySourceVersion: Version, createdAt: Timestamp, version: Version,
});
const OrganizationLineageResource = z.strictObject({
  predecessorOrganizationId: Id, successorOrganizationId: Id, relationship: z.literal("formed_successor"),
  reasonCode: DomainCode, occurredAt: Timestamp, sourceVersion: Version, lineageVersion: Version,
});
const Collection = <T extends z.ZodType>(item: T) => z.strictObject({
  items: z.array(item).max(50), nextCursor: z.string().max(512).nullable(), hasMore: z.boolean(),
});
const MembershipCollection = Collection(MembershipTenureResource);
const RepresentationCollection = Collection(RepresentationEdgeResource);
const NameStatementCollection = Collection(NameOwnershipStatementResource);
```

No response contains provider/session mechanics, legal identity, personal alias linkage, identifiers, raw evidence, terms document bytes, or a claim that the platform adjudicated name ownership. Error responses always use BE00’s exact four top-level fields: code, message, requestId and details.

### Field validation and error-code matrix

| Field / location | Constraint | Validation result |
|---|---|---|
| All path IDs | UUID, one canonical resource identifier | 400 INVALID_REQUEST with JSON Pointer path |
| All mutation Idempotency-Key headers | 8–128 printable ASCII bytes, byte-identical after optional outer whitespace removal, stored as digest only | 400 INVALID_REQUEST |
| All required If-Match headers | One strong quoted positive bigint per BE00 grammar | 400 INVALID_REQUEST; stale authorized value is 409 VERSION_MISMATCH |
| mode/typeCode/typeCodes | Closed versioned registry, lowercase normalized, typeCodes unique and max 7 | 422 ORGANIZATION_TYPE_UNKNOWN or VALIDATION_FAILED |
| personId/partyId | UUID and existence evaluated only after shape validation | 422 for shape; safe 404/403 for policy |
| startsOn/endsOn | ISO date; endsOn after startsOn; no impossible future/history rule beyond endpoint policy | 422 DATE_INVALID or PERIOD_INVALID |
| startsAt/endsAt | RFC3339 UTC; endsAt strictly later | 422 TERM_INVALID |
| activities/domains | Nonempty where required, unique, max 32, registered codes only | 422 SCOPE_INVALID |
| territories | ISO alpha-2 or sole WORLDWIDE; WORLDWIDE cannot coexist with another value | 422 TERRITORY_INVALID |
| communicate | Boolean and evaluated separately from commercial authority | 422 COMMUNICATION_INVALID |
| ceilingMinor/currency | Both present or both absent; amount safe nonnegative integer; currency three uppercase letters; null ceiling means no monetary authority | 422 CEILING_INVALID or CURRENCY_REQUIRED |
| documentHash/termsHash/memberSetHash | Exactly 64 lowercase hex and hash must match canonical bytes/member set | 422 HASH_INVALID or 409 TERMS_HASH_MISMATCH |
| terms object | Schema-versioned strict object, complete required keys, no unknown keys | 422 GOVERNANCE_TERMS_INCOMPLETE |
| owners | 1–32 UUIDs, unique and current party references | 422 NAME_OWNERS_INVALID |
| amountMinor | Positive safe integer and one payee only | 422 TREASURY_AMOUNT_INVALID or TREASURY_MULTI_PAYEE_DISABLED |
| evidenceRef/counterpartConfirmationId | UUID to protected implementation record; caller cannot submit evidence content | 422 EVIDENCE_REFERENCE_INVALID |
| expectedMemberSetHash | Canonical sorted current permanent-member set hash | 409 GOVERNANCE_MEMBER_SET_STALE |
| disposition rows | Closed kind/status, finite count, explicit unresolved disposition when terminal lifecycle permits unresolved obligations | 422 DISPOSITION_INVALID or 409 DISPOSITION_REQUIRED |

Error profiles ensure every registry route has a complete error set:

| Route profile | Applies to | 4xx errors | 5xx errors |
|---|---|---|---|
| Public/read | ORG-02, MEM-06, REP-04, AUTH-01, GOV-02, NAME-02, TRE-01, LIFE-05 | INVALID_REQUEST, UNAUTHENTICATED where auth is required, NOT_FOUND, FORBIDDEN where existence is safe, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Organization | ORG-01, TYPE-01, TYPE-02 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, ORGANIZATION_TYPE_UNKNOWN, ORGANIZATION_MODE_REQUIRED, ORGANIZATION_VERSION_CONFLICT, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Membership | MEM-01 through MEM-05 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, TERMS_ACCEPTANCE_REQUIRED, MEMBERSHIP_STATE_INVALID, MEMBERSHIP_VERSION_CONFLICT, MEMBERSHIP_ASSERTION_REJECTED, RETROACTIVE_END_CONFIRMATION_REQUIRED, CAPACITY_OVERLAP, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Representation | REP-01 through REP-03 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, REPRESENTATION_SCOPE_INVALID, REPRESENTATION_TERM_INVALID, REPRESENTATION_CURRENCY_REQUIRED, REPRESENTATION_CONFIRMATION_REQUIRED, REPRESENTATION_STATE_INVALID, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Mandate/authority | MAN-01, MAN-02 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, MANDATE_SCOPE_EXCEEDS_GRANTOR, MANDATE_TERM_EXCEEDS_GRANTOR, MANDATE_CEILING_EXCEEDS_GRANTOR, MANDATE_STATE_INVALID, AUTHORITY_STALE, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Governance | GOV-01 through GOV-04 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, GOVERNANCE_TERMS_INCOMPLETE, GOVERNANCE_TERMS_IMMUTABLE, GOVERNANCE_CONFIRMATION_REQUIRED, GOVERNANCE_MEMBER_SET_STALE, GOVERNANCE_CONFIRMATION_STALE, GOVERNANCE_ALREADY_ACTIVE, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Name | NAME-01 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, NAME_STATEMENT_INVALID, NAME_STATEMENT_LEGAL_CONCLUSION_UNSUPPORTED, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Treasury | TRE-02 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, TREASURY_MANDATE_REQUIRED, TREASURY_CEILING_EXCEEDED, TREASURY_CURRENCY_MISMATCH, TREASURY_MULTI_PAYEE_DISABLED, TREASURY_RESOURCE_UNAVAILABLE, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |
| Lifecycle | LIFE-01 through LIFE-04 | INVALID_REQUEST, UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, ORGANIZATION_NOT_REOPENABLE, ORGANIZATION_DISSOLUTION_VOTE_REQUIRED, ORGANIZATION_NOT_BAND, OBLIGATION_DISPOSITION_REQUIRED, SUCCESSOR_LINEAGE_REQUIRED, ORGANIZATION_TERMINAL, CONFLICT, RATE_LIMITED | DEPENDENCY_UNAVAILABLE, INTERNAL_ERROR |

Every declared code maps to BE00’s response class. VALIDATION_FAILED details contain one row per field × constraint; CONFLICT details may contain expectedVersion/currentVersion only after authorized resource resolution; FORBIDDEN details contain reasonCode and safe recoveryAction; NOT_FOUND details are empty. No route returns a domain-specific 500 with stack, SQL, policy graph, private evidence, or provider text.

## Database Schema

### Schema and ownership boundary

Canonical relationship state lives in non-exposed schema identity_private. Browser roles have no table grants. Hono calls migration-owned security-invoker RPCs and allowlisted security-invoker views. RLS is enabled and forced on every mutable/private table. Any exceptional security-definer RPC has an empty fixed search_path, fully qualified names, PUBLIC execution revoked, named grants only, and positive/negative tests. The existing Party and PersonParty foreign keys are references to companion-owned canonical records; this spec never duplicates their identity fields.

All mutable canonical tables carry created_at, updated_at and positive bigint version unless marked append-only. Server actor/context fields are derived, never accepted from JSON. Protected columns are omitted from public projections.

The IA typed-field registry’s generic core keys are normalized without duplicating authority: logical id maps to the physical primary key shown below; logical state maps to the row’s explicit state or temporal active/ended status; owner_id is a nullable derived owner/control reference where ownership can be ownerless and is never caller-writable. The deep-dive names remain the canonical domain fields. This mapping resolves the registry’s generic owner/state requirement against the deep-dive organization fields and keeps lifecycle separate from ownership_state.

### Canonical tables

| Table | Columns and constraints | State/immutability |
|---|---|---|
| identity_private.organization_party | party_id UUID PK/FK party; ownership_state unclaimed/owned/ownerless; lifecycle active/dormant/closing/closed/dissolving/dissolved; inferred_quiet_at nullable timestamptz; closing_at nullable timestamptz; version bigint >0; created_at/updated_at | Ownership and lifecycle are orthogonal. Lifecycle terminal dissolved cannot reopen. Quietness is an inference only, never public lifecycle truth. |
| identity_private.organization_type_registry | type_code lowercase PK; registry_version positive; display policy; active boolean; created_at/updated_at | Versioned protected registry. Launch codes are band, collective, studio, venue, label, agency, shop. Deactivation prevents new assignment but preserves history. |
| identity_private.organization_type_assignment | id UUID PK; organization_id FK; type_code FK registry; starts_at; ends_at nullable and later than start; actor_id; version; created_at/updated_at | One temporal row per add; ending removes surface, not history. Active means ends_at is null. |
| identity_private.membership_tenure | id UUID PK; organization_id FK; person_id FK; state invited/asserted/confirmed/ended/disputed/rejected/expired; provenance invitation/historical_assertion; starts_on; ends_on nullable and later; accepted_at/revoked_at nullable; accepted_terms_version_id nullable FK; version; actor_id; created_at/updated_at | New rejoin is a new tenure. Rejected assertion/evidence remains protected. Current authority exists only in confirmed state plus date/terms predicates. |
| identity_private.membership_capacity_period | id UUID PK; tenure_id FK; capacity `permanent|touring|staff|honorary`; starts_on; ends_on nullable and later; version; created_at/updated_at | Temporal child. Period is contained by tenure and non-overlapping within a tenure. Permanent is the only seeded commercial capacity. |
| identity_private.representation_edge | id UUID PK; principal_party_id FK; representative_party_id FK and distinct; activities text[] nonempty; domains text[] nonempty; territories text[] nonempty; starts_at; ends_at later; communicate boolean; ceiling_minor nullable bigint >=0; currency nullable char(3); agreement_ref nullable UUID; state draft/pending/active/rejected/revoked/expired; principal_confirmed_at/representative_confirmed_at nullable; version; created_at/updated_at | Both confirmations required for active. Null ceiling is non-monetary. Overlap warns/coexists; exact scope is not unique-blocked. |
| identity_private.mandate_grant | id UUID PK; relationship_type membership/representation; relationship_id UUID; activities text[] nonempty; domains_mode all/explicit; domains text[]; ceiling_minor/currency paired nullable; starts_at/ends_at within source term; source explicit/default/governance; grantor_party_id FK; state draft/active/revoked/expired; version; created_at/updated_at | Scope, term and ceiling are subset-checked against grantor snapshot in the same transaction. A dependent grant revokes when its source loses authority. |
| identity_private.authority_projection | projection_id UUID PK; human_id FK; acting_party_id FK; source_relationship_id nullable; source_mandate_id nullable; activities; domains; communicate; ceiling_minor/currency nullable; valid_from/valid_through; projection_version bigint >0; derived_at | Derived implementation projection; client cannot write it. Source snapshot and version are retained for decisions and downstream reads. |
| identity_private.governance_terms_version | id UUID PK; organization_id FK; version_no positive unique per organization; terms_schema_version positive; terms_json strict JSONB; document_hash bytea exactly 32; state draft/proposed/active/superseded/withdrawn/rejected; proposed_at/effective_at; supersedes_terms_id nullable; required_member_set_hash bytea exactly 32; version; created_at/updated_at | Content, hash and required member set immutable after proposed. One active version per organization. |
| identity_private.governance_confirmation | id UUID PK; terms_id FK; member_person_id FK; decision `confirm|reject`; terms_hash bytea exactly 32; acting_context_id UUID; occurred_at; version | Append evidence. Unique current decision per terms/member; a changed decision is a successor evidence row only if the terms state allows it. No confirmation after member-set drift. |
| identity_private.name_ownership_record | id UUID PK; organization_id FK; terms_version_id nullable; owners UUID[] nonempty; disposition `asserted|licensed|disputed|superseded`; trademark_reference nullable; effective_at; superseded_at nullable; actor_id; version; created_at/updated_at | Implementation record supporting IDA-12. Competing statements coexist; supersession never deletes. Trademark reference is self-supplied, not verification. |
| identity_private.treasury_authorization | id UUID PK; organization_id FK; acting_party_id; mandate_id FK; activity; amount_minor >0; currency; payee_party_id exactly one; authority_source_version; decision `authorized|refused|expired|revoked`; created_at; version | Implementation record only. It is a policy decision/evidence row, not a balance, transfer, split, escrow, tax or payment ledger. |
| identity_private.organization_obligation_disposition | id UUID PK; organization_id FK; kind rights/name/funds/contract/other; status unresolved/assigned/waived; reference_id nullable; actor_id; occurred_at; version | Implementation record required for terminal close/dissolve. Finite resolvable obligations gate closure; unresolved disposition is explicit and does not delete work. |
| identity_private.organization_lineage | id UUID PK; predecessor_organization_id FK; successor_organization_id FK distinct; relationship formed_successor; reason_code; source_version; occurred_at; lineage_version; actor_id | Implementation record for IDA-14 lineage. Immutable, one successor link per re-form command, no destructive reopen or merge. |
| identity_private.organization_duplicate_review | id UUID PK; organization_id FK; status pending/clear/possible_duplicate/review_required/resolved; detector_version; normalized_input_hash; job_id nullable; result_ref nullable; created_at/updated_at | Implementation record for bounded duplicate detection. Timeout never rolls back creation or silently merges; later review can offer claim/merge workflow owned by an approved surface. |

Platform audit_private.audit_events, platform_private.idempotency_records and platform_private.outbox_events remain Shard 00 canonical records. This spec adds no replacement audit, idempotency or outbox tables. Terms JSON and evidence references are protected; event payloads contain IDs only.

### Indexes, uniqueness and exclusion constraints

| Table | Required indexes/constraints | Reason |
|---|---|---|
| organization_party | PK party_id; partial index (lifecycle, ownership_state); index (closing_at) where lifecycle in closing,dissolving | Safe organization reads and lifecycle sweeps |
| organization_type_registry | PK type_code; unique (registry_version,type_code); index active/type_code | Versioned lookup and migration |
| organization_type_assignment | partial unique (organization_id,type_code) where ends_at is null; index (organization_id,starts_at desc); index (type_code,ends_at) | One active type, history and consumer eligibility |
| membership_tenure | indexes (organization_id,state,starts_on); (person_id,state,starts_on); (accepted_terms_version_id); partial index confirmed current dates | Roster, self read, authority and terms member set |
| membership_capacity_period | exclusion constraint tenure_id WITH = and daterange(starts_on, coalesce(ends_on,'infinity'),'[)') WITH &&; index (tenure_id,starts_on) | Non-overlap and contained temporal capacity |
| representation_edge | indexes (principal_party_id,state,starts_at); (representative_party_id,state,starts_at); GIN activities/domains/territories; index (ends_at) where state=active | Scoped lookup, expiry and overlap warnings |
| mandate_grant | indexes (relationship_type,relationship_id,state); (grantor_party_id,state,starts_at); partial index (ends_at) where state=active; GIN activities/domains | Authority resolution and dependent revocation |
| authority_projection | unique (human_id,acting_party_id); index (source_relationship_id,source_mandate_id); index (valid_through) | Current projection and invalidation |
| governance_terms_version | unique (organization_id,version_no); partial unique organization_id where state=active; index (organization_id,state,proposed_at desc) | One current active version and proposal reads |
| governance_confirmation | unique (terms_id,member_person_id) for current decision; index (terms_id,decision); index (member_person_id,occurred_at desc) | Unanimity and evidence lookup |
| name_ownership_record | index (organization_id,effective_at desc); partial index (organization_id) where superseded_at is null; GIN owners | Statements and public/private projection |
| treasury_authorization | unique (mandate_id,authority_source_version,amount_minor,payee_party_id) for idempotent decision; index (organization_id,created_at desc); index (decision,created_at) | No duplicate policy decision and bounded evidence |
| obligation_disposition | index (organization_id,status,kind); unique (organization_id,kind,reference_id) when reference_id is not null | Terminal gate and recovery |
| organization_lineage | unique predecessor_organization_id; unique successor_organization_id; index (occurred_at) | No branching or duplicate predecessor |
| organization_duplicate_review | unique (organization_id,detector_version,normalized_input_hash); index (status,created_at); index job_id | Async continuation and review |

All partial/exclusion expressions use migration-created enum/check-compatible values. A constraint violation maps to a typed 409/422, never a generic database error. Membership tenure itself is not globally exclusion-constrained because historical rejoin creates a new tenure; authority queries select confirmed valid intervals.

### RPC and projection registry

| RPC/view | Caller | Revalidation and write boundary | Returned data |
|---|---|---|---|
| identity_private.rpc_create_organization | authenticated Hono command | mode, registry, actor, idempotency; inserts org, initial rows, review record, audit and outbox | OrganizationResource and review job reference |
| identity_private.rpc_change_organization_type | Hono command | owner/admin authority, one type, organization CAS; assignment history retained | Assignment or organization projection |
| identity_private.rpc_invite_membership | Hono command | invitation authority, current terms, person FK, org CAS | Tenure projection |
| identity_private.rpc_assert_membership | Hono command | historical provenance/evidence ref and org/person policy | Asserted tenure without current authority |
| identity_private.rpc_accept_or_end_membership | Hono command | self/counterparty predicate, tenure CAS, terms lock and retroactive confirmation | Updated tenure and invalidation refs |
| identity_private.rpc_add_capacity_period | Hono command | tenure authority, containment and exclusion constraint | Capacity period |
| identity_private.rpc_create_or_confirm_representation | Hono command | participant party context, dual confirmation row lock and edge CAS | Pending/active edge |
| identity_private.rpc_revoke_representation | Hono command | participant/source grant, edge CAS, projection invalidation | Revoked edge |
| identity_private.rpc_grant_or_revoke_mandate | Hono command | source snapshot subset and dependent cascade under serializable transaction | Mandate and source version |
| identity_private.rpc_propose_or_confirm_governance | Hono command | member-set/hash lock, current terms, confirmation identity/context | Proposal or confirmation |
| identity_private.rpc_activate_governance | Hono command | terms/member/confirmation locks and one-active constraint | Active terms and activation event |
| identity_private.rpc_record_name_statement | Hono command | owner/admin grant, strict statement, append-only insert | Attributed statement |
| identity_private.rpc_authorize_treasury_act | Hono command | member view plus mandate/activity/amount/currency/payee policy | Implementation authorization record |
| identity_private.rpc_close_reopen_dissolve_reform | Hono command | lifecycle CAS, finite disposition/vote/lineage checks | Organization or lineage resource |
| api_identity.organization_control_snapshot_v1 | Shard 22 read | security-invoker view joins active label assignment and current owner/admin snapshot | IDs, type/control versions, valid-through |
| api_identity.authority_snapshot_v1 | Shard 30 read | security-invoker view exposes accepted source versions only | Canonical IDs and authority-source version; never announce_waive |
| api_identity.acting_context_event_projection_v1 | Shard 39 read | security-invoker event projection, no provider claims | Opaque party/context IDs and versions |

RPC names are registry identifiers, not public URL names. A command must use one RPC that owns the state/audit/idempotency/outbox boundary; a sequence of client-visible RPCs cannot substitute for the transaction. Views are read-only and cannot mint grants or write source state.

### Retention, tombstones and deletion

- Organization, type, tenure, capacity, representation, mandate, governance, confirmation, statement and lineage history is retained as immutable evidence. Removing a surface ends/marks the row; it never hard-deletes relied-upon history.
- Rejected membership assertions retain only a protected evidence reference and safe state; public reads cannot discover the rejected assertion or evidence.
- Authority projections are disposable derived records. Revocation deletes/invalidates the active projection within the revocation transaction and queues idempotent rebuild; the source relationship and audit remain.
- Duplicate-review and attempt summaries follow the Shard 00 30-day operational retention unless a legal hold or owning policy extends it. Normalized hashes are not reversible names.
- Treasury authorization is retained as policy evidence, not as money history. Any payment provider or ledger owns its own retention and deletion contract.
- A restored database never purges lineage or source history to make downstream state look current. Recovery marks stale projections and reconciles them from canonical rows.

### Grants and RLS

| Record/projection | anon | authenticated human | owner/admin | operator | queue/consumer | service/maintenance |
|---|---|---|---|---|---|---|
| Organization public projection | SELECT only publication-approved fields | same plus authorized detail | same | named evidence projection | read by ID for registered invalidation | no direct browser path |
| Organization and type tables | deny | RPC/view only when current context can read | RPC write only current owner/admin grant | named recovery capability, reason and audit | expiry/projection RPC only | named migration/runbook RPC only |
| Membership/capacity | deny | own confirmed/invited row; organization roster only when current member policy allows | organization-scoped current grant | bounded evidence capability | projection invalidation only | no wildcard access |
| Representation | deny | participant/scoped representative fields | source-party grant within scope | named recovery only | invalidation only | no direct table writes |
| Mandate/authority projection | deny | own eligible projection only | current grant and source relationship | evidence projection with reason | registered projection refresh | no interactive use |
| Governance terms/confirmations | deny | own confirmation and member-eligible terms | proposer/owner/admin within governance grant | named protected evidence | activation consumer sees IDs/hashes only | runbook only |
| Name statement | deny unless public projection approved | authorized organization view | current owner/admin append | bounded review projection | org changed invalidation | runbook only |
| Treasury implementation records | deny | confirmed member read of policy view; authorize via RPC only | exact mandate predicate | explicit financial-policy capability and reason | no fund effect; status refresh only | named reconciliation |
| Obligation/lineage/duplicate support records | deny | no direct table access | route-specific projection | named recovery projection | job/expiry RPC | runbook only |

RLS predicates repeat the server-derived human ID, acting party ID, resource ownership/relationship, current term, capability, target lifecycle, and disclosure policy. Caller-supplied partyId, humanId, grantorPartyId, member set, role, source version or capability is never trusted. Shard 22 gets a read-only label-control snapshot requiring active label assignment plus current owner/admin authority; its label_distribution_mandate cannot grant organization control. Shard 30 gets canonical relationship/authority versions only and cannot write or infer announce_waive/P-07. Shard 39 gets opaque party/context event references only and never provider-derived authority.

The treasury read contract exposes eligible references to consumer-owned band money records only when a confirmed member passes the view predicate. It never fabricates a platform-held pooled balance, multi-party split, escrow, tax result or transfer status. A consumer must re-run this shard’s authority predicate at its own command boundary.

### Field-level SQL type and relationship ledger

This ledger makes the 01c persistence contract implementer-complete. Every domain field has an SQL type and nullability, each FK or intentional opaque cross-shard reference is named, query indexes are tied to the route patterns, and every relation is forced-RLS with the grants stated in the policy matrix below.

| Table | Typed fields, nullability, and constraints | Foreign keys and relationship boundary | Query indexes and uniqueness | RLS and grants |
|---|---|---|---|---|
| identity_private.organization_party | party_id uuid NOT NULL PK; ownership_state text NOT NULL CHECK IN (unclaimed, owned, ownerless); lifecycle text NOT NULL CHECK IN (active, dormant, closing, closed, dissolving, dissolved); inferred_quiet_at timestamptz NULL; closing_at timestamptz NULL; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | party_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; owner/control is server-derived and may be NULL by ownership_state | PK party_id; index lifecycle, ownership_state; partial index closing_at WHERE lifecycle IN (closing, dissolving) | forced RLS; no anon or authenticated table grant; organization RPC and scoped projections only |
| identity_private.organization_type_registry | type_code text NOT NULL PK CHECK lowercase registry code; registry_version bigint NOT NULL CHECK >0; display_policy jsonb NOT NULL; active boolean NOT NULL; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | no caller FK; protected registry is migration-owned | PK type_code; unique registry_version, type_code; index active, type_code | forced RLS; named registry RPC and release principal only |
| identity_private.organization_type_assignment | id uuid NOT NULL PK; organization_id uuid NOT NULL; type_code text NOT NULL; starts_at timestamptz NOT NULL; ends_at timestamptz NULL CHECK ends_at > starts_at; actor_id uuid NOT NULL; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; type_code REFERENCES identity_private.organization_type_registry(type_code) ON DELETE RESTRICT; actor_id REFERENCES platform_private.party(id) ON DELETE RESTRICT | PK; partial unique organization_id, type_code WHERE ends_at IS NULL; index organization_id, starts_at DESC; index type_code, ends_at | forced RLS; owner/admin mutation RPC; no direct client grant |
| identity_private.membership_tenure | id uuid NOT NULL PK; organization_id uuid NOT NULL; person_id uuid NOT NULL; state text NOT NULL CHECK IN (invited, asserted, confirmed, ended, disputed, rejected, expired); provenance text NOT NULL CHECK IN (invitation, historical_assertion); starts_on date NOT NULL; ends_on date NULL CHECK ends_on > starts_on; accepted_at timestamptz NULL; revoked_at timestamptz NULL; accepted_terms_version_id uuid NULL; version bigint NOT NULL CHECK >0; actor_id uuid NOT NULL; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; person_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; accepted_terms_version_id REFERENCES identity_private.governance_terms_version(id) ON DELETE RESTRICT; actor_id REFERENCES platform_private.party(id) ON DELETE RESTRICT | PK; index organization_id, state, starts_on; index person_id, state, starts_on; index accepted_terms_version_id; partial current-confirmed index | forced RLS; own-member and organization-scoped RPC projections; operators require assignment and reason |
| identity_private.membership_capacity_period | id uuid NOT NULL PK; tenure_id uuid NOT NULL; capacity text NOT NULL CHECK IN (permanent, touring, staff, honorary); starts_on date NOT NULL; ends_on date NULL CHECK ends_on > starts_on; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | tenure_id REFERENCES identity_private.membership_tenure(id) ON DELETE RESTRICT | PK; GiST exclusion tenure_id plus daterange; index tenure_id, starts_on | forced RLS; subject or organization RPC only; no direct table grant |
| identity_private.representation_edge | id uuid NOT NULL PK; principal_party_id uuid NOT NULL; representative_party_id uuid NOT NULL CHECK distinct; activities text[] NOT NULL CHECK cardinality >0; domains text[] NOT NULL CHECK cardinality >0; territories text[] NOT NULL CHECK cardinality >0; starts_at timestamptz NOT NULL; ends_at timestamptz NOT NULL CHECK ends_at > starts_at; communicate boolean NOT NULL; ceiling_minor bigint NULL CHECK >=0; currency char(3) NULL; agreement_ref uuid NULL; state text NOT NULL CHECK IN (draft, pending, active, rejected, revoked, expired); principal_confirmed_at timestamptz NULL; representative_confirmed_at timestamptz NULL; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | principal_party_id and representative_party_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; agreement_ref is a protected object/evidence opaque ref | PK; index principal_party_id, state, starts_at; index representative_party_id, state, starts_at; GIN activities, domains, territories; index ends_at WHERE state = active | forced RLS; participant-scoped RPC; no anonymous rows or direct grants |
| identity_private.mandate_grant | id uuid NOT NULL PK; relationship_type text NOT NULL; relationship_id uuid NOT NULL; activities text[] NOT NULL CHECK cardinality >0; domains_mode text NOT NULL CHECK IN (all, explicit); domains text[] NOT NULL; ceiling_minor bigint NULL CHECK >=0; currency char(3) NULL; starts_at timestamptz NOT NULL; ends_at timestamptz NOT NULL CHECK ends_at > starts_at; source text NOT NULL CHECK IN (explicit, default, governance); grantor_party_id uuid NOT NULL; state text NOT NULL CHECK IN (draft, active, revoked, expired); version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | relationship_id is a typed relationship reference resolved by relationship_type; grantor_party_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; no polymorphic SQL FK is permitted | PK; index relationship_type, relationship_id, state; index grantor_party_id, state, starts_at; partial index ends_at WHERE state = active; GIN activities, domains | forced RLS; current grantor or named operator RPC only; dependent consumers receive versioned projections |
| identity_private.authority_projection | projection_id uuid NOT NULL PK; human_id uuid NOT NULL; acting_party_id uuid NOT NULL; source_relationship_id uuid NULL; source_mandate_id uuid NULL; activities text[] NOT NULL; domains text[] NOT NULL; communicate boolean NOT NULL; ceiling_minor bigint NULL CHECK >=0; currency char(3) NULL; valid_from timestamptz NOT NULL; valid_through timestamptz NULL; projection_version bigint NOT NULL CHECK >0; derived_at timestamptz NOT NULL | human_id and acting_party_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; source IDs reference identity_private relationship/mandate records where owned, otherwise carry an opaque source boundary | PK; unique human_id, acting_party_id; index source_relationship_id, source_mandate_id; index valid_through | forced RLS; security-invoker projection only; queue refresh RPC has no interactive grant |
| identity_private.governance_terms_version | id uuid NOT NULL PK; organization_id uuid NOT NULL; version_no bigint NOT NULL CHECK >0; terms_schema_version bigint NOT NULL CHECK >0; terms_json jsonb NOT NULL CHECK jsonb_typeof(terms_json) = object; document_hash bytea NOT NULL CHECK octet_length = 32; state text NOT NULL CHECK IN (draft, proposed, active, superseded, withdrawn, rejected); proposed_at timestamptz NOT NULL; effective_at timestamptz NULL; supersedes_terms_id uuid NULL; required_member_set_hash bytea NOT NULL CHECK octet_length = 32; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; supersedes_terms_id REFERENCES identity_private.governance_terms_version(id) ON DELETE RESTRICT | PK; unique organization_id, version_no; partial unique organization_id WHERE state = active; index organization_id, state, proposed_at DESC | forced RLS; member-set scoped read; activation RPC requires owner/admin and all confirmations |
| identity_private.governance_confirmation | id uuid NOT NULL PK; terms_id uuid NOT NULL; member_person_id uuid NOT NULL; decision text NOT NULL CHECK IN (confirm, reject); terms_hash bytea NOT NULL CHECK octet_length = 32; acting_context_id uuid NOT NULL; occurred_at timestamptz NOT NULL; version bigint NOT NULL CHECK >0 | terms_id REFERENCES identity_private.governance_terms_version(id) ON DELETE RESTRICT; member_person_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; acting_context_id is a BE01 context opaque ref | PK; unique terms_id, member_person_id for current decision; index terms_id, decision; index member_person_id, occurred_at DESC | forced RLS; current member sees own confirmation; activation RPC only writes; no direct grant |
| identity_private.name_ownership_record | id uuid NOT NULL PK; organization_id uuid NOT NULL; terms_version_id uuid NULL; owners uuid[] NOT NULL CHECK cardinality >0; disposition text NOT NULL CHECK IN (asserted, licensed, disputed, superseded); trademark_reference text NULL; effective_at timestamptz NOT NULL; superseded_at timestamptz NULL; actor_id uuid NOT NULL; version bigint NOT NULL CHECK >0; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; terms_version_id REFERENCES identity_private.governance_terms_version(id) ON DELETE RESTRICT; owner IDs are party refs validated by RPC | PK; index organization_id, effective_at DESC; partial index organization_id WHERE superseded_at IS NULL; GIN owners | forced RLS; owner/admin append RPC; public projection only after approval |
| identity_private.treasury_authorization | id uuid NOT NULL PK; organization_id uuid NOT NULL; acting_party_id uuid NOT NULL; mandate_id uuid NOT NULL; activity text NOT NULL; amount_minor bigint NOT NULL CHECK >0; currency char(3) NOT NULL; payee_party_id uuid NOT NULL; authority_source_version bigint NOT NULL CHECK >0; decision text NOT NULL CHECK IN (authorized, refused, expired, revoked); created_at timestamptz NOT NULL; version bigint NOT NULL CHECK >0 | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; acting_party_id and payee_party_id REFERENCES platform_private.party(id) ON DELETE RESTRICT; mandate_id REFERENCES identity_private.mandate_grant(id) ON DELETE RESTRICT | PK; unique mandate_id, authority_source_version, amount_minor, payee_party_id; index organization_id, created_at DESC; index decision, created_at | forced RLS; confirmed-member policy view and authorize RPC only; no payment or balance grant |
| identity_private.organization_obligation_disposition | id uuid NOT NULL PK; organization_id uuid NOT NULL; kind text NOT NULL CHECK IN (rights, name, funds, contract, other); status text NOT NULL CHECK IN (unresolved, assigned, waived); reference_id uuid NULL; actor_id uuid NOT NULL; occurred_at timestamptz NOT NULL; version bigint NOT NULL CHECK >0 | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; reference_id is an owner-domain opaque ref | PK; index organization_id, status, kind; unique organization_id, kind, reference_id WHERE reference_id IS NOT NULL | forced RLS; lifecycle RPC and assigned operator projection only |
| identity_private.organization_lineage | id uuid NOT NULL PK; predecessor_organization_id uuid NOT NULL; successor_organization_id uuid NOT NULL CHECK successor_organization_id <> predecessor_organization_id; relationship text NOT NULL CHECK relationship = formed_successor; reason_code text NOT NULL; source_version bigint NOT NULL CHECK >0; occurred_at timestamptz NOT NULL; lineage_version bigint NOT NULL CHECK >0; actor_id uuid NOT NULL | predecessor_organization_id and successor_organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; actor_id REFERENCES platform_private.party(id) ON DELETE RESTRICT | PK; unique predecessor_organization_id; unique successor_organization_id; index occurred_at DESC | forced RLS; participant and operator evidence RPC only; immutable after insert |
| identity_private.organization_duplicate_review | id uuid NOT NULL PK; organization_id uuid NOT NULL; status text NOT NULL CHECK IN (pending, clear, possible_duplicate, review_required, resolved); detector_version text NOT NULL; normalized_input_hash bytea NOT NULL CHECK octet_length = 32; job_id uuid NULL; result_ref uuid NULL; created_at timestamptz NOT NULL; updated_at timestamptz NOT NULL | organization_id REFERENCES identity_private.organization_party(party_id) ON DELETE RESTRICT; job_id REFERENCES platform_private.jobs(id) ON DELETE RESTRICT; result_ref is a protected detector result ref | PK; unique organization_id, detector_version, normalized_input_hash; index status, created_at; index job_id | forced RLS; duplicate-review worker and assigned operator RPC only; no public enumeration |

## Middleware & Policies

### Inherited Hono order

BE00 lines 255–269 are normative: route registry/request ID; transport/security; raw webhook branch; Supabase session authentication; acting-context resolution; strict Zod validation; capability/resource authorization; idempotency and CAS; one RPC transaction; response normalization; observability completion. This shard adds no Auth provider mechanic. User JWT metadata, URL context, body actor IDs and Queue claims cannot grant authority.

### Exact ten-step authority resolution

Every protected read and command runs these steps in order inside the same transaction that commits the mutation. A failure stops before canonical mutation, audit success, or outbox success.

1. Verify the Supabase session and active account through the Shard 00 authentication boundary; derive one human/person UUID.
2. Load the requested acting party from server-side context; reject client-provided identity, role, capability and party claims.
3. Resolve self, alias, organization membership or representation only when the relationship is active, accepted and within its date/term; this spec does not implement alias mechanics.
4. Select one current mandate source: active governance projection, explicit active grant, or the exact band default. Never union expired, revoked, pending or unreadable sources.
5. Require the requested activity in that source; for representation also require the requested domain. Membership domains are all only when the source says all.
6. For send/reply/notice actions require communicate separately, regardless of commercial activity grants.
7. Require territory, term, resource lifecycle/visibility and any domain invariant; consumer-owned purpose capability is an additional conjunct. Shard 30’s announce_waive is never generated, implied or returned by this resolver.
8. For money compare integer minor units and exact ISO currency to the source ceiling/default. No automatic conversion; null ceiling fails every monetary action.
9. Recheck resource lifecycle, policy/NDA/visibility, expected version, RLS and RPC predicates immediately before write.
10. Commit canonical mutation, authority-source snapshot, audit decision, idempotency result and outbox event atomically.

The decision record includes human, acting party, source relationship, source mandate, source version, activity, domain, term, territory, ceiling/currency, resource predicate and any explicit consumer capability reference. The consumer capability reference for announce_waive is always absent in this shard; Shard 30 records it in its own aggregate.

### Principal matrix and policy details

| Principal | Allow | Deny / disclosure |
|---|---|---|
| Anonymous | ORG-02 publication projection only | Membership, mandates, governance, treasury and private name statements |
| Self person | Accept own invitation, assert own historical tenure where policy permits, confirm governance as required member | Fabricate organization consent, add org type, grant mandate or act for another party |
| Organization member | Read permitted roster/governance and act through current accepted mandate/default | Membership presence alone grants no generic authority outside explicit band seed |
| Owner/admin | Manage types, relationships, mandates, eligible governance, name and lifecycle within current grant | Exceed grant, rewrite history, adjudicate name/legal ownership, infer Shard 30 capability |
| Representation participant | Conjoined activity/domain/territory/term/ceiling/communicate scope | Unscoped private access, ownership transfer, generic monetary action or alias disposal |
| Operator | Named capability, recent step-up, reason and bounded evidence projection | General browsing, arbitrary merge, legal adjudication or direct table edit |
| Queue/service principal | Registered event/job/projection with lease and current canonical re-read | Interactive authority, wildcard party access, provider/Queue claim as authorization |

High-risk commands (membership end, representation confirm/revoke, mandates, governance, treasury and lifecycle) require recent step-up where the route policy says high-risk, named capability and reason. Denials preserve canonical state and emit a safe audit denial. Wrong-resource existence-sensitive requests collapse to 404. Rate limits inherit BE00; organization creation also applies friction review after more than 3 in 24 hours or more than 10 lifetime without a permanent count-only denial.

### Per-operation middleware, error envelope, limits, telemetry, and test matrix

Every row below is keyed to the authoritative operation ID. Route Registry cells remain the single source for exact route-specific auth, rate, idempotency, and success/error status values; this matrix adds the boundary execution policy and makes each operation explicit.

| Operation ID | Auth and ownership | Rate limit | Input validation | CORS policy | Global error envelope | Error and retry guidance | Pagination and limits | Idempotency and concurrency | Observability | Test oracle |
|---|---|---|---|---|---|---|---|---|---|---|
| ORG-01 | The authoritative Route Registry ORG-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry ORG-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for ORG-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry ORG-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for ORG-01; assert exact ApiError envelope and no unauthorized side effect. |
| ORG-02 | The authoritative Route Registry ORG-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry ORG-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for ORG-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy public-read allowlist; credentials=false; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry ORG-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for ORG-02; assert exact ApiError envelope and no unauthorized side effect. |
| TYPE-01 | The authoritative Route Registry TYPE-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry TYPE-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for TYPE-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry TYPE-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for TYPE-01; assert exact ApiError envelope and no unauthorized side effect. |
| TYPE-02 | The authoritative Route Registry TYPE-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry TYPE-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for TYPE-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry TYPE-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for TYPE-02; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-01 | The authoritative Route Registry MEM-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MEM-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-01; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-02 | The authoritative Route Registry MEM-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MEM-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-02; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-03 | The authoritative Route Registry MEM-03 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-03 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-03 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MEM-03 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-03; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-04 | The authoritative Route Registry MEM-04 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-04 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-04 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MEM-04 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-04; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-05 | The authoritative Route Registry MEM-05 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-05 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-05 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MEM-05 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-05; assert exact ApiError envelope and no unauthorized side effect. |
| MEM-06 | The authoritative Route Registry MEM-06 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MEM-06 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MEM-06 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Opaque cursor; default 25 and maximum 50; deterministic created_at DESC then id DESC; only the operation declared allowlisted filters; offset and unknown filters rejected. | The authoritative Route Registry MEM-06 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MEM-06; assert exact ApiError envelope and no unauthorized side effect. |
| REP-01 | The authoritative Route Registry REP-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry REP-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for REP-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry REP-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for REP-01; assert exact ApiError envelope and no unauthorized side effect. |
| REP-02 | The authoritative Route Registry REP-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry REP-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for REP-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry REP-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for REP-02; assert exact ApiError envelope and no unauthorized side effect. |
| REP-03 | The authoritative Route Registry REP-03 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry REP-03 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for REP-03 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry REP-03 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for REP-03; assert exact ApiError envelope and no unauthorized side effect. |
| REP-04 | The authoritative Route Registry REP-04 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry REP-04 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for REP-04 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Opaque cursor; default 25 and maximum 50; deterministic created_at DESC then id DESC; only the operation declared allowlisted filters; offset and unknown filters rejected. | The authoritative Route Registry REP-04 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for REP-04; assert exact ApiError envelope and no unauthorized side effect. |
| MAN-01 | The authoritative Route Registry MAN-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MAN-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MAN-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MAN-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MAN-01; assert exact ApiError envelope and no unauthorized side effect. |
| MAN-02 | The authoritative Route Registry MAN-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry MAN-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for MAN-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry MAN-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for MAN-02; assert exact ApiError envelope and no unauthorized side effect. |
| AUTH-01 | The authoritative Route Registry AUTH-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry AUTH-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for AUTH-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry AUTH-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for AUTH-01; assert exact ApiError envelope and no unauthorized side effect. |
| GOV-01 | The authoritative Route Registry GOV-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry GOV-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for GOV-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry GOV-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for GOV-01; assert exact ApiError envelope and no unauthorized side effect. |
| GOV-02 | The authoritative Route Registry GOV-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry GOV-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for GOV-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry GOV-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for GOV-02; assert exact ApiError envelope and no unauthorized side effect. |
| GOV-03 | The authoritative Route Registry GOV-03 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry GOV-03 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for GOV-03 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry GOV-03 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for GOV-03; assert exact ApiError envelope and no unauthorized side effect. |
| GOV-04 | The authoritative Route Registry GOV-04 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry GOV-04 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for GOV-04 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry GOV-04 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for GOV-04; assert exact ApiError envelope and no unauthorized side effect. |
| NAME-01 | The authoritative Route Registry NAME-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry NAME-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for NAME-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry NAME-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for NAME-01; assert exact ApiError envelope and no unauthorized side effect. |
| NAME-02 | The authoritative Route Registry NAME-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry NAME-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for NAME-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Opaque cursor; default 25 and maximum 50; deterministic created_at DESC then id DESC; only the operation declared allowlisted filters; offset and unknown filters rejected. | The authoritative Route Registry NAME-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for NAME-02; assert exact ApiError envelope and no unauthorized side effect. |
| TRE-01 | The authoritative Route Registry TRE-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry TRE-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for TRE-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry TRE-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for TRE-01; assert exact ApiError envelope and no unauthorized side effect. |
| TRE-02 | The authoritative Route Registry TRE-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry TRE-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for TRE-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry TRE-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for TRE-02; assert exact ApiError envelope and no unauthorized side effect. |
| LIFE-01 | The authoritative Route Registry LIFE-01 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry LIFE-01 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for LIFE-01 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry LIFE-01 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for LIFE-01; assert exact ApiError envelope and no unauthorized side effect. |
| LIFE-02 | The authoritative Route Registry LIFE-02 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry LIFE-02 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for LIFE-02 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry LIFE-02 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for LIFE-02; assert exact ApiError envelope and no unauthorized side effect. |
| LIFE-03 | The authoritative Route Registry LIFE-03 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry LIFE-03 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for LIFE-03 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry LIFE-03 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for LIFE-03; assert exact ApiError envelope and no unauthorized side effect. |
| LIFE-04 | The authoritative Route Registry LIFE-04 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry LIFE-04 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for LIFE-04 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Not a list endpoint; pagination parameters are rejected and the operation returns one bounded resource, job, redirect, or fixed projection. | The authoritative Route Registry LIFE-04 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for LIFE-04; assert exact ApiError envelope and no unauthorized side effect. |
| LIFE-05 | The authoritative Route Registry LIFE-05 auth and ownership predicate applies; actor and acting party are server-derived; known visible target without capability is 403 and concealed or unreadable target is 404. | The authoritative Route Registry LIFE-05 bucket applies; emit RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset and Retry-After on 429; caller input cannot widen the bucket. | Strict Zod 4 request schema for LIFE-05 runs at the Hono boundary before authorization and RPC; unknown keys, malformed path, query, header or body are rejected; signed raw bytes are verified before parsing where this operation has a webhook branch. | CORS policy first-party-auth allowlist; credentials only for same-site; wildcard and reflected origins denied; Vary: Origin. | BE00 ApiError { code, message, requestId, details }; code is the application enum, message is safe and localizable, and details is a bounded allowlist. | Operation-declared HTTP statuses and application codes remain authoritative; 4xx is not auto-retried, 429 honors Retry-After, and 502/503/504 retry at most twice after 250 ms and 750 ms with the same idempotency binding after status reconciliation; unknown mutation outcome stays pending or manual review. | Single bounded lineage chain; no pagination parameters accepted; predecessor and successor order is canonical and finite. | The authoritative Route Registry LIFE-05 idempotency and CAS rule applies; mutation replay requires the same normalized request and expected version, while reads perform no mutation reservation. | Emit requestId, operationId, status, duration, rate, authorization, conflict, idempotency and retry outcomes; retain IDs or hashes only and never request payloads or PII. | Contract, authz, RLS, 403/404, rate, replay, conflict, timeout and redaction tests are required for LIFE-05; assert exact ApiError envelope and no unauthorized side effect. |
## Data Flow

### Protected mutation transaction

1. Middleware creates validated RequestContext and normalized request hash.
2. Domain RPC revalidates human, acting party, relationship, target, lifecycle, current terms and If-Match.
3. Idempotency record reserves the operation under actor, operation ID and key hash. Same key/hash waits/replays; different hash returns IDEMPOTENCY_MISMATCH.
4. RPC locks the aggregate and relevant source rows, runs the ten-step resolver, checks unique/exclusion/state constraints and writes canonical rows.
5. The same transaction writes authority snapshot, audit allowed/completed or failed evidence, any implementation record, outbox event and idempotency response.
6. Commit success returns authoritative resource/ETag; a lost response replays exact result. Commit failure creates no canonical mutation or trusted event.
7. Immediate outbox dispatch is best effort; BE00 sweeper leases undispatched rows. Queue consumers re-read current rows/version and never trust event payload as state.

### Idempotency, ETag and CAS contract

- ORG-01 uses operation ID ORG-01 and actor-bound Idempotency-Key; all other mutations use their route operation ID and require the key. Key reuse with the same normalized request, actor, acting party, target, contract version and expected version replays the original status/body/ETag. Changed body, target, context or version returns 409 IDEMPOTENCY_MISMATCH.
- BE00 canonicalization applies before hashing: strict Zod output, sorted object keys, absent optional keys omitted, NFC strings, lowercase UUIDs, RFC3339 UTC, canonical integers and no non-finite numbers. Raw JSON key order cannot create a second effect.
- Mutable organization, assignment, tenure, edge, mandate, terms, confirmation, authorization and lifecycle rows use the exact strong quoted If-Match tag. A missing or malformed tag is 400; an authorized stale tag is 409 VERSION_MISMATCH. No unapproved 428 response is introduced.
- CAS occurs after the ten-step authority recheck and inside the same RPC as state, source snapshot, audit, outbox and idempotency completion. A losing concurrent writer creates no state, event or new idempotency result.
- Dual representation confirmation locks the edge and both party confirmation columns. Governance activation locks the proposal, current permanent-member rows, confirmation rows and organization pointer. Mandate revocation locks the source and all dependent grants in deterministic ID order.
- A transaction that times out at the client is reconciled with the original key before a retry. A provider or queue ambiguity cannot be marked failed_retryable in this shard because no provider effect is owned here; canonical relationship status is re-read instead.

### Organization creation and duplicate detection

ORG-01 validates exactly one mode and registry types. The transaction creates OrganizationParty, a confirmed self membership only for self_member mode, no inferred membership for shadow/custodial or external-reference mode, initial type assignments, a duplicate-review implementation record and identity.organization.changed.v1. Type-aware duplicate detection runs before commit with a hard two-second bound when available. On timeout the organization still commits; a duplicate-review job continues from the normalized input hash. A possible match opens an explicit review/claim/merge offer; there is no silent merge and no reattribution.

### Relationships, governance and lifecycle

- Type add/removal writes one assignment at a time. Removing the last type leaves a typeless collective. Type-specific fields remain undiscoverable until the owning domain gate passes.
- Invitation acceptance locks the invitation, tenure and current governance terms. A historical assertion stores protected evidence and grants no current authority. Ending now revokes immediately; contested retroactive dates preserve current revocation and record dispute without rewriting history.
- Representation creates pending scope; each party confirmation is a separate idempotent CAS. Activation is only after both confirmations. Revoke/expiry invalidates dependent projections atomically.
- Mandate grant checks requested activities/domains/term/ceiling as subset of the grantor snapshot. Revoke recursively revokes dependent subgrants and emits context revocation for each affected person.
- Governance proposal snapshots the current permanent member set. Join/leave freezes the proposal. Activation locks proposal, exact member-set hash, every confirmation and active pointer in one RPC; rejection leaves prior/default rules authoritative.
- Name statements append competing assertions and never claim clearance. Treasury authorization checks one confirmed band member, current mandate, exact amount/currency/ceiling and one payee; it never moves money.
- Ordinary closure may reopen only from closed. Band dissolution follows active governance and is terminal. Unresolved name/funds/rights obligations get explicit disposition rows. Re-form creates a new successor party and immutable lineage; no work is deleted.

### Event and queue contracts

All events are BE00 PlatformEvent with positive aggregateVersion serialized as a decimal string. Payloads are identifier-only and use the exact Shard 01 names:

| Event | Payload | Emitted by this shard | Consumer behavior |
|---|---|---|---|
| identity.organization.changed.v1 | { organizationId } | create/type/name/lifecycle/lineage changes and governance proposal/confirmation invalidations | Refetch type-owned and search projections |
| identity.relationship.changed.v1 | { relationshipType, relationshipId } | membership, capacity, representation and mandate changes | Refetch roster, authority and notifications |
| identity.acting-context.revoked.v1 | { personId, partyId, relationshipId } | relationship/mandate revocation when a concrete person context is affected, one event per affected context | Purge context caches; submit still revalidates |
| identity.governance.activated.v1 | { organizationId, termsVersionId } | successful unanimous activation only | Refetch mandate projection and governance consumers |

The event type literal plus schemaVersion 1 is the dispatch key. No terms JSON, evidence, names, person details, money, provider payload or authority claim enters the event or QueueEnvelope. Events are inserted with state in the same transaction as their mutation. Consumers use BE00 at-least-once delivery, 5-minute default job lease where applicable, 15s/60s/300s retry delays, max four deliveries and a configured DLQ.

| Consumer | Event pair | Lease/retry | Terminal behavior |
|---|---|---|---|
| identity.organization.projection-refresh | identity.organization.changed.v1/1 | 2-minute lease, CAS projection version, max four deliveries | Refetch current organization; stale/duplicate is no-op; unknown schema to DLQ |
| identity.authority.projection-refresh | identity.relationship.changed.v1/1 and acting-context.revoked.v1/1 | 2-minute lease, current relationship/projection CAS | Rebuild from current source; revoked source removes projection; repeated event safe |
| identity.governance.consumer-refresh | identity.governance.activated.v1/1 | 2-minute lease, terms/version CAS | Refetch active terms; stale event no regression; manual review on invariant failure |
| identity.organization.duplicate-review | identity.organization.changed.v1/1 when duplicate review job is registered | BE00 job lease and attempt summaries | Timeout/ambiguous detector remains pending; exhausted review is manual, never auto-merge |

Realtime, if enabled, carries only authorized IDs and versions. Offline reconnect is reauthenticate, resolve context, refresh terms/settings, submit original idempotency key/If-Match, then commit or explicit conflict.

### External seam contracts and no-provider boundary

This shard owns canonical relationship and governance state. It has no external identity-provider write and accepts no provider payload, provider-selected owner, or remote legal decision. Its non-local seams are the named authority projection, bounded duplicate detector, and BE00 outbox; a circuit-open result is always unavailable or pending and never an authority grant.

| Operations | Seam and owner | Exact request | Exact response | Timeout | Retry policy | Circuit, open state, and recovery |
|---|---|---|---|---:|---|---|
| ORG-01 through LIFE-05 | 01a and BE00 authority projection | `actorHumanId`, `actingPartyId`, `targetOrganizationId`, `capabilityCode`, `purposeCode`, `sourceVersion`, `requestId` | `decision`, `organizationVersion`, `relationshipVersion`, `mandateVersion`, `scope`, `expiresAt` | 2,000 ms | Two safe reads at 250 ms and 750 ms; a mutation re-resolves inside its RPC and is reconciled by idempotency key | Five failures in 60 seconds open for 60 seconds; commands return 503 `DEPENDENCY_UNAVAILABLE`, reads return typed unavailable or concealed 404, and no capability is minted |
| ORG-01 | Organization duplicate detector worker | `organizationId`, `normalizedInputHash`, `registryVersion`, `detectorVersion`, `requestId` | `reviewState`, `matchReferenceHashes`, `detectorVersion`, `completedAt` | 2,000 ms pre-commit bound | One bounded in-transaction attempt; timeout commits the organization with a pending review job delivered at 15, 60, and 300 seconds, maximum four deliveries | Five detector failures in 60 seconds open for 60 seconds; review remains pending and manual, with no auto-merge or reattribution; a later successful result is CAS-checked |
| ORG-01 through LIFE-05 | BE00 outbox and relationship projection consumers | `eventId`, `eventType`, `aggregateId`, `aggregateVersion`, `correlationId`, identifier-only payload | `accepted`, `inboxState`, `projectionVersion` | 2,000 ms per dispatch | Three deliveries at 15, 60, and 300 seconds, then DLQ; consumers refetch current rows and acknowledge stale versions | Five dispatch failures in 60 seconds open for 60 seconds; canonical transaction stays committed, projections remain last-known or unavailable, and sweeper reconciliation resumes |

## State Machines

| Aggregate | States and transitions | Invariant |
|---|---|---|
| Organization ownership | unclaimed -> owned or ownerless; owned -> ownerless when current owner authority ends | Ownership is independent of lifecycle; no owner is inferred from provider text |
| Organization lifecycle | active <-> dormant; active/dormant -> closing -> closed; band active/dormant -> dissolving -> dissolved; closed -> active only for ordinary reopen | Dissolved terminal; unresolved disposition is explicit, not deletion |
| Type assignment | temporal active (ends_at null) -> ended (ends_at set) | One active row per organization/type; removal hides surfaces, preserves row |
| Membership | invited -> confirmed/rejected/expired; asserted -> confirmed/rejected/disputed; confirmed -> ended/disputed | Pending/rejected/disputed/expired grants no current authority; immediate revocation is independent of contested historic date |
| Capacity | valid temporal period -> ended by ends_on | Child period contained in tenure and non-overlapping; permanent seeds only exact band default |
| Representation | draft -> pending -> `active|rejected|expired`; active -> `revoked|expired` | Active only after both parties confirm; exact overlap requires explicit dual confirmation; partial overlap warns |
| Mandate | draft -> active -> `revoked|expired` | Active scope/term/ceiling always subset of current source; dependent grants revoke with source |
| Governance terms | draft -> proposed -> `active|rejected|withdrawn`; active -> superseded | Proposed content and member set immutable; one active version |
| Governance confirmation | absent -> `confirm|reject` for one exact terms/member hash | No confirmation from a non-current permanent member or stale terms; activation requires all confirms |
| Name statement | asserted/licensed/disputed -> superseded | Append-only assertion; no legal adjudication or deletion |
| Treasury authorization | requested -> `authorized|refused|expired|revoked` | Implementation policy record only; no funds or transfer state |
| Lineage | predecessor terminal -> formed_successor | Immutable one-way link; successor is a new party |

## Error Handling

### Four-field envelope and safe details

Inherited BE00 ApiError is exactly: code string, message string, requestId UUID and details object. This shard cannot add top-level status/type/title/error fields. INVALID_REQUEST/VALIDATION_FAILED use bounded FieldViolation arrays; CONFLICT may expose authorized expected/current versions and recoveryAction; FORBIDDEN exposes reasonCode only; NOT_FOUND is empty; RATE_LIMITED matches headers; DEPENDENCY_UNAVAILABLE names only dependency class and retryability; INTERNAL_ERROR details is empty.

### Failure and partial-state matrix

| Boundary | Before commit | After commit / ambiguous | Hygiene and surface |
|---|---|---|---|
| Zod/transport | No idempotency, state, audit or outbox row | Not applicable | 400/413/415/422; sanitized security telemetry only |
| Auth/context/RLS | No mutation | Not applicable | 401 or safe 403/404; never use submitted party/role |
| Unique/CAS/state | Transaction rolls back | Same-key replay returns committed result | 409 typed conflict; no lost update or reservation |
| Duplicate detector | Creation may proceed when detector times out | Review job continues from hash | Organization remains canonical; no auto-merge or reattribution |
| Membership/representation/mandate | Invalid/pending source remains unchanged | Revocation commit invalidates projection and events atomically | Past valid facts survive; no authority from stale source |
| Governance activation | Proposal/confirmation unchanged on stale set or hash | Commit fixes active pointer and event | Rejection/prior/default remains authoritative; no partial unanimity |
| Name statement | No statement on invalid fields | Competing valid statements coexist | No platform legal conclusion; prior statements remain |
| Treasury policy boundary | No authorization record if mandate/ceiling/currency/payee fails | Authorized record is replayable only | No balance, provider, split, escrow or tax effect |
| Lifecycle/lineage | No terminal transition if version/vote/disposition invalid | Successor and lineage commit atomically | Dissolution terminal; re-form, not reopen; work retained |
| PostgreSQL/RPC disconnect | Client receives typed dependency failure; transaction outcome unknown | Reconcile idempotency record before retry | Never assert failure/success from transport alone |
| Outbox/Queue | Business commit stands | Lease/sweep retries; duplicate delivery safe | Canonical state remains authoritative; DLQ/manual review visible |
| Consumer crash | No external effect before lease/current-state check | CAS and event identity prevent regression | Retry bounded; stale terminal event is no-op |
| Realtime/cache | No canonical effect | Hint loss/staleness | Authorized refetch; no compensation from hint |
| Restore/recovery import | Protected writes fenced | Restore may precede events/projections | BE00 restoreEpoch blocks consumers/effects until identity reconciliation |

Retry is automatic only for idempotent reads and registered retryable background work. An ambiguous mutation reconciles by idempotency key, resource ETag or job/operation status before retry. No provider integration is owned here; any downstream provider effect remains outside this shard.

## Observability, Rate and Abuse Controls

Every route/consumer emits the BE00 allowlisted NDJSON fields: operation, route template or consumer, request/correlation/causation/trace IDs, actor class, acting-context class, safe entity type/version, outcome, error code, duration, dependency class and retryability. IDs are hashed/omitted unless an approved operational purpose exists. Never log terms JSON, names/evidence content, person identifiers, cookies, JWTs, payee data, raw bodies or capability graphs.

Audit records are append-only for every allowed, denied, completed and failed command, with actor/context, target, reason, source relationship/mandate/version, expected/current version where safe, correlation ID and outcome. Metrics cover request count/duration/error, RLS/RPC conflict, idempotency replay/mismatch, duplicate detector latency/timeouts, authority denial reason, membership/relationship state transitions, governance activation, outbox age, queue age/retry/DLQ, projection lag, treasury refusal and lifecycle terminal transitions. High-risk traces and diagnostic errors are 100%; ordinary success sampling follows BE00.

Rate classes are explicit in the route table. Public reads are 120/min/IP with burst 30/10s; authenticated reads 300/min/user and 600/min/party; ordinary mutations 60/min/user and 120/min/party; high-risk commands 10/min/user and 10/min/party. Treasury view is limited to 120/min/user and 240/min/party. Organization creation applies friction review above 3/24h or 10 lifetime. Limits do not create permanent count-only denial, and 429 includes Retry-After and matching RateLimit headers. Enumeration protection uses concealed 404, bounded collection pages, context-bound cursors and no public membership/mandate listing.

## Test Strategy

### Contract and handler tests

- Route registry, OpenAPI and Hono method/path declarations match all 30 routes above exactly.
- Every route has success tests and every declared 4xx/5xx profile, including four-field ApiError, content/cache/rate/request-ID headers and forbidden-detail absence.
- Every field × constraint row is tested at boundary, malformed, unknown-key, duplicate-array, Unicode, date/time, currency, hash, UUID, safe-integer and collection-limit values.
- Example request bodies parse exactly; actor/party/member IDs in bodies are rejected where server-derived.
- Public ORG-02 cannot leak members, mandates, governance or private statements; authorized reads cannot cross party or context.

### Database, RLS and RPC tests

- Migration tests cover every enum/check/FK, active type partial unique, capacity exclusion/containment, terms one-active unique, confirmation uniqueness, lineage non-branching and terminal-state restrictions.
- Positive and negative RLS tests cover anon, correct human, wrong human, wrong party, forged JWT/body party, pending/revoked/expired mandate, stale terms, operator reason, Queue lease, service-role misuse and over-disclosure.
- RPC tests prove organization mutation, source snapshot, audit, idempotency and outbox are all-or-nothing; audit/outbox failure rolls back canonical state.
- Concurrent type, membership, representation dual confirmation, mandate revoke, governance confirm/activate and lifecycle commands produce one winner with typed version conflict for the loser.

### Authority, state and idempotency tests

- Property tests exercise the exact ten-step resolver: no union of expired/revoked sources; communicate is separate; territory/domain/term/ceiling are conjunctive; null ceiling rejects money; currency mismatch rejects; resource version is rechecked.
- Same-key/same-hash concurrent commands create one effect and exact replay; changed body returns IDEMPOTENCY_MISMATCH; rollback leaves no reservation; post-commit disconnected response replays.
- Membership invitation without current terms cannot authorize; historical assertion grants no authority; contested retroactive date preserves immediate revocation.
- Representation pending/rejected never authorizes; overlap warns; exact overlap requires dual confirmation; revoke emits context invalidation.
- Mandate exceeding source scope/term/ceiling is rejected by database and service; source revocation cascades dependents while historic facts remain.
- Governance member-set drift, stale confirmation, rejection and activation atomicity are covered; defaults remain authoritative until active.
- Treasury tests prove single payee, current mandate, amount/currency ceiling and no payment ledger/effect. Lifecycle tests prove dissolved cannot reopen and re-form creates one successor lineage.

### Event, queue, recovery and downstream tests

- PlatformEvent and QueueEnvelope strict schema, schema-version mismatch to DLQ, ID-only payload, decimal aggregate version, duplicate/out-of-order replay and stale no-regression.
- Lease acquisition/heartbeat/expiry/CAS, retry schedule 15s/60s/300s, max four deliveries, attempt-summary retention and manual DLQ review.
- Duplicate detector hard two-second timeout, creation continuation, no silent merge and detector replay.
- Shard 22 contract test requires active label assignment plus current owner/admin snapshot and separately valid label distribution mandate; label mandate alone fails.
- Shard 30 contract test proves generic negotiate/bind/book/sign/spend/administer fields never create announce_waive or P-07; exact capability remains Shard 30-owned and P-07 debt survives revocation.
- Shard 39 contract test consumes canonical acting context/event IDs and never creates identity from provider text.
- Restore drill proves restoreEpoch fencing, RLS/RPC checks, idempotency/outbox/projection reconciliation and no external effect before reopening.

### Performance, security and accessibility

Protected RPC p95 is <=300ms, Tier 2 response p95 <1,200ms/p99 <2s, authenticated read p95 <750ms, async acceptance p95 <=500ms/p99 <=1s, outbox undispatched p95 <=2s, queue first attempt p95 <=60s and DLQ <0.1% daily. Tests include query plans for partial/exclusion/index paths, abuse burst limits, CSRF/session expiry, cache-control and redaction sentinels. Backend error paths preserve stable JSON Pointer fields, truthful states and recovery actions for WCAG 2.2 AA frontend handoff; loading never appears as an empty roster, balance or authority view.

## Release, Migration and Recovery

Migrations use BE00 expand, backfill, switch, contract ordering. Add registry/enums/tables/indexes and dual-write/backfill before switching readers; never destructively rewrite historical memberships, statements, terms, lineage or audit. Backfills are resumable/idempotent, bounded, observable and hold-aware. Grants/RLS/RPC negative tests are promotion gates.

Supabase Free provides no PITR or production RPO/RTO guarantee, so protected authority, rights, and publication writes remain disabled. Synthetic/local recovery evidence cannot open the gate. If a separately owner-approved recovery capability is later introduced, production-verified artifact/environment provenance and restoreEpoch verification must pass before the established recovery order: database integrity, RLS/RPC negative tests, idempotency/outbox/projection consistency, relationship/terms/member-set reconciliation, public projection checks, protected writes, then async effects. Replayed events re-read current authority and retain original event identity. Missing runbook/config/registry evidence disables the dependent capability; it does not create a fallback authority.

## Deepening and Ambiguity Gate

| Pass | Focus | Result | Outcome |
|---|---|---|---|
| 1 | Cross-endpoint consistency | All 30 routes map to IDA-06–IDA-14, schema aliases, errors, auth, rate, SLO and audit behavior | PASS |
| 2 | Sequencing/concurrency | Every mutable command uses strong ETag/CAS; idempotency, authority recheck, audit and outbox share one RPC; governance activation is one lock boundary | PASS |
| 3 | Failure cascades | Duplicate timeout, stale relationships, outbox loss, Queue duplicates, restore and telemetry failures have explicit no-partial-state outcomes | PASS |
| 4 | Authorization | Principal × route classes include allow predicate, 403/404 disclosure, step-up/reason, RLS and no client authority | PASS |
| 5 | Observability | HTTP/RPC/authority/consumer/outbox/projection/lifecycle metrics and safe audit/log/span fields are declared | PASS |
| 6 | Rate and abuse | Public/auth/high-risk limits, friction thresholds, enumeration resistance, cursor binding and mass-assignment rejection are explicit | PASS |
| 7 | Partial-state hygiene | Each canonical mutation, support record, outbox event and projection failure has rollback/retry/reconcile/manual-review behavior | PASS |
| 8 | Source contradiction resolution | Deep-dive fields and IA interaction states win; support records are implementation records; Shard 30 announce_waive/P-07 stays outside generic authority | PASS |
| 9 | Two-implementer convergence | Routes, Zod fields, transitions, indexes, RLS predicates, event payloads, queue leases and tests yield one implementation choice | PASS |
| 10 | Final adversarial scan | Hostile principal, stale source, exact currency, overlap, terminal lifecycle, Queue replay and downstream ownership scan found no unresolved contract | PASS |

### Quality-gate checklist

- Classification, split group and referenced inventory are complete.
- Endpoint reconciliation includes every owned interaction and no dangling endpoint.
- Every route has request fields/examples, success response, error profile, field constraints, auth predicate, idempotency/CAS, rate/deadline/SLO and observability.
- Organization, type, tenure, capacity, representation, mandate, authority, governance, confirmation, name, treasury, lifecycle and lineage schemas are explicit.
- Required partial unique, exclusion, FK, temporal and immutable constraints are explicit.
- Non-exposed schema, grants, forced RLS, security-invoker views/RPCs and principal predicates are explicit.
- Exact ten-step authority resolution is normative and rejects generic announce_waive inference.
- State machines, transaction/outbox/idempotency/CAS boundaries, event payloads, leases, retries and DLQ are explicit.
- Partial-state, recovery fence, migration, observability, abuse and accessibility tests are explicit.
- Shards 22, 30 and 39 consumers are cross-referenced with ownership preserved.
- No auth provider mechanics, personal aliases/legal identity, identifiers/legacy, estate or memorial records are owned.
- No unresolved product or architecture question remains; implementation records are labeled and bounded.

## Ambiguity Gate

**PASS.** Implementer simulation and devil's-advocate review covered ORG-01, ORG-02, TYPE-01/02, MEM-01 through MEM-06, REP-01 through REP-04, MAN-01/02, AUTH-01, GOV-01 through GOV-04, NAME-01/02, TRE-01/02, and LIFE-01 through LIFE-05, strict request and success schemas, per-operation middleware and CORS, BE00 ApiError envelopes, authorization concealment, idempotency and concurrency, typed persistence, state recovery, integration boundaries, observability, tests, and source reconciliation. No unresolved implementation ambiguity remains; open questions are none.

## Open Questions

None. The only implementation-level choices resolved here are schema placement in non-exposed identity_private, half-open temporal ranges for capacity, explicit support-record names, and route operation IDs. These choices do not change IA product scope. Provider integrations, label distribution mandates, booking waivers and analytics bindings remain consumer-owned dependency gates.

## Changelog

| Date | Change | Workflow | Sections affected |
|---|---|---|---|
| 2026-08-28 | Reconciled approved Shard 01 split; corrected downstream IA paths and authored complete organization, relationship, authority, governance, name, treasury and lifecycle BE contract | /write-be-spec | All |
| 2026-08-28 | Added exact ten-step resolution, 30-route registry, Zod examples/error matrix, canonical schema/index/RLS design, event/queue contract, failure hygiene, tests and passes 1–10 | /write-be-spec-write | API, schema, policy, flow, errors, tests, gates |


<!-- spec-graph: auto-generated -->
## Related Specs

### Derives from
- [[specs/ia/01-identity-authority|Shard 01 — Identity authority and party governance]]

### References
- [[specs/ia/01-identity-authority|Shard 01 — Identity authority and party governance]]
- [[specs/ia/deep-dives/01-identity-authority|Deep Dive 01 — Identity authority and party governance]]
- [[specs/be/00-infrastructure|Cross-cutting Platform Foundation — Backend Specification]]
- [[specs/be/01a-auth-account-linking|Authentication & Account Linking — Backend Specification]]
- [[specs/be/01b-party-identity-aliases|Shard 01b — Party Identity & Aliases — Backend Specification]]
- [[specs/be/01d-identifiers-legacy|Identifiers & Legacy — Backend Specification]]
- [[specs/ia/22-release-distribution|Shard 22 — Release and distribution lifecycle]]
- [[specs/ia/30-booking-contracts|Shard 30 — Booking, negotiation and contracts]]
- [[specs/ia/39-analytics-ingestion-reporting|Shard 39 — Analytics ingestion, matching and reporting]]
