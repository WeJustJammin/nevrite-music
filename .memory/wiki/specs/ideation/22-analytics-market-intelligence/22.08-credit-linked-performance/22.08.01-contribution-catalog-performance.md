# Feature: Contribution Catalog Performance

> **Number**: 22.08.01
> **Parent**: [Credit-Linked Performance](./22.08-credit-linked-performance-index.md)
> **Status**: [SURFACE]
> **Last updated**: 2026-07-16
> **Origin**: **Deep Think addition** — not in the sweep's candidate list.

## Overview

The records you played on, and how they're doing. For a session musician this is the first time their
work has ever existed as a **catalog** rather than as a scattered memory — forty records across a
decade, most of which they have no idea about since the week they tracked them. It is the direct
payoff of provenance: the credits captured at the session (domain 02) become a portfolio that
accumulates, which is precisely D-18's "the reason they cannot leave".

## Role Lens

| Role | Access | What They See/Do |
|------|--------|-------------------|
| Musician | Full | Every record they hold a counter-attested credit on, with that record's performance, on one timeline. Sorts by recency, momentum, size. Sees their catalog **grow as a body of work** rather than as a list of forgotten sessions. This is the persona the feature exists for and the only tool in the world that could show them this. |
| Producer | Full | Same, for records they produced, mixed or engineered. Their catalog is usually larger and older than a session player's, which makes the rollup more valuable and the coverage problem worse — a producer's 200-record back catalog spans distributors that no longer exist. |
| Operator | None | Not rendered. No credits, no catalog. |
| Fan | None | Never rendered here. A fan may see a *proof* on a public listing (22.08.03) — a total, never the catalog, because the catalog reveals relationships that are sometimes confidential. |

## Behavior

The rollup joins the counter-attested credit graph (domain 02) to matched catalog performance
(22.02.02), producing: the set of recordings you are credited on, each with its performance series,
and aggregate views over the set.

Two rules define it:

- **Streams are never divided** (index D-01). A drummer on a 4M-stream record played on a 4M-stream
  record. Dividing by split would conflate performance with money — domain 10's question, not this
  one — and would produce the absurdity of a drummer "having" 400k streams.
- **Coverage honesty is structural** (index D-03). This rollup spans releases the user does not
  control, assembled from sources with independent failure states. It is patchy **by construction**,
  and the patchiness must degrade the claim rather than hide behind a clean total.

### Happy Path

1. User opens "My Work".
2. System resolves their counter-attested credits (domain 02) to matched recordings (22.02.02).
3. Catalog renders: each record, its performance, its trajectory — with per-record coverage stated.
4. Aggregate view: the body of work, over time, with an explicit coverage figure.
5. User drills into any record; sees who else was on it (subject to domain 02's visibility rules).

### Edge Cases / Failure Modes

| Scenario | What Happens | User Sees |
|----------|-------------|-----------|
| Credited on a record we have no data for | The dominant case, not an edge case | Record listed, performance unknown — **not omitted**. Omitting it would silently shrink their career |
| Credit is self-claimed, not counter-attested | Included in the private catalog, excluded from proof | Distinguished visibly; index D-02 |
| Record has 6 contributors | All six see the full numbers | No division; index D-01 |
| Coverage is 30% of their credits | The aggregate is meaningless as a total | Coverage stated **before** the number: "we have data for 12 of your 41 credited records" |
| A credit is revoked/disputed | Rollup shrinks; any published proof invalidates | Cascade per `22.08-credit-linked-performance-cx.md#CX-01` Q2 |
| Ghost production / NDA'd work | The credit may exist privately but not publicly | Domain 02's visibility model governs; the catalog respects it |
| Record performs badly | The honest answer is unflattering | Neutral framing; pull-only (22.05.02 D-04) |
| Old catalog, distributor defunct | No data obtainable at any price | Listed, unknown — permanently. An honest gap that will never close |

### States

| State | Trigger | What User Sees |
|-------|---------|---------------|
| Empty | No credits in domain 02 | Routes to credits — the honest message is that the catalog is downstream of capture |
| Populated | Credits + data | Catalog with per-record coverage |
| Partial | Some records lack data | The normal state. Coverage figure stated prominently, not buried |
| Unknown | Credits exist, no data at all | Catalog as a list with no numbers — still valuable; it is their work |
| Error | Rollup failed | Retry |

## Deep Think Annotations

| # | Hypothesis | Source | Outcome |
|---|-----------|--------|---------|
| DT-01 | This is the domain's only **uncopyable** node, and its reason to exist | Reasoning from D-18 against the incumbent set | ✅ CONFIRMED. Every incumbent — Chartmetric, Soundcharts, Viberate, Songstats — models **one artist entity** and ingests the same purchasable DSP data. None can answer "how are the records I played on doing?", because none holds a counter-attested credit graph; and they cannot retroactively manufacture one, because it only exists if you were **in the room** when the credit was captured (D-18). Jaxsta and Muso.AI hold credits but not the session, and no daily hook — the failure mode problem-statement names explicitly. This feature is the intersection those two failures leave empty, and it is the reason domain 22 is more than a Chartmetric clone. Everything else here is table stakes; this is the wedge. |
| DT-02 | A domain expert would expect **coverage, not accuracy, to be the binding constraint** — and nobody said so | Reasoning about where the rollup's data actually comes from | ✅ CONFIRMED, and it is the feature's central risk. The rollup spans records **the user does not own**: a session drummer cannot connect a DSP account for someone else's release. So coverage depends entirely on (a) 22.02.02 matching via the credit graph rather than via a claimed artist profile (22.02.02 D-04), and (b) obtaining performance data for releases we hold no grant on — which may simply be unobtainable (22.01 Q-01, Q-03). **The honest expectation is that coverage is low and permanently incomplete**, especially for older catalog. That is survivable *if* the feature is honest about it — a list of 41 records with data for 12 is still the only such list in existence, and still valuable. It is fatal if presented as a total. Hence index D-03 and the "listed, unknown — not omitted" rule. |
| DT-03 | What breaks at the intersection with domain 02 and 05? | Cross-domain failure reasoning | ✅ CONFIRMED — a **circular cold-start**, and it is the platform's hardest sequencing problem, not just this feature's. The chain: this feature needs counter-attested credits at volume → credits are captured because Producers run sessions on WeJammin (personas.md: the Producer is "the capture point") → Producers come because the platform is where the work happens → the work is there because consolidation drew people in → and one of the strongest reasons to bother capturing a credit is *this feature*, which shows the credit paying off. So the payoff that motivates capture requires the capture to already exist. This is exactly D-18's claim ("consolidation is the precondition for provenance") arriving as a concrete ordering constraint: **22.08 cannot be the beachhead alone — it must ride on a consolidation surface that brings people in for another reason.** That is a real answer to problem-statement Q-03 and it comes from this feature, which the sweep never proposed. |

## Cross-Cut Notes

- Touches **22.08.02 / 22.08.03** — why: the slice is a lens on this rollup; the proof asserts it. See `22.08-credit-linked-performance-cx.md#CX-01`, `#CX-02`.
- Touches **02 Credits & Attribution** ([02](../../02-credits-attribution/)) — why: the credit graph is the subject, the permission model, and the cold-start dependency. The tightest coupling in domain 22.
- Touches **22.02.02 Catalog Matching** ([22.02.02](../22.02-external-identity-catalog-matching/22.02.02-recording-release-catalog-matching.md)) — why: DT-02. Credit-graph-native matching is what makes coverage possible at all (22.02.02 D-04).
- Touches **22.01.03 Ingestion Health** ([22.01.03](../22.01-source-connections-ingestion/22.01.03-ingestion-health-gaps-freshness.md)) — why: 22.01.03 DT-02 — a gap here silently shrinks a career and then gets exported as proof.
- Touches **10 Royalties & Collections** ([10](../../10-royalties-collections/)) — why: index D-01's boundary. Performance is not money; 10 owns the money.
- Touches **05 Services Marketplace** ([05](../../05-services-marketplace/)) — why: DT-03. The rollup becomes a sales asset via 22.08.03.
- Touches **23 Career, Finance & Business** ([23](../../23-career-finance-business/)) — why: this is the career-portfolio view; 23 owns the income half of the same picture.

## Decisions

| # | Decision | Context |
|---|----------|---------|
| D-01 | Records with **no data are listed, never omitted** | Omission silently shrinks a career. An unknown is honest; an absence is a lie of convenience — and this is the number they will quote to a client. |
| D-02 | Coverage is stated **before** the aggregate | DT-02. A total computed over 30% coverage is not a total, and the user must know that before they read the number, not after. |
| D-03 | Streams are **never divided** by split | Index D-01. Performance ≠ money; conflating them makes both wrong. |
| D-04 | Self-claimed credits are in the private catalog, **never** in proof | Index D-02. The badge's value is that it cannot be faked. |

## Open Questions

| # | Question | Owner | Deferred To |
|---|----------|-------|-------------|
| Q-01 | Can we obtain performance data for releases we hold **no grant** on? If not, coverage collapses to records the user controls — and the feature fails the exact persona it exists for. (Index Q-01 — the single biggest risk in domain 22.) | User | `/create-prd-stack` |
| Q-02 | The cold-start is circular (DT-03). Which consolidation surface carries the user in until credits accumulate? This is domain 22's contribution to problem-statement Q-03. | User | MoSCoW / `/ideate-validate` |
| Q-03 | Does this belong in domain 02 rather than 22? (Index Q-02.) | User | `/ideate-validate` |
