# Feature: Source Declaration — Samples, Interpolations & AI Content

> **Number**: 07.08.04
> **Parent**: [Delivery, Readiness & QC](./07.08-delivery-readiness-qc-index.md)
> **Status**: [PARTIAL]
> **Last updated**: 2026-07-17

> **Deep Think addition** — not in the sweep's candidate list (sub-domain "Deep Think Additions"). It is a named
> harm in `meta/personas.md`: *"uploads a stem containing an uncleared sample and **exposes every collaborator
> downstream**."* A specific harm, with a specific blast radius, and 35 candidates covered none of it.

## Overview

Declaring what in this song did not originate with the people making it: a sample of a recording, an
interpolation of a composition, a library loop, a preset, an AI-generated stem. It is the provenance thesis
pointed **backwards** — the domain's other features capture who *made* this; this one captures where the parts
*came from*. Both are facts that are trivially knowable at the moment of creation and effectively unknowable
three years later, which is exactly when they become expensive.

## Role Lens

| Role | Access | What They See/Do |
|------|--------|-------------------|
| Musician | Full | Declares sources on their own contributions. As a **writer**, declares interpolations (lyrics or melodies quoting another work). Sees the song's full source picture — including, crucially, what *other people* brought in, which is the thing that can hurt them and that they currently have no way to learn. |
| Producer | Full | Usually the one who **actually knows** — they pulled the loop from Splice, they ran the AI stem separator, they know the drums came from a library. Their declaration is the highest-quality one available. |
| Operator | None | Not their business. |
| Fan | None | Not visible here — but a Fan is the **downstream victim** of a failure via `07.08.05` (parent CX-05). |

## Behavior

A declaration attaches to an **asset or a section**, not to the song as a whole — "the drums in the second verse
are from a library" is the actual fact, and a song-level flag cannot express it or clear it.

Types differ legally and cannot be collapsed:

| Type | What it is | Clears through |
|---|---|---|
| **Sample** | A piece of a *recording* | Both master and publishing — two rights holders, two negotiations |
| **Interpolation** | A re-performed *composition* (melody or lyric quote) | Publishing only |
| **Library / preset** | Licensed material | Its own licence terms — usually pre-cleared, sometimes not |
| **AI-generated** | Machine-produced audio | Not a clearance question at all — a *copyrightability* and *disclosure* question |

The platform's job is **capture and propagation**, not clearance. Clearance is domains 09 and 11 (sub-domain
Q-02). What 07 owns is being present at the moment the fact is knowable and recording it — and then making sure
it travels to everyone it can harm.

That propagation is the point: `meta/personas.md`'s harm is that the sample exposes **every collaborator
downstream**, none of whom did anything wrong and none of whom can discover the problem. A declaration is how they
find out while it is still cheap.

> **Ratified 2026-07-22 (D-09).** D-02's moment-of-use prompt is delivered by the DAW bridge, and **no non-web
> client is authorised** (`07.09` D-05), so it does not fire in the v1 window. Declaration in v1 is **voluntary
> and unprompted** — a producer or contributor declares against a region from the web app, and nothing asks
> them to. The "nobody declares anything" row below is therefore the realistic v1 default, and D-05's
> `sources not reviewed` is what a v1 song shows by default. The feature is not re-scoped (domain D-10), and
> D-05/D-07's honesty machinery is what keeps the gap visible rather than reading as a clean bill.

### Happy Path

> **Post-gate happy path — not v1 (D-09).** Step 2 is the bridge prompt, which `07.09` D-05 defers. The v1 shape
> is: the producer declares the loop by hand whenever they think of it, or — the realistic case — nobody does,
> and the region stays `sources not reviewed` until readiness (`07.08.03`) surfaces it at the exit.

1. Producer drops a library loop into the second verse
2. The DAW bridge (`07.09.02`) sees an audio file whose origin is a known sample library and asks — **at that
   moment**, not at delivery — "this came from Splice. Declare it?"
3. One tap; declaration attached to the region, with the library and licence terms recorded
4. It flows into readiness (parent CX-02), the handoff package (`07.08.01`), and the roster's view — everyone on
   the song can now see what is in it
5. At sync pitch time (domain 11), the clearance status is already known rather than reconstructed

### Edge Cases / Failure Modes

| Scenario | What Happens | User Sees |
|----------|-------------|-----------|
| Undeclared sample discovered after release | The harm the feature exists to prevent. Routes to 09/11 for retroactive clearance; every downstream artifact is affected | Prominent; the roster is notified — they are exposed and did not know |
| Contributor declares a sample **the others did not know about** | **The whole value.** They find out now, cheaply, instead of via a claim | Roster notified: "Alex declared a sample in the second verse" |
| AI-generated stem | Not a clearance question. A copyrightability question (in the US, purely AI-generated material is not copyrightable), a DSP-policy question, and a credit question | Declared as AI with the tool recorded — see DT-03 |
| Sample is from the artist's **own** earlier work | Still a declaration; still needs the split from that earlier work applied. Self-sampling is not free | Links to the source Song if it is on the platform |
| Producer does not know what the previous engineer used | Genuinely unknowable. Declarable as "unknown origin" — an honest state that is far better than silence | "Origin unknown" — flagged as a risk, not a failure |
| Nobody declares anything | The realistic default without a prompt at the right moment (DT-02). This is why the DAW bridge matters here — and **it is the v1 default**, because no client is authorised to fire that prompt (D-09) | Readiness gap at release; regions read `sources not reviewed`, never `none` (D-05) |
| Contributor tries to declare a source on **another person's** contribution | Allowed but attributed — a Producer who knows the drummer used a library loop can declare it, but the declaration records **who declared it** (D-07), and the owning contributor is notified so they can confirm or correct. A declaration about your work you didn't make is a fact you must be able to see. | "Sam declared a sample on your bassline — confirm or dispute" |
| Two contributors declare the **same source on the same region** concurrently | Deduplicated on (region, source, type); the second declaration merges rather than creating a duplicate. Conflicting *types* on the same region (one says sample, one says interpolation) are **not** auto-merged — both are kept and surfaced as a conflict, because the legal difference is real (D-01, Behavior table) | "Two source types declared for verse 2 — resolve" |
| Contributor **removes** a declaration to hide an exposure | Removal is never silent (per DT-04's honest-degradation principle, now D-07): the region returns to **`sources not reviewed`**, not to a clean state, the removal is audit-logged with actor and timestamp, and the roster is notified. You cannot un-declare your way to a false clean. | Roster notified: "Alex removed a sample declaration from verse 2"; region flagged not-reviewed |
| A "preset" that is a full melodic loop | **P-06 (D-08):** the declaration stands as made — the platform never reclassifies it. The line between preset and sample is genuinely blurry and the user will not know it either, so the capture prompt asks enumeratively (naming loop / one-shot / drum hit / vocal / break / stem / bought beat per `11.05.01` D-05) rather than offering a bare "preset" that quietly absorbs a loop. If anyone later believes the type is wrong, that is the ordinary attributed type-conflict above: both types are kept and surfaced, never auto-merged and never auto-resolved. No detector, no reclassification, no rejection. | Enumerated choices at declaration; if disputed, "Two source types declared for this region — resolve", rendered distinctly from both `sources declared: none` and `sources not reviewed` |

### States

| State | Trigger | What User Sees |
|-------|---------|---------------|
| Loading | Fetching declarations for the song/region | Skeleton rows on the source list; the readiness contribution shows as pending, never as "none" (that ambiguity must not appear mid-load, per DT-04) |
| Not reviewed | Nobody has actively confirmed the sources for this region | **`sources not reviewed`** — the honest default, visibly distinct from an active "none" (D-05); counts as an open readiness gap |
| None | Someone actively confirmed there are no external sources | "Sources declared: none" — an affirmative statement, never rendered like Not-reviewed (D-05, DT-04) |
| Declared | Sources recorded | List by asset/section, with type and status |
| Unknown origin | Declared as unknown | Honest risk flag |
| Cleared | Clearance recorded (via 09/11) | Cleared badge |
| Uncleared | Declared, not cleared | Readiness gap, weighted heavily (parent CX-02) |
| Error | Fetch failed | Error with retry |

## Deep Think Annotations

| # | Hypothesis | Source | Outcome |
|---|-----------|--------|---------|
| DT-01 | This is the **provenance thesis pointed backwards**, and its absence from 35 candidates is a real gap | `meta/personas.md` names it as the Musician's worst accidental behavior with a named blast radius: "exposes **every collaborator downstream**." `meta/problem-statement.md`'s logic applies exactly — the fact (this loop came from Splice on 14 Nov) is free to record now and effectively unrecoverable in three years, when a claim arrives and nobody remembers where the drums came from. The sweep found 35 candidates about capturing who *made* the song and none about where the *parts came from*. | ✅ CONFIRMED — the same argument that justifies the entire domain justifies this feature. It is arguably the **second-most-thesis-aligned feature in domain 07** after `07.06.02`, and it was invisible to a 14-lens sweep. |
| DT-02 | Declaration must be prompted **at the moment of use**, not at delivery | Direct application of `07.06.02` DT-01's logic. At delivery, "did you use any samples?" is a blank question about work done weeks ago by possibly someone else — it will be answered "no" by default, honestly and wrongly. At the moment the loop lands in the session, the bridge can see a file that came from a known library path and ask a *confirmable* question. **Same principle, same dependency, same failure without it.** | ✅ CONFIRMED — this feature is **structurally dependent on `07.09` (the DAW bridge)**, exactly like `07.04.01` and `07.06.02`. Without it, this is a delivery-time form that gets a false "no". Three features in this domain now share that dependency, which is itself the strongest available argument for the bridge's MoSCoW placement. |
| DT-03 | AI declaration is the same mechanism as sample declaration | The merge that created this feature (samples + AI in one node) | ⚠️ **PARTIALLY REJECTED — the mechanism is shared, the semantics are not, and conflating them would be wrong in a way that matters.** A sample is a **clearance** question: someone owns it, you negotiate, you pay. AI-generated audio is a **copyrightability and disclosure** question: in the US, purely AI-generated material is not copyrightable, so an AI stem may mean part of the record *nobody owns* — which is not a clearance problem, it is a rights-model problem, and it propagates into domain 09's splits in a way no sample does. Plus DSP policies on AI content are evolving and inconsistent, and the disclosure obligation is regulatory rather than contractual. **Shared capture surface, divergent downstream handling.** They stay in one feature because the *capture moment* is identical (declare where this came from) and because forcing users to find two different forms guarantees neither gets used — but the model must not treat AI as a sample with a different label. |
| DT-04 | Declaring nothing means the song is clean | The natural reading of an empty declaration list, and the one a sync agent or a label will make | ❌ **REJECTED — and this is the feature's sharpest trap.** An empty list is ambiguous between "we checked and there is nothing" and "nobody ever thought about it", and those are opposite facts with opposite risk. Worse: a sync agent seeing "no samples declared" will read it as a **clearance assurance the platform never made**, which turns a UI ambiguity into a commercial representation. The fix is to make the distinction explicit — a song is either **`sources declared: none`** (someone actively confirmed) or **`sources not reviewed`** (nobody looked), and those must never render alike. This mirrors `07.04.05` D-05 and `07.06.03` D-01: **honest degradation over false assurance**, now the fourth appearance of that pattern in this domain and unambiguously a global principle rather than a local choice. |

## Cross-Cut Notes

- Touches **09 Rights & Ownership** ([../../09-rights-ownership/](../../09-rights-ownership/)) — declarations feed clearance and split obligations; per DT-03, an AI stem is a *rights-model* problem that lands here
- Touches **11 Music Licensing** ([../../11-music-licensing/](../../11-music-licensing/)) — sample clearance is theirs; sub-domain Q-02's boundary (07 captures, 09/11 clear)
- Touches **07.09 DAW Bridge** ([../07.09-daw-bridge-capture-at-source/](../07.09-daw-bridge-capture-at-source/)) — per DT-02 this is a **hard structural dependency**; the third feature in the domain with one. **v1 state (D-09, 2026-07-22)**: the dependency is unmet — no non-web client is authorised (`07.09` D-05), so the moment-of-use prompt does not exist and declaration is voluntary. `07.09` keeps `Should`, unphased (domain D-10); parked on evidence, not severed, and this feature is not re-scoped
- Touches **07.08.03 Readiness Score** ([./07.08.03-metadata-completeness-readiness-score.md](./07.08.03-metadata-completeness-readiness-score.md)) — the heaviest-weighted gap; see parent CX-02
- Touches **07.08.05 Remix Programs** ([./07.08.05-remix-stems-delivery-programs.md](./07.08.05-remix-stems-delivery-programs.md)) — parent CX-05, the highest-consequence edge in the sub-domain
- Touches **07.02.02 Lyric Workspace** ([../07.02-songwriting-composition-workspace/07.02.02-lyric-workspace-per-line-attribution.md](../07.02-songwriting-composition-workspace/07.02.02-lyric-workspace-per-line-attribution.md)) — lyrical interpolations are declared here
- Touches **14 Digital Goods & Plugin Marketplace** ([../../14-digital-goods-marketplace/](../../14-digital-goods-marketplace/)) — sample libraries sold there could be **pre-declared automatically** when used, which is a genuinely elegant loop: buy the pack here, use it here, declaration is free
- Touches **24 Trust, Safety & Disputes** ([../../24-trust-safety-disputes/](../../24-trust-safety-disputes/)) — infringement claims

## Decisions

| # | Decision | Context |
|---|----------|---------|
| D-01 | Declarations attach to an **asset or section**, not to the song | "The drums in verse 2" is the actual fact; a song-level flag can neither express nor clear it. |
| D-02 | Prompted **at the moment of use** via the DAW bridge, not at delivery | Per DT-02 — a delivery-time question gets an honest, wrong "no". ⚠️ **Held, not overturned, for the v1 window — see D-09** (2026-07-22): the reasoning is unchallenged, but the surface that delivers the prompt is unauthorised, so in v1 there is no prompt at any moment and declaration is voluntary. |
| D-03 | 07 owns **capture and propagation**; 09/11 own **clearance** | Sub-domain Q-02 — the natural boundary of the thesis: capture at source, clear elsewhere. |
| D-04 | AI content shares the capture surface but **not** the downstream model | Per DT-03 — a clearance question and a copyrightability question are not the same thing. |
| D-05 | **`sources declared: none`** and **`sources not reviewed`** are distinct states that never render alike | Per DT-04 — an empty list read as clearance assurance is a representation the platform never made. |
| D-06 | "Unknown origin" is a **first-class, honest** state | Genuinely unknowable is common (a previous engineer's work) and far better recorded than silently absent. |
| D-08 | **P-06:** a mis-typed declaration (a "preset" that is really a full melodic loop) is **flagged by a human, never reclassified by the platform** | Per D-03 — 07 owns capture, 09/11 own clearance — so 07 cannot mint a clearance class or route a clearance review. The type axes belong elsewhere (`14.04.02` DT-01: "a preset makes no sound. It is a settings file"; `14.04.01` DT-07 on melodic loops as compositions), and per P-04 D-06 / P-05 D-07 this feature references rather than restates them. Reclassifying would also require the detector Q-04 and `05.06.04` DT-02 reject — "the honest posture is declaration, not detection" — and would breach domain D-05 ("measure and show; never judge"). Rejection was foreclosed separately: this is a capture prompt (D-02), and domain D-04 names "the capture prompt never blocks" as an enumerated instance. Mitigation is upstream: ask enumeratively so the ambiguity surfaces at declaration time. |
| D-07 | Declarations record **who declared** and are **append-only with an audit trail**; a removal reverts the region to `sources not reviewed`, is logged, and notifies the roster | Anyone can declare on anyone's contribution (the Producer often knows best), but attribution keeps that honest and lets the owner confirm/dispute; silent removal would recreate exactly the invisible-exposure harm the feature exists to prevent — the same honest-degradation principle as D-05/DT-04. |
| D-09 | **D-02's moment-of-use prompt does not fire in v1; declaration is voluntary and unprompted.** No non-web client is authorised to watch a session (`07.09` D-05), so the bridge cannot see a library path or ask at the moment of use | Owner ratification DQ-08.2 / DQ-08.4, 2026-07-22. DT-02 is held, not overturned — a delivery-time question still gets an honest, wrong "no", which is why the answer is *not* to fall back to a delivery-time form. What v1 has instead is a voluntary declaration surface plus D-05's two-state honesty: an undeclared region reads `sources not reviewed`, never `none`, so the gap stays visible to the roster and to `07.08.03`'s readiness score rather than reading as a clean bill. Domain D-06 makes that wording mandatory: "no samples declared" presented to a sync agent as assurance is the representation Q-01 already flags. `07.09` keeps `Should`, unphased (domain D-10); D-02 returns in full if that gate reopens. |

## Open Questions

| # | Question | Owner | Deferred To |
|---|----------|-------|-------------|
| Q-01 | Per DT-04 — does a declaration create **liability or assurance** for the platform? Displaying "sources declared: none" to a sync agent edges toward a representation. This may be the most legally-exposed surface in domain 07 and it is one I added. | User | `/ideate-validate` (compliance) → `/create-prd-security` |
| Q-02 | Per DT-03 — what exactly does an AI declaration record: the tool, the prompt, the degree (fully generated vs AI-assisted vs stem-separated)? The line between "used an AI de-noiser" and "the melody is AI" is enormous and users will not draw it consistently. | User | `/ideate-discover` Step 5 |
| Q-03 | Per the 14 cross-cut — can library purchases on WeJammin **auto-declare** on use? Elegant, and it only covers material bought here, which is a small fraction. | Agent | `/ideate-discover` Step 5 |
| Q-04 | Should the platform attempt **automated** sample detection (audio fingerprinting)? It would catch what declaration misses — and it makes the platform a detective against its own users, with false positives that accuse people of infringement. Strongly inclined to reject; recording as a question because the harm it prevents is real. | User | MoSCoW |


<!-- spec-graph: auto-generated -->
## Related Specs

### Constrained by
- [[decisions.md#d-07|D-07]]
- [[decisions.md#d-01|D-01]]
- [[decisions.md#d-08|D-08]]
- [[decisions.md#d-05|D-05]]
- [[decisions.md#d-02|D-02]]
- [[decisions.md#d-03|D-03]]
- [[decisions.md#d-04|D-04]]
- [[decisions.md#d-06|D-06]]
