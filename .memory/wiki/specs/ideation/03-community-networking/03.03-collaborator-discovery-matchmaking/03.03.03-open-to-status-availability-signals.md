# Feature: Open-To Status & Availability Signals

> **Number**: 03.03.03
> **Parent**: [Collaborator Discovery & Matchmaking](./03.03-collaborator-discovery-matchmaking-index.md)
> **Status**: [PARTIAL]
> **Last updated**: 2026-07-17

## Overview

The supply side's declaration of **appetite** — what I want to do next, which is routinely different from
what my credits prove I have done. The session drummer who wants to co-write, the mixer who is done taking
mix work, the band open to support slots but not to festivals. Credits (domain 02) are the capability axis;
this is the intent axis, and the matcher (03.03.02) needs both. It is also the one input on this platform
that **decays silently** — nothing in the world falsifies a stale "open to work", which makes decay
handling the feature's central problem rather than a detail.

## Role Lens

| Role | Access | What They See/Do |
|------|--------|-------------------|
| Musician | Full | Declares appetite per role — and per personas.md this is where a single availability boolean fails hardest: their roles are *simultaneous*, so "open to session work, open to co-writes, not open to unpaid, not taking new bands" are four concurrent truths. Any UX forcing one status onto the multi-hyphenate reproduces the "are you an artist OR a producer?" failure personas.md names explicitly. |
| Producer | Full | Declares session-owner appetite ("taking mix work from October"). Per personas.md they will not leave the DAW to maintain a web form — so if keeping this true requires discipline, it will be false within weeks. DT-01 is therefore not an optimisation but the difference between the signal working and not. |
| Operator | Config | Config-only, and deliberately narrow: an Operator's real availability is a **calendar with hard resource constraints** (domain 16/17) — a room is booked or it is not. Per personas.md their inventory is "time and space… perishable and physically constrained", which is a different kind of fact from "I fancy a co-write". Conflating them would let a matcher treat a booked Tuesday as a mood. What they may declare here is appetite for *kinds* of work (open to hosting rehearsals, not to club nights). |
| Fan | None | No access (parent D-05). Per personas.md the Fan "will never file a split sheet" — they have no appetite to declare in a collaborator market. |

> **Access levels**: Full · Config · Read-only · Reports · None

## Behavior

A signal is a tuple of (role, appetite, scope, freshness), declared per-role and opt-in (D-02, D-03).

- **Role** — which of the user's simultaneous roles this appetite applies to (session drummer, mixer,
  co-writer). A user carries several signals concurrently; they are never collapsed into one status.
- **Appetite** — the declared intent for that role: what they want *next*, distinct from what their
  credits (domain 02) prove they have done. Capability is the credit axis; appetite is the intent axis;
  the matcher (03.03.02) scores against both.
- **Scope** — the qualifiers that decide whether a match is worth anyone's time: remote or in-the-room
  (per D-15 a physics question, not a preference — the ~25–30 ms desync ceiling caps a playable radius),
  paid or split-based, genre, commitment level. The "not open to unpaid" / "split-based only" collision
  is the most common real mismatch in music collaboration, and the market should surface it before
  contact rather than after a wasted exchange (`03.03-collaborator-discovery-matchmaking-cx.md#CX-02`).
- **Freshness** — a horizon after which the signal lapses unless refreshed. This is the feature's central
  problem, not a detail: per DT-01 an expiring signal is the only honest design, because appetite is the
  one input on this platform that nothing in the world falsifies.

A live signal is a scoring input to the matcher (03.03.02) and is matched against open collaboration calls
(03.03.04). It is opt-in: absence of a signal means "not matchable on appetite" (D-02), never a silent
default of "open".

> The exact appetite *vocabulary* (the enumerated set of declarable intents) is
> `[PENDING — Step 5 Must-depth or /write-be-spec]`, pending the taxonomy owner (parent Q-03). This is
> long-tail detail; the signal's structure, lifecycle, and behaviour above are complete.

### Happy Path

1. User declares an appetite for a role, with scope qualifiers
2. System records it with a freshness horizon
3. The signal becomes a scoring input (03.03.02) and is matched against open calls (03.03.04)
4. As the horizon approaches, the user is prompted to confirm or let it lapse — one tap, in a surface they already open

### Edge Cases / Failure Modes

> This is a Should at Level-1 depth: the PRIMARY failure modes are covered; deep long-tail edge detail is
> deferred to Must-depth or `/write-be-spec`.

| Scenario | What Happens | User Sees |
|----------|-------------|-----------|
| **Signal goes stale** — set 8 months ago, year now full (the ghost problem, the primary failure mode) | Horizon lapses; scoring stops reading the signal (D-01). Without expiry every match containing them would be a false positive, and two dead leads teach the demand side the market is dead | Before lapse: "Your 'open to session work' signal expires soon — still true? [Confirm] [Let it lapse]". After lapse: signal shown greyed as *Lapsed*, with one-tap re-activate |
| **Fan or no-appetite user is empty** | Fans have None access (D-05, personas.md); a pro who declared nothing is not matchable on appetite (D-02) — the opt-in default, not an opt-out | Empty state: "You're not currently listed as open to anything. [Add a signal]". No signal is ever created on their behalf |
| **"Open to everything" breadth-spammer** — every appetite set to maximise inbound | Degrades market precision for everyone; indistinguishable from the genuine multi-hyphenate from the signal alone (DT-03, Q-02) | `[PENDING — Step 5 Must-depth]` discriminator: breadth credible only where the credit graph (domain 02) evidences it. Penalising breadth outright would penalise the core persona |
| **Concurrent edit** — user updates a signal on two devices, or edits mid-refresh-prompt | Last-write-wins on a per-role signal; the refresh prompt for a signal edited in-flight is dismissed against the newer value | Newer edit persists; a stale refresh prompt silently resolves rather than overwriting the fresh edit |
| **Signal contradicts booking calendar** — open to gigs, calendar (16/17) full | Not reconciled — appetite and calendar are different facts (D-04). An Operator's booked Tuesday is a hard resource constraint; an appetite is a mood. The matcher reads both but neither overwrites the other | Both remain as declared; the matcher may down-rank on the calendar conflict without touching the appetite signal |
| **Advertising availability while unreachable** — signal set but reachability policy (03.04.04) blocks everyone | A self-inflicted dead end: the user is discoverable but no one can contact them | Contradiction warning at declaration time: "You're open to work but your contact settings block new messages — collaborators can't reach you. [Review reachability]" |
| **Producer's signal never updates** — lives in a DAW, per personas.md the predictable outcome | Exactly why default-on (DT-02) and non-expiry (DT-01) both fail this persona; expiry + cheap one-tap refresh in a surface they already open is the mitigation | Refresh prompts are one tap and never require leaving to a web form; an untended signal lapses honestly rather than lying |

### States

| State | Trigger | What User Sees |
|-------|---------|---------------|
| Loading | Profile/settings opening | Skeleton |
| Empty | No signals declared | Not matchable on appetite — and per DT-02 this should be the *default* rather than an opt-out |
| Populated | Signals active | Signals with freshness shown |
| Error | Write failed | Retry |
| Expiring | Freshness horizon approaching | Prompt to confirm or lapse |
| Lapsed | Horizon passed without confirmation | Signal is inactive; scoring stops reading it. User is told it lapsed rather than discovering it silently |

## Deep Think Annotations

| # | Hypothesis | Source | Outcome |
|---|-----------|--------|---------|
| DT-01 | Open-to status is a profile field the user maintains | The obvious design; how every platform does it | ❌ REJECTED: it is a **decaying claim**, and it is the only input in this domain that nothing in the world falsifies. A credit is fixed by a past event; a calendar is corrected by a booking; an appetite set in January is simply wrong by June and nothing tells anyone. Per personas.md, neither the Producer (in a DAW) nor the Musician (running a portfolio of gigs) will tend a form. Therefore signals must **expire by default** and require a cheap refresh. This is the single most important call in this feature: an unexpiring signal set makes the whole matcher untrustworthy within one year, and an untrusted matcher is not used. |
| DT-02 | An expert would expect this to be opt-**in**, defaulting to silent | Adversarial reasoning about the Producer/Operator inbound flood (03.01.02 DT-01) | ✅ CONFIRMED: defaulting people to "open" maximises marketplace liquidity and destroys the experience of the exact personas whose presence makes the platform valuable. A Producer defaulted to "open to mix work" gets the "check out my beat" flood on day one and leaves. Liquidity gained by default-on is fake liquidity — it is people who did not choose to be there, and their non-response teaches the demand side that the market is dead. |
| DT-03 | Appetite breadth should be unlimited | Naive | ⏳ DEFERRED to Step 5: a user open to everything is either genuinely versatile (the multi-hyphenate, which is the *core persona* — personas.md explicitly warns against penalising simultaneity) or gaming inbound. These are indistinguishable from the signal alone, and penalising breadth would penalise the persona this product is built for. Possible discriminator: breadth is credible when the credit graph evidences it across those roles. Recorded as Q-02 rather than guessed. |

## Cross-Cut Notes

- Touches **03.03.02 Match Scoring** ([03.03.02-match-scoring-fit-signals.md](./03.03.02-match-scoring-fit-signals.md)) — the appetite input; see `03.03-collaborator-discovery-matchmaking-cx.md#CX-01`
- Touches **03.03.04 Open Collaboration Calls** ([03.03.04-open-collaboration-calls.md](./03.03.04-open-collaboration-calls.md)) — calls match against signals; this pairing is the market (`#CX-02`)
- Touches **01 Identity, Profiles & Organizations** ([01-identity-profiles-organizations/](../../01-identity-profiles-organizations/)) — the signal renders on a profile; ownership is contested (parent Q-02)
- Touches **16 Venues, Studios & Spaces** ([16-venues-studios-spaces/](../../16-venues-studios-spaces/)) and **17 Live Booking & Settlement** ([17-live-booking-settlement/](../../17-live-booking-settlement/)) — calendar availability is a *different fact* from appetite and must not be merged with it
- Touches **05 Services Marketplace** ([05-services-marketplace/](../../05-services-marketplace/)) — "open to paid mix work" is arguably a 05 supply signal; boundary hazard, see Q-03
- Touches **04 Opportunities & Casting** ([04-opportunities-casting/](../../04-opportunities-casting/)) — the same signal should serve 04's casting matcher; if 04 builds its own, a user maintains two truths and both rot

## Decisions

| # | Decision | Context |
|---|----------|---------|
| D-01 | Signals expire by default and require cheap refresh | DT-01 — the only input nothing falsifies; without expiry the matcher is untrustworthy within a year. |
| D-02 | Signals are opt-in, defaulting to silent | DT-02 — default-on liquidity is fake liquidity and burns the Producer persona immediately. |
| D-03 | Appetite is per-role and multiple, never a single status | personas.md — the multi-hyphenate's roles are simultaneous; one status reproduces the "artist OR producer?" failure. |
| D-04 | Appetite (here) and calendar availability (16/17) are different facts and are not reconciled | An Operator's booked Tuesday is a hard resource fact; an appetite is not. Merging them would let a matcher treat a booking as a preference. |

## Open Questions

| # | Question | Owner | Deferred To |
|---|----------|-------|-------------|
| Q-01 | What is the expiry horizon (DT-01)? Too short and it nags the personas least willing to be nagged; too long and the ghost problem returns. It likely differs per appetite type. | User | `/ideate-discover` Step 5 |
| Q-02 | Should appetite breadth be constrained or credibility-weighted (DT-03), given that penalising breadth penalises the core multi-hyphenate persona? | User | `/ideate-discover` Step 5 |
| Q-03 | Is "open to paid mix work" a signal here or a supply listing in domain 05? If both, the user maintains two truths that will diverge. | User | `/ideate-validate` |
| Q-04 | Is the signal public, or visible only to those who can reach you (03.04.04)? A public availability index is scrapeable (Q-03 on 03.03.01) and tells competitors exactly who is between jobs. | User | `/create-prd-security` |


<!-- spec-graph: auto-generated -->
## Related Specs

### Constrained by
- [[decisions.md#d-05|D-05]]
- [[decisions.md#d-02|D-02]]
- [[decisions.md#d-03|D-03]]
- [[decisions.md#d-15|D-15]]
- [[decisions.md#d-01|D-01]]
- [[decisions.md#d-04|D-04]]
