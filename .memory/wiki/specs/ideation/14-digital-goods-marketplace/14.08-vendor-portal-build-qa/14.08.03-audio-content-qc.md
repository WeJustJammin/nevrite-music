# Feature: Audio Content QC & Metadata Verification

> **Number**: 14.08.03
> **Parent**: [14.08 Vendor Portal, Build Submission & QA](./14.08-vendor-portal-build-qa-index.md)
> **Status**: [PARTIAL]
> **Last updated**: 2026-07-17

## Overview

Checking audio content before it publishes — clipping, true-peak, loudness, dead silence,
truncated files, wrong sample rates — and verifying the declared musical metadata (key, BPM) that
the whole search index depends on (14.04 CX-01). This is the content half of the submission gate,
sharing nothing with executable QA but the moment it fires (`#R-01`).

## Role Lens

| Role | Access | What They See/Do |
|------|--------|-------------------|
| Musician | Full | **Their gate.** The realistic vendor (14.01.01 DT-02) is a musician with a folder of samples, and this is the only QA they will ever meet. It must read as a helpful engineer's second pair of ears — "3 of these clip, 12 are labelled in the wrong key" — rather than a gatekeeper rejecting their work. |
| Producer | Full | Same, at higher volume and with higher standards. Also the persona most likely to have *deliberately* clipped or distorted content, which is why QC must warn rather than refuse (DT-01). |
| Operator | None | No column. Studios do not publish sample packs. Explicit absence. |
| Fan | None | Not on the fan surface. |

## Behavior

On submission of audio content (CX-02), each file is analysed: peak and true-peak levels,
integrated loudness, DC offset, dead silence at head/tail, truncation, declared vs actual sample
rate and bit depth. Separately, musical metadata is **extracted** (key, BPM) and cross-checked
against the vendor's declaration.

The central design principle is that most findings are **warnings, not blockers** (DT-01) — a
clipped break may be the product. Extraction disagreeing with a declaration is a *signal*, not a
verdict, and whose value wins is unresolved (14.04.01 Q-03, and 14.04.04 D-04's recommendation:
record both, rank the agreement).

For a 5,000-file pack this is long-running and async (CX-02 q2).

**This gate also writes.** Per `14.03.01` D-12 (owner ratification, 2026-07-30) this feature owns
the **embedded tempo/key/loop metadata write** — the ACIDized-WAV / Apple-Loops convention that
makes a loop drop into a DAW at the right tempo (14.04.01 DT-03, D-06). It happens **here, once, at
ingest, into the stored master**, immediately after reconciliation and before publish. It used to
be specified as a delivery-time write, which was wrong on the merits and fatal to delivery's
integrity contract: "this loop is 142 BPM" is a fact about the product, identical for every buyer,
and re-deriving it per download meant no published SHA-256 could ever describe the delivered bytes.
The values written are exactly the reconciled ones (14.04.01 D-05), which is why this gate is the
right owner — it is the only place that holds both the declaration and the extraction. Confidence
travels: an **estimated key is embedded only where declaration and extraction agree** (D-04), so a
guess is never stamped into a file that will outlive our database. A file whose key is honestly
undefined is written with no key rather than a fabricated one.

**A metadata correction is a re-write of the master, and it changes the master's hash.** Fixing one
wrong key on one loop is therefore not a cosmetic edit — it produces new bytes, a new published
SHA-256, and a revision event on the `14.03.02` append-and-correct path (`14.03.02` D-05). This gate
owns performing that re-write and re-hash; it does not own the revision semantics.

**What triggers a finding (severity, not exact thresholds — those are `[PENDING — /write-be-spec]`):**

| Check | Finding | Default severity |
|-------|---------|------------------|
| True-peak over ceiling (industry default ≈ -1 dBTP) | Inter-sample clipping likely | Warning — dismissible (a crushed 808 is the sound) |
| Sample-level clipping (consecutive full-scale samples) | Hard clip | Warning — dismissible |
| Head/tail dead silence past a threshold (e.g. > 5 ms) | Ruins DAW drag-drop timing | Warning — the high-value finding |
| Truncated file (abrupt non-zero tail cut) | Playback pop / incomplete render | Warning |
| DC offset above a small fraction of full scale | Headroom loss, click on layering | Warning |
| Declared sample rate/bit depth ≠ actual | The listing would lie | **Blocker** (D-01) |
| Extracted key/BPM ≠ declared | Metadata disagreement | Signal, not verdict — both recorded (D-02) |

Exact numeric thresholds (dBTP ceiling, silence ms, DC-offset percentage, consecutive-sample count) are a
backend-spec decision and are deferred, but the *shape* — warn-by-default, one narrow blocker class — is locked here.

### Happy Path

1. Vendor uploads a pack (14.08.01).
2. Each file is analysed; metadata is extracted per file.
3. Extraction is cross-checked against the vendor's declared key/BPM.
4. Results return as a per-file report: 3 clip, 12 have head silence, 8 disagree on key.
5. Vendor fixes what they meant to fix and dismisses what was intentional.
6. **Reconciled tempo/key/loop metadata is embedded into the stored master** (D-04), estimated keys only where declaration and extraction agree.
7. The master is hashed; that SHA-256 is the one delivery publishes for this build (`14.03.01` D-12).
8. Publish proceeds; the extracted metadata also feeds the index (14.04 CX-01) with a confidence value (14.04.04 D-04). **The index and the file now carry the same values from the same write** — which is the point: an index that knows 142 BPM and a file that does not is a broken product with excellent search.

### Edge Cases / Failure Modes

| Scenario | What Happens | User Sees |
|----------|-------------|-----------|
| Clipped file — deliberately (a crunchy break, a distorted 808) | Blocking this would refuse the product the vendor meant to make | Warning, dismissible with a reason. Never a blocker — see DT-01 |
| Extraction says F#, vendor says F | One of them is wrong and we cannot tell which | Both recorded; agreement ranked higher (14.04.04 D-04). Not silently overridden |
| Key is genuinely undefined (kick, noise sweep, atonal texture) | Forcing a value poisons the index (14.04.04 DT-02) | Nullable, honestly | 
| 5,000-file pack | QC takes a long time | Async with progressive results; the vendor is not blocked at a spinner |
| Dead silence at file head | Ruins drag-and-drop timing in a DAW — a real defect the vendor won't notice | Warning with the offset; the kind of finding that earns the feature its trust |
| Pack contains an uncleared third-party recording | **QC cannot see this.** Loudness analysis says nothing about provenance. | The domain's central exposure and this gate is blind to it — see DT-02 |
| Sample rate declared 48k, files are 44.1k | Straightforward mismatch | Blocker — the spec sheet would lie (14.01.01) |
| **Vendor corrects one loop's key after publish** | Per D-04 this is not an edit to a database row — the metadata lives **in the file**, so correcting it re-writes the master and changes its published SHA-256. It becomes a revision on `14.03.02` D-05's append-and-correct path; existing holders keep the build they hold and are offered the corrected one. | "Fixing this re-issues the pack as a new version. Anyone who already downloaded it keeps their copy and will be offered the fix." Never a silent in-place edit — a buyer's verified hash must not stop matching under them. |
| **Metadata correction on a watermarked pack** | Unchanged here. The new master is hashed as normal; per `14.03.01` D-12 the marked-delivery per-grant hash is derived from whichever master a grant resolves to, so a correction needs no special handling on the delivery side. | Nothing beyond the revision notice above. |

### States

| State | Trigger | What User Sees |
|-------|---------|---------------|
| Loading | QC running | "Checking 5,000 files — 3,200 done" |
| Empty | No audio submitted | Not shown — software vendors never meet this |
| Populated | Report ready | Per-file findings grouped by type, with severity |
| Error | Analysis failed | Retry; a QC failure on content should not permanently block a publish the way it does for code |
| Partial | Some files analysed | Progressive results |
| Blocked | A genuine blocker (declared/actual format mismatch) | Named blocker + remedy |

## Deep Think Annotations

| # | Hypothesis | Source | Outcome |
|---|-----------|--------|---------|
| DT-01 | Audio QC should reject substandard content — clipping, silence, wrong loudness are defects | The candidate's phrasing ("audio QC" listed alongside malware scanning as a gate), which imports executable QA's binary pass/fail | ❌ REJECTED — **most "defects" in music content are intentional, and a gate that cannot tell the difference will refuse the product the vendor meant to make.** A clipped, crushed 808 is the sound; head silence may be a deliberate pre-roll; a -3 LUFS lo-fi break is a genre, not an error. Applying executable QA's binary posture here (14.08.02 D-01: unconditional, non-waivable, fails closed) would reject the store's most plausible inventory for being *correctly made*. The right shape is inverted: **warnings the vendor can dismiss with a reason**, not blockers — with a hard blocker reserved for the narrow class where the *listing would lie* (declared 48k, actual 44.1k). The valuable findings are the ones the vendor didn't intend and can't hear: a 4 ms head silence that ruins drag-and-drop timing, a truncated tail, a DC offset. That reframes the feature from gatekeeper to **an engineer's second pair of ears** — which is also the only framing a musician-vendor will tolerate between sessions. |
| DT-02 | QC verifies the content is what it claims to be | The natural read of "quality control + metadata verification" — the feature name implies verification of claims | ❌ REJECTED, and the gap is the domain's whole risk profile in one line. QC verifies the claims that **don't matter much** (is it 48k, is it clipping, is it really 142 BPM) and is structurally blind to the one that matters most: **does the vendor hold the rights to this audio?** Loudness analysis cannot see a Motown break. Key extraction cannot see an uncleared vocal. So the store's central exposure (14.04.01 DT-02: a pack sold with rights its vendor never held, exposing every downstream release) passes straight through this gate untouched, and the only thing standing between it and 400 buyers is an attestation (14.08.01 DT-01) that traces harm rather than preventing it. The honest options are narrow and all cost real money: **audio fingerprinting against commercial recordings** at submission (the same capability 14.05.02 Q-03 wants for re-sold exclusives — worth noticing they are one investment), human review on flagged submissions, or accepting the exposure and pricing it. Naming this here rather than discovering it in production is most of what this annotation is for. |

## Cross-Cut Notes

- Touches **14.08.01 Submission** ([path](./14.08.01-vendor-onboarding-product-submission.md)) — **High confidence**: same gate, disjoint checks. See `14.08-vendor-portal-build-qa-cx.md#CX-02`.
- Touches **14.08.02 Build QA** ([path](./14.08.02-automated-build-qa.md)) — **rejected** as a pair (`#R-01`). They share a moment and nothing else.
- Touches **14.08.04 Staged Rollout** ([path](./14.08.04-release-channels-staged-rollout.md)) — **rejected** (`#R-02`): content has no runtime, so nothing to stage.
- Touches **14.04.04 Musical Metadata Index** ([path](../14.04-sound-content-catalogs/14.04.04-musical-metadata-index-search.md)) — **High confidence**: extraction feeds the index with a confidence value (14.04.04 D-04).
- Touches **14.04.01 Sample Packs** ([path](../14.04-sound-content-catalogs/14.04.01-sample-loop-pack-catalog.md)) — this is that feature's gate, and per D-04 the **executor of its D-06**: the embedded-metadata write happens here, not at delivery.
- Touches **14.03.01 Delivery** ([path](../14.03-delivery-versioning-library/14.03.01-download-delivery-resumable-transfer.md)) — **new, per `14.03.01` D-12**: this gate's write is what makes delivery's published SHA-256 truthful. Because the metadata is already in the master, an unmarked artifact is served byte-identical to it. Delivery serves and never authors; the ordering dependency is ingest-before-grant, which is structurally guaranteed — nothing is purchasable before it publishes.
- Touches **14.03.02 Versioning & Archive** ([path](../14.03-delivery-versioning-library/14.03.02-versioning-updates-legacy-archive.md)) — **new, per D-04**: a metadata correction re-writes the master and changes its hash, entering that feature's append-and-correct revision path (`14.03.02` D-05). This gate performs the re-write; it does not decide the revision semantics.
- Touches **14.05.02 Exclusivity** ([path](../14.05-beat-instrumental-licensing/14.05.02-exclusive-rights-purchase-auto-delist.md)) — per DT-02, audio fingerprinting serves both this gate and re-sold-exclusive detection (14.05.02 Q-03). **One investment, two features.**
- Touches **09 Rights & Ownership** ([path](../../09-rights-ownership/)) — per DT-02, the rights question this gate cannot answer belongs there.
- Routed to `/create-prd-architecture`: audio fingerprinting against commercial recordings, if pursued.

## Decisions

| # | Decision | Context |
|---|----------|---------|
| D-01 | Findings are dismissible warnings by default; blockers are reserved for cases where the listing would lie | Per DT-01. Most "defects" in music content are intentional, and refusing them rejects the store's realistic inventory for being correctly made. |
| D-02 | Extraction disagreeing with a vendor's declared metadata is recorded as a signal, not an override | Per 14.04.04 D-04 — record both, rank the agreement. Neither source is reliably right. |
| D-03 | QC does not claim to verify provenance, and the UI must not imply it | Per DT-02 — the gate is blind to the domain's central risk, and implying otherwise is worse than saying nothing. |
| D-04 | **This feature owns the embedded tempo/key/loop metadata write — once, at ingest, into the stored master — and owns re-writing and re-hashing it on a metadata correction** | Per `14.03.01` D-12 (owner ratification of run-3 finding `14-digital-goods-marketplace#001`, 2026-07-30). The write was previously specified at delivery (14.04.01 D-06, "delivered files carry"), which collided head-on with delivery's single published SHA-256 and would have failed verification on every content download. Ingest is the correct owner on the merits, independent of the collision: the values are a property of the product, not the buyer, and this gate is the only place holding both the vendor's declaration and the extraction that D-05 of 14.04.01 reconciles between them. Two consequences this feature now carries. (1) **Confidence travels** — an estimated key is embedded only where declaration and extraction agree, consistent with D-02's "signal, not override"; a key that is honestly undefined is written absent, never fabricated (14.04.04 DT-02). (2) **A correction is a new build** — re-writing one loop's key produces new master bytes and a new published hash, handed to `14.03.02` D-05's revision path. Reversibility: **Low** — delivery's integrity contract now depends on this write having already happened. |

## Open Questions

| # | Question | Owner | Deferred To |
|---|----------|-------|-------------|
| Q-01 | Per DT-02: is **audio fingerprinting against commercial recordings** run at submission? It is the only automated defence against the domain's central exposure, and the same capability 14.05.02 Q-03 needs — one investment serving two features. Real cost, real licensing (ACRCloud/Audible Magic-class). Deferral re-pointed from the completed `/ideate-validate` to `/create-prd-stack`: the question is a named third-party provider selection with a cost and licensing profile, which is the stack stage's decision shape (options + trade-offs, owner picks). The Cross-Cut Notes' existing `/create-prd-architecture` routing covers the *how*, "if pursued"; this row is the *whether* and the provider. | User | `/create-prd-stack` |
| Q-02 | ~~Does the platform extract metadata at all, or trust the vendor (14.04.01 Q-03)? Key detection is genuinely unreliable; a wrong extraction that overrides a right declaration is worse than no extraction.~~ **RESOLVED — extraction runs, and the override rule is per field class, never global.** Per `14.04.01` D-05: "**Metadata reconciliation is per field class, not per pack** — deterministic fields hard-block on disagreement; loop BPM is arithmetic and wins; key never overrides a declaration", which that decision states "satisfies 14.08.03 D-02's 'signal, not override' without pretending a header read is a guess". So: deterministic fields (sample rate, bit depth) → extraction wins and the mismatch is this feature's one blocker class (D-01); derivable loop BPM → the arithmetic wins; estimated key → neither wins, both recorded, only agreement ranks highly (`14.04.04` D-04). `14.04.01` Q-03 and `14.04.04` Q-03 are both marked resolved on the same ruling. | Agent | ✅ Resolved (2026-07-23) |
| Q-03 | **[OWNER]** Is there human review on high-risk audio submissions (sample-heavy, fingerprint-flagged), or attestation only? Deferral re-pointed from the completed `/ideate-validate` to `/create-prd`: same call as 14.08.01 Q-02 on the content side — whether the platform staffs a review function is an ops-cost and risk-acceptance decision no later stage derives. | User | `/create-prd` |


<!-- spec-graph: auto-generated -->
## Related Specs

### Constrained by
- [[decisions.md#d-04|D-04]]
- [[decisions.md#d-12|D-12]]
- [[decisions.md#d-06|D-06]]
- [[decisions.md#d-05|D-05]]
- [[decisions.md#d-01|D-01]]
- [[decisions.md#d-02|D-02]]
- [[decisions.md#d-03|D-03]]
