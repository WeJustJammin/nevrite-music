/**
 * Wrangler entrypoint. Keep this module default-only: `src/index.ts` is also
 * a public application factory and intentionally exposes named contracts.
 */
import handler from './index';

export default handler;
